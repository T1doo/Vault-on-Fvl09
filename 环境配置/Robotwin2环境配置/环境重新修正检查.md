# RoboTwin 2 环境重新修正检查（fvl09 → fvl05）

> 开始时间：2026-08-09 23:46 CST  
> 完成时间：2026-08-10 01:49 CST  
> 当前服务器：fvl05  
> 工作区：`/nfs_share/lijunhui/Robotwin2`  
> 状态：已完成（基础 simulator 端到端验收通过）

## 目标

对从 fvl09 直接迁移到 fvl05 的 RoboTwin 2 基础环境做一次从文件到实际运行的完整复核。检查迁移后的绝对路径、Python/Conda 环境、个人目录内 CUDA 工具链、依赖元数据、SAPIEN/CuRobo、GPU 计算、官方渲染和最小数据采集链路。若迁移环境缺乏可靠性或不能通过验证，允许删除并重建个人目录内可再生成的 RoboTwin 2 环境、CUDA 工具链与缓存。

## 安全边界与保留项

- 所有可控写入与删除只允许发生在 `/nfs_share/lijunhui` 内，不运行 `sudo`，不修改系统 CUDA、驱动或其他用户的文件和进程。
- 删除前精确确认目标；可以重建 `Robotwin2/env`、`Robotwin2/tools` 中项目专用工具链及可再生成缓存。
- 保留官方源码仓库、配置与审计记录、`datasets`、`models`，以及现有 smoke 配置和采集产物；除非后续证据显示某个保留项本身损坏并先明确记录。
- GPU 操作前检查实时占用，只选择当时空闲、健康的设备，不停止或干扰他人任务。
- 不执行 Git commit 或 push。

## 实时工作记录

### 2026-08-09 23:46 CST：任务建立与编辑前基线

- 用户要求完整检查迁移环境，并明确表示：个人目录内 RoboTwin 相关环境与 CUDA 等组件可以直接删除后重新配置。
- 新建本文档作为本轮专项实时记录；既有 `RoboTwin2环境配置实时日志.md` 继续作为项目总时间线同步追加关键节点。
- 编辑前检查：
  - 官方仓库 `/nfs_share/lijunhui/Robotwin2/project/RoboTwin` 位于 `main...origin/main`，工作树干净。
  - Vault 仓库已有与本轮并存的未提交修改；不覆盖、不暂存、不提交这些改动。
  - `Robotwin2` 当前仍有迁移来的 `env`、`cache`、`tools/cuda-12.1`、`tools/miniforge3` 等目录。
- 已读取既有实时日志末尾。2026-08-04 的记录只证明：路径曾做迁移修补，PyTorch CUDA 与官方 `test_render.py` 曾在获批宿主机 GPU 上通过；fvl05 尚未重跑数据采集 smoke test。本轮不把历史通过当作当前完整验收。
- 下一步：盘点仓库版本、安装说明、现有环境清单、残留旧路径、软链接、包来源与活动进程，形成“保留修复 / 全量重建”的证据。

### 2026-08-09 23:52 CST：迁移残留审计与重建决策

- 用户补充提醒：fvl05 的显卡与 fvl09 不同。后续不复用 fvl09 的 GPU 索引、UUID、坏卡结论或 Vulkan 选择策略；以 fvl05 的 RTX A6000（compute capability 8.6）、当前驱动、实时占用和实际 CUDA/SAPIEN 验证为准。
- 当前官方 RoboTwin 仓库：commit `c3ddfa8b97d5519efa828b075999bd0006778e5e`，`main...origin/main`，工作树干净。
- 重新审计官方 `script/_install.sh`：它会安装 requirements、从未固定 commit 的 PyTorch3D `stable` 分支构建、直接修改 SAPIEN 与 MPLib 的环境内源码、克隆 CuRobo v0.7.8 并 editable 安装；未发现 `sudo` 或系统包管理器，但不能直接盲跑。
- 两个本地依赖源码仓库均无本地改动：
  - CuRobo v0.7.8 commit `d64c4b005459db10c5dd867d8b30a87d5bda9bdb`；
  - PyTorch3D commit `75ebeeaea0908c5527e7b1e305fbc7681382db47`（0.7.8）。
- 当前环境的表面检查仍有部分通过项：`pip check` 无冲突；个人目录内 `nvcc` 为 CUDA 12.1.105；SAPIEN URDF 与 MPLib planner 的官方要求补丁仍在。
- 但精确搜索旧根路径 `/bigbig_nfs_share/lijunhui` 得到决定性异常：
  - 迁移后的 `env` 与 `tools/miniforge3` 中至少 1,361 个脚本、二进制或 `.pyc` 文件仍嵌有旧路径；此前只修补部分 shebang/元数据，不能证明整个 Conda 前缀可安全搬迁。
  - 6 份实际生成的 embodiment CuRobo 配置全部仍引用旧根路径：ARX-X5、franka-panda、piper、ur5-wsg、aloha 左臂与右臂。这会直接破坏涉及对应资产路径的规划/采集。
- 决策：不再继续修补迁移前缀，执行干净重建。保留大体量官方 assets、源码、数据、模型与 smoke 产物；重建可再生成的 Python/Conda 前缀、Miniforge、项目 CUDA 工具链、依赖编译产物与缓存，并重新生成 6 份 CuRobo 路径配置。
- 删除前下一步：导出当前版本/来源基线，核验 assets 完整性和磁盘空间，检查没有使用目标目录的活动进程，再记录精确删除清单。

### 2026-08-09 23:59 CST：硬件重新定标、资产保护核验与重建范围冻结

- 获准在宿主机设备上下文只读查询 fvl05 当前 GPU：8 张均为 NVIDIA RTX A6000 48 GiB，驱动 `535.274.02`，compute capability 8.6 与当前构建目标一致；官方当前文档仍推荐 Python 3.10 / CUDA 12.1，驱动要求也满足。
- 查询瞬间 GPU 0、1 仅 16 MiB / 0%，GPU 2–7 约 25.5 GiB / 100%，显然有其他项目运行。未查看、停止或影响任何他人进程。之后的任何 GPU 测试都必须临启动前复查；若无明确空闲卡，则不运行 demo。
- 当前官方在线文档与 `main` 已在 2026-08 初引入 XPolicyLab、`scripts/`、`env_cfg/` 和新的数据布局，而本地已验证源码是 2026-05-23 的 commit `c3ddfa8...`，使用 `script/`、`task_config/` 和旧数据布局。升级源码会同时引入代码、子模块、配置与数据格式迁移，超出本次“把既有项目在 fvl05 上可靠重建环境”的范围。因此本轮固定本地 commit，不暗中升级源码；最新上游升级作为独立决策保留。
- 保留资产核验：三个官方 ZIP 的字节数仍与首次下载记录一致，并分别通过完整 CRC 测试：
  - `background_texture.zip` 10,970,687,027 bytes；
  - `objects.zip` 3,737,778,549 bytes；
  - `embodiments.zip` 219,859,313 bytes。
- 已确认既有 `demo_clean_smoke` 的 3 个 HDF5、视频、指令、轨迹、seed 与 scene info 均仍存在；不会覆盖或复用该输出目录。
- 服务器个人 NFS 当前可用空间约 27 TiB，足够重建。已验证保留的 Miniforge 安装器 SHA-256 为 `42260ffe3830fb953d5eee1bbb32229ff06aa7c3833c1ed7a9a0420a95685d94`，与 2026-07-25 官方校验记录一致。
- 宿主机进程检查：除检查命令自身外，`lijunhui` 没有使用 `/nfs_share/lijunhui/Robotwin2` 的活动进程。
- 精确重建清单：
  - 删除并重建 `Robotwin2/env`；
  - 删除并重建 `Robotwin2/tools/miniforge3`；
  - 删除并重建 `Robotwin2/tools/cuda-12.1`；
  - 删除并重建 `Robotwin2/cache`；
  - 仅在干净的 CuRobo 依赖仓库内清除已预览的 `.so` 与 `__pycache__` 构建产物，再从固定源码重编译；
  - 保留 `project/RoboTwin`、`project/pytorch3d`、三个资产 ZIP 与解压目录、数据、模型、配置、历史日志和 Miniforge 已校验安装器。
- 下一步：执行上述精确删除，立即复核保留项与 Git 状态，然后从零安装 Miniforge、Python 3.10 基础环境和个人 CUDA 12.1 工具链。

### 2026-08-10 00:04 CST：旧迁移前缀精确删除完成

- 删除前再次确认主仓库与 CuRobo 仓库无本地修改，并重读本文档当前尾部。
- 已顺序删除且确认消失：
  - `/nfs_share/lijunhui/Robotwin2/env`
  - `/nfs_share/lijunhui/Robotwin2/tools/miniforge3`
  - `/nfs_share/lijunhui/Robotwin2/tools/cuda-12.1`
  - `/nfs_share/lijunhui/Robotwin2/cache`
- 为降低共享 NFS 元数据压力，大目录按顺序删除，没有并发清理；所有命令正常退出。
- 在 CuRobo 固定源码仓库内执行了预览过的精确 `git clean -fdx`，只移除了 5 个旧 CUDA 扩展 `.so` 与 Python `__pycache__`；源码仍在固定 commit，未改 tracked 文件。
- 删除后复核：`datasets`、`models`、`config`、`tmp`、`project/RoboTwin`、`project/pytorch3d` 全部保留；官方主仓库仍为干净的 `main...origin/main`；Miniforge 已校验安装器与既有 smoke 日志/数据均保留。
- 可恢复性：被删除内容是可再生成的环境、工具链、缓存与编译产物，不能从回收站直接恢复；本轮将依据固定版本重新生成。
- 下一步：使用已核验的离线 Miniforge 安装器创建全新环境管理器，然后联网安装 Python 3.10 基础前缀与个人 CUDA 12.1 工具链。

### 2026-08-10 00:12 CST：Miniforge 安装器边界问题与合规断点续装

- 第一次运行已校验的 Miniforge batch 安装器时，payload 完整解包，但脚本在创建 `/home/lijunhui/.conda` 时因只读边界退出。没有判定成功，也没有在 home 或系统留下文件。
- 检查安装器 shell 头确认两个硬编码行为：它先 `unset CONDARC`，随后直接 `mkdir ~/.conda`，并在 base 安装命令中强制 `CONDA_REGISTER_ENVS=true`。因此仅传项目 `CONDARC` 无法满足“RoboTwin 可控状态全部位于 Robotwin2 根目录”的要求。
- 在项目 `config/condarc` 中新增 `register_envs: false`，防止后续普通 Conda 命令登记到 `~/.conda/environments.txt`。
- 删除第一次半成品后，第二次解包仍在相同硬编码位置按预期停止。随后不修改已校验安装器和 payload，从准确断点手动执行安装器原本的固定 base transaction，但把 `CONDA_REGISTER_ENVS` 改为 `false`。
- 默认文件沙箱首次执行 transaction 时因无法访问服务器的 `127.0.0.1` 代理而失败；已按网络边界要求在获准联网上下文重试。89 个固定包全部成功链接，transaction 正常结束。
- 按安装器后续逻辑完成 `postconda.tar.bz2` 解包，并清理 `postconda.tar.bz2`、`install_tmp` 与 `.messages.txt` 中间项；没有运行 `conda init`。
- 验证：
  - Conda `26.3.2`；Mamba `2.6.0`；base Python `3.13.13`；
  - 三者前缀均为 `/nfs_share/lijunhui/Robotwin2/tools/miniforge3`；
  - 新 Miniforge 的 `bin`、`conda-meta` 和 Python site-packages 中无旧根路径 `/bigbig_nfs_share/lijunhui`；
  - `/home/lijunhui/.conda` 不存在；没有全局或 home Conda 登记状态。
- 下一步：用新 Mamba 在 `/nfs_share/lijunhui/Robotwin2/env` 创建 Python 3.10 基础环境，并从 NVIDIA 固定 channel 在 `/nfs_share/lijunhui/Robotwin2/tools/cuda-12.1` 创建 CUDA 12.1.1 工具链。

### 2026-08-10 00:17 CST：全新 Python 3.10 基础前缀完成

- 编辑/安装前复核官方主仓库仍干净，并重读专项日志尾部。
- 使用新 Mamba 从 conda-forge 创建 `/nfs_share/lijunhui/Robotwin2/env`，安装 165 个包、下载约 221 MB，transaction 正常结束。
- 实际基础版本：Python `3.10.20`、pip `26.2.1`、CMake `4.4.2`、Ninja `1.13.2`、ffmpeg `8.1.2`、UnZip `6.00`。
- 路径检查：新环境的 `bin`、`conda-meta` 与 Python 标准库中无 `/bigbig_nfs_share/lijunhui`；ffmpeg 编译信息中的 `/home/conda/feedstock_root/...` 是 conda-forge 构建机元数据，不是本机运行路径或越界写入。
- 发现 Mamba 2.6.0 不识别 Conda 的 `register_envs` 配置：尽管命令显式设置为 false，仍新建 `/home/lijunhui/.conda/environments.txt`，且其中只有本轮环境前缀一行（34 bytes）。
- 已精确删除本轮创建的 `/home/lijunhui/.conda`，复核前该目录在 Miniforge 安装完成后并不存在，因此未误删既有用户状态。后续前缀创建改用 Conda CLI，以遵守 `register_envs: false`。
- 下一步：使用 Conda CLI 和 NVIDIA 固定 `cuda-12.1.1` channel 创建个人 CUDA Toolkit 前缀，再复核 home 无登记残留。

### 2026-08-10 00:26 CST：个人 CUDA 12.1.1 工具链从零创建完成

- 使用项目内 Conda CLI 从 NVIDIA 官方固定 channel `nvidia/label/cuda-12.1.1` 创建 `/nfs_share/lijunhui/Robotwin2/tools/cuda-12.1`；transaction 正常结束，共安装 64 个包，约 3.29 GB。
- 该前缀只包含用户态 CUDA Toolkit，不包含也未修改 NVIDIA 内核驱动；未调用 `sudo`、系统包管理器或共享 CUDA。
- 实测 `nvcc` 为 CUDA `12.1`、构建号 `V12.1.105`；`cuda.h`、`cuda_runtime.h` 与 `cub/cub.cuh` 均存在，cuBLAS、cuFFT、cuSPARSE、cuSOLVER 等开发库来自同一固定 NVIDIA channel。
- 激活脚本通过 `sh -n`；激活后 `python`、`pip`、`nvcc` 分别解析到新 Python 前缀和个人 CUDA 前缀，`CUDA_HOME=/nfs_share/lijunhui/Robotwin2/tools/cuda-12.1`，`TORCH_CUDA_ARCH_LIST=8.6`，且不会把 `/share/apps/cuda` 或 `/usr/local/cuda` 放在项目工具链之前。
- 新 CUDA 的 `bin` 与 `conda-meta` 中无旧根路径 `/bigbig_nfs_share/lijunhui`。
- Conda 曾创建空的 `/home/lijunhui/.conda/environments.txt`（0 bytes）；确认它是本轮新建且不含任何状态后已精确清理。最终复核 `/home/lijunhui/.conda` 不存在。
- 下一步：安装固定 Python 依赖，按已审计的上游安装逻辑应用 SAPIEN/MPLib 兼容补丁，并针对 RTX A6000（sm_86）重编译 CuRobo 与 PyTorch3D。

### 2026-08-10 00:45 CST：官方基础 Python 依赖安装完成，发现并定位 Setuptools 兼容项

- 在项目激活变量生效且缓存、临时目录均固定到 `Robotwin2` 的联网环境中执行 `python -m pip install -r script/requirements.txt`；命令正常退出。
- 解析结果保持核心兼容组合：PyTorch `2.4.1`、TorchVision `0.19.1`、SAPIEN `3.0.0b1`、MPLib `0.2.1`、NumPy `1.26.4`、SciPy `1.10.1`、Open3D `0.18.0`、Zarr `2.18.3`。`pip check` 报告 `No broken requirements found`。
- 首轮批量导入没有误标成功：导入 `sapien` 时出现 `ModuleNotFoundError: No module named 'pkg_resources'`。根因是基础前缀带来的 Setuptools `84.0.0` 已不再提供旧版 SAPIEN 依赖的 `pkg_resources`。
- 该问题已与仓库 `script/_install.sh` 的既定步骤对齐：上游明确固定 `setuptools==69.5.1`。下一步按这一已审计版本降级，再重新执行完整导入与 `pip check`；不会修改系统 Python。
- 同时确认尚未应用的两处上游兼容补丁原始上下文仍准确存在：SAPIEN URDF/SRDF UTF-8 与 `.srdf` 路径处理，以及 MPLib screw plan 的 `collide` 判断。

### 2026-08-10 00:54 CST：Setuptools 兼容修复及 SAPIEN/MPLib 补丁验证通过

- 第一次固定 Setuptools 的命令在默认文件沙箱中因无法连接服务器 `127.0.0.1` 代理而失败，没有发生安装变更；随后自动在已授权的项目联网范围重试，成功将 Setuptools `84.0.0` 精确替换为上游指定的 `69.5.1`。
- 修复后再次执行 `pip check`，仍为 `No broken requirements found`。完整核心导入通过：Python `3.10.20`、Setuptools `69.5.1`、NumPy `1.26.4`、SciPy `1.10.1`、torch `2.4.1+cu121`、torchvision `0.19.1+cu121`、SAPIEN `3.0.0b1`、MPLib `0.2.1`、Open3D `0.18.0`、Trimesh `4.4.3`、Gymnasium `0.29.1`、HDF5 `3.16.0`、Zarr `2.18.3`。
- 默认文件沙箱不暴露 GPU，因此此处 `torch.cuda.is_available()` 为 false，按既定边界只视为 CPU/动态库导入验证；不据此判断宿主 GPU 故障。
- 用 `apply_patch` 在隔离环境中应用上游实际执行的兼容修改：SAPIEN URDF/SRDF 文件读取显式使用 UTF-8；MPLib screw plan 移除 `or collide` 条件。两个文件均通过 `py_compile`，SAPIEN/MPLib 重新导入通过。
- 更正上一条中“`.srdf` 路径处理”的表述：现有表达式 `urdf_file[:-4] + "srdf"` 对 `robot.urdf` 已正确得到 `robot.srdf`，而官方脚本实际的 `sed` 也只修改编码；若额外加 `".srdf"` 会产生双点路径，因此本轮没有引入该错误。
- 一条组合验证命令在全部 Python 验证完成后因尾部 shell 引号书写错误退出；未造成文件变更，剩余两项源码上下文检查已用独立命令重跑并通过。
- 下一步：安装 CuRobo 的固定兼容依赖，使用个人 CUDA 12.1 和 `TORCH_CUDA_ARCH_LIST=8.6` 从干净 commit 重编译五个扩展。

### 2026-08-10 00:58 CST：CuRobo 依赖就绪；首次源码内构建被 NFS Git 安全检查阻止

- 已安装 CuRobo v0.7.8 所需依赖，并固定关键兼容版本：`warp-lang==1.12.0`、`scikit-image==0.21.0`、`scipy==1.10.1`、`setuptools==69.5.1`；另安装 pybind11、numpy-quaternion、yourdfpy、setuptools-scm、importlib-resources 等。安装命令正常结束。
- 依赖后 `pip check` 无冲突；编译预检确认 torch CUDA 为 12.1、`CUDA_HOME` 指向个人 Toolkit、目标架构为 8.6、nvcc 为 12.1.105、宿主只读编译器为 GCC/G++ 11.4.0。
- 首次从 `envs/curobo` 执行无隔离 editable 构建时，尚未进入 nvcc：Setuptools-SCM 的文件发现子进程忽略了已显式传入且含精确白名单的项目 Git 配置，因 NFS 所有者映射触发 `dubious ownership`，元数据生成退出。源码、Python 环境和 CUDA 前缀未损坏。
- 主仓库和 CuRobo 仓库直接 `git status` 均正常且干净；CuRobo 仍为固定 commit `d64c4b005459db10c5dd867d8b30a87d5bda9bdb`（tag v0.7.8）。不会关闭 Git 安全检查或添加通配白名单。
- 采用此前已验证的 NFS 安全方案：从该精确 commit 用 `git archive` 导出不含 `.git` 的版本化临时快照，设置构建版本 0.7.8，编译普通 wheel 并安装进隔离环境。这样既绕过 Setuptools 子进程的所有权误判，也不改变受版本控制源码。

### 2026-08-10 01:06 CST：CuRobo v0.7.8 的五个 A6000 CUDA 扩展编译、安装与加载通过

- 从 commit `d64c4b005459db10c5dd867d8b30a87d5bda9bdb` 导出的无 `.git` 快照完成普通 wheel 构建；使用个人 CUDA 12.1、`TORCH_CUDA_ARCH_LIST=8.6`、`MAX_JOBS=8`，全程不需要也未占用 GPU。
- 生成并成功安装 `nvidia_curobo-0.7.8-cp310-cp310-linux_x86_64.whl`；构建产物大小 `53,854,123` bytes，构建时记录的 SHA-256 为 `113c2e702b6d2b457496c4656d187526bf1c3fee3c8188b850052260aadf82ed`。
- 安装前缀中五个扩展均存在：`lbfgs_step_cu`、`kinematics_fused_cu`、`line_search_cu`、`tensor_step_cu`、`geom_cu`。`cuobjdump --list-elf` 在代表性 `geom_cu` 中明确列出 `sm_86.cubin`，确认不是继承 fvl09 的旧架构产物。
- 首次直接加载底层 `.so` 时因没有预先导入 torch 而报 `libc10.so` 未找到；这是 PyTorch C++ 扩展的动态库加载顺序，不是链接缺失。按 RoboTwin 实际顺序先 `import torch` 后，五个扩展全部加载成功。
- `pip check` 仍无冲突；主仓库和 CuRobo 固定源码仓库保持干净。
- 下一步：从已固定的 PyTorch3D `0.7.8` commit 导出同类无 Git 快照，针对个人 CUDA 12.1 / sm_86 编译并验证 `pytorch3d._C`。

### 2026-08-10 01:13 CST：PyTorch3D 0.7.8 / sm_86 编译、安装与 CPU 算子验证通过

- 先固定安装已知兼容依赖 `iopath==0.1.10` 与 `portalocker==3.2.0`；没有让解析器改动 torch、NumPy 或 SciPy。
- 没有复用迁移来的旧构建快照；从干净 commit `75ebeeaea0908c5527e7b1e305fbc7681382db47`（V0.7.8）新导出 `/nfs_share/lijunhui/Robotwin2/tmp/pytorch3d-75ebeea-fvl05-src`，确认不含 `.git`。
- 使用 `FORCE_CUDA=1`、个人 `CUDA_HOME`、个人 CUB headers、`TORCH_CUDA_ARCH_LIST=8.6` 与 `MAX_JOBS=8` 成功构建并安装 `pytorch3d-0.7.8-cp310-cp310-linux_x86_64.whl`；构建产物大小 `3,950,721` bytes，构建时 SHA-256 为 `2526219d67c066ec72da6a078f4e07c3fcc64bee6f22400966ad0bd749b6e793`。
- `cuobjdump --list-elf` 在 `_C` 扩展中连续列出 `sm_86.cubin`，确认新构建针对 fvl05 RTX A6000。
- 首次导入检查从 PyTorch3D 源码仓库目录运行，当前目录遮蔽了 site-packages，导致 `from pytorch3d import _C` 失败；切换到 RoboTwin 实际运行目录后，模块明确从隔离环境加载，`pytorch3d._C` 导入成功。
- 进一步执行了 CPU `knn_points` 数值小测（索引和距离符合预期）与 `Meshes` 三角形结构小测，均通过；`pip check` 仍为零冲突，两个官方源码仓库均保持干净。
- 下一步：重新生成六份 embodiment CuRobo 绝对路径配置，完成全环境旧 fvl09 前缀/共享 CUDA/错误 shebang 检查，再进入宿主 GPU 空闲卡验证。

### 2026-08-10 01:14 CST：六份 embodiment 绝对路径配置已全部迁移到 fvl05

- 重新审计 `script/update_embodiment_config_path.py`：只在仓库根下寻找六个 `*_tmp.yml`，将 `${ASSETS_PATH}`/`$ASSETS_PATH` 替换为当前工作目录并覆盖对应生成文件；无 sudo、外部下载、删除或系统写入。
- 在 `/nfs_share/lijunhui/Robotwin2/project/RoboTwin` 运行脚本，六份配置全部报告成功：ARX-X5、aloha-agilex 左/右臂、franka-panda、piper、ur5-wsg。
- 独立复核而非只信脚本输出：每个生成文件与“模板 + 当前根路径替换”的期望内容逐字节一致；12 个绝对资源引用全部存在；生成配置中旧根 `/bigbig_nfs_share/lijunhui` 为零。
- 该批生成文件属于仓库忽略的运行资产配置；官方主仓库工作树仍为干净的 `main...origin/main`。
- 下一步：扫描整个新 Python、Miniforge、个人 CUDA 与配置树中的旧前缀、失效软链接和脚本 shebang，并做激活/依赖清单复核。

### 2026-08-10 01:18 CST：约 9 GB 新环境全前缀与运行路径纯净性检查通过

- 对新 Python 环境、Miniforge、个人 CUDA、项目配置及 embodiment 生成配置执行二进制可见文本全扫描（`rg -a`，300 秒保护）；扫描约 9 GB，旧根 `/bigbig_nfs_share/lijunhui` 命中数为 0。
- 软链接检查：运行前缀（排除 Miniforge 下载包缓存）中失效链接为 0，所有软链接目标中旧根命中为 0。Miniforge `pkgs` 下载/解包缓存内有 4 个包自身的未闭合相对链接（两份 `_openmp_mutex`、两份 Python compiler-compat），它们不在运行前缀、无旧根且不影响已验证命令；保留为缓存事实，不误报为运行环境故障。
- `env/bin`、Miniforge `bin` 和个人 CUDA `bin` 中没有指向旧根、home Conda、共享 CUDA 的 shebang。
- 激活复核：Python/pip 指向 `Robotwin2/env`，nvcc 指向个人 `tools/cuda-12.1`；`CUDA_HOME`、sm_86、TMPDIR、pip/HF/Torch/Matplotlib 缓存均在 `Robotwin2`；`LD_LIBRARY_PATH` 未设置，PATH 中不含 `/share/apps/cuda` 或 `/usr/local/cuda`。
- Conda 构建包的 `direct_url.json` 中存在 `/home/conda/feedstock_root/...`，这是 conda-forge 构建机元数据而非本机路径或写入；本轮两个源码 wheel 的 provenance 均指向 `Robotwin2/tmp` 内的新快照。
- `/home/lijunhui/.conda` 最终仍不存在；主仓库非资产源代码中也未发现旧根或迁移来的 GPU UUID。
- 下一步：执行更完整的项目模块/资源静态与 CPU smoke，随后在宿主上下文实时查询 GPU 占用，只选择当时空闲卡做 CUDA tensor 和官方渲染验证。

### 2026-08-10 01:38 CST：静态检查、宿主 CUDA、任务导入与官方渲染均通过

- 对第一方代码执行内存中语法编译（不写 `.pyc`）：685 个 Python 文件全部可编译。可选 `policy/DexVLA` 中一处 `is not ""` 触发 Python SyntaxWarning，但不构成语法错误，也不属于基础 simulator 环境修复范围。
- CPU 文件沙箱内尝试导入任务模块时，CuRobo 在模块定义阶段创建默认 CUDA tensor，因设备未直通而报 `No CUDA GPUs are available`；未将其误判为安装失败。随后完成无设备检查：7 份 task YAML 全部解析，5 个 embodiment registry 目标及其 `config.yml` 全部存在，objects/embodiments/background_texture 三棵资产树均非空。
- GPU 状态具有时效性：23:59 检查时 GPU 2–7 正被其他任务满载，本轮没有触碰；01:37:30 再检查时 8 张卡均只占 12–15 MiB 驱动基线、0%（GPU 0 瞬时 1%）、P8，且 compute-app 列表为空。选用的是 fvl05 GPU 2 / UUID `GPU-4306d28e-0eeb-2e26-bda4-b1b44058f63e`，不是继承 fvl09 索引结论。
- 新环境宿主 CUDA 实测：torch `2.4.1+cu121`、torch CUDA `12.1`，`torch.cuda.is_available()==True`，仅一张可见 RTX A6000；创建 1024 元素 CUDA tensor 并求和得到 `1024.0`。
- 再次确认 GPU 2 回到 14 MiB、0%、P8 后，宿主导入 `CuroboPlanner`，并导入/构造 50 个 RoboTwin task 类；全部通过，进程内仅报告 2,048 bytes CUDA allocation。退出后卡再次回到基线且无 compute process。
- 随后在同样的即时空闲检查后运行官方未修改 `script/test_render.py`（180 秒硬超时），约 6 秒正常退出并明确输出 `Render Well`。只有 SAPIEN 旧 API DeprecationWarning，无渲染错误；退出后 GPU 2 为 14 MiB、0%、P8、无计算进程。
- 结论：系统驱动 → PyTorch cu121 → 新编译 CuRobo → RoboTwin task imports，以及 SAPIEN Vulkan/光追渲染链在 fvl05 A6000 上均已实机打通，同时没有持续占用或干扰他人 GPU。
- 下一步：创建新的、与历史输出隔离的一集 fvl05 smoke 配置；运行前再次实时检查 GPU，只在仍空闲时执行完整规划、仿真、渲染、HDF5/视频/指令/轨迹采集验证。

### 2026-08-10 01:45 CST：独立一集 fvl05 端到端采集 smoke 全部通过

- 新建忽略型运行配置 `task_config/demo_clean_fvl05_recheck.yml`：仅 1 集、clean domain、aloha-agilex、4 路 RGB、endpose 与 qpos，输出固定到全新 `data/beat_block_hammer/demo_clean_fvl05_recheck/`；运行前确认配置和目标目录均不存在，因此没有覆盖历史 `demo_clean_smoke`。
- 审计了采集末尾调用的 instruction 生成脚本：它只使用本地 scene info、任务模板和对象描述生成 JSON，不调用 `description/utils/agent.py`，没有 Azure/OpenAI/外网访问。
- 长测试改用 fvl05 GPU 1 / UUID `GPU-414c52ba-72c6-fc45-95d6-1e9750bbc21b`：该卡在 23:59 其他 6 张卡满载时就已空闲，运行前再次为 14 MiB、0%、P8 且无 compute process。运行期间 GPU 1 只有与本次命令精确匹配的 `collect_data.py` PID；另一个项目后来在 GPU 3 启动，本轮未触碰 GPU 3。
- 使用唯一激活脚本并设置 20 分钟硬超时，直接运行 `python script/collect_data.py beat_block_hammer demo_clean_fvl05_recheck`。内置 render 通过；规划 seed 0 正常失败一次，自动重试 seed 1 成功；随后按成功轨迹完成 1 集数据回放、存储和本地指令生成。进程正常退出，没有触发超时。
- 生成视频：H.264、320×240、30 FPS、1510 帧、50.333333 秒；程序明确报告视频保存及 episode cache 删除。进程退出后 GPU 1 回到 14 MiB、0%、P8，无本轮计算进程。
- 第一次在未激活 shell 直接调用 ffprobe 时继承共享 CUDA 12.2 的 `LD_LIBRARY_PATH`，读取共享 `libOpenCL.so.1` 报 I/O error；使用项目激活脚本重跑后视频探测完整通过。这再次证明必须从 `config/activate_robotwin2.sh` 启动项目。
- HDF5/JSON/PKL 内容级验收：
  - HDF5 共 1510 帧；front/head/left/right 4 路 RGB 首尾 JPEG 均成功解码为 320×240×3，相机 intrinsics/extrinsics/cam2world 形状正确且全为有限值；
  - `joint_action/vector` 为 1510×14，并逐元素等于左右 6 轴与左右 gripper 拼接；左右 endpose 均为 1510×7 且为有限值；配置未收 pointcloud，因此其形状正确为 1510×0；
  - `_traj_data/episode0.pkl` 为字典，含 `left_joint_path` 与 `right_joint_path`；seed.txt 为 1；scene info 为 hammer/right arm；
  - instructions 的 seen/unseen 各 100 条，全部完成占位符替换；cache 内条目为 0。
- 第一版验收脚本把 h5py dataset 直接写成 `dataset[:, None]`，触发 h5py 不支持 None indexing；此前相机等断言已经通过。改为先读 NumPy array 再扩维后，完整验收脚本正常退出，确认是检查代码写法而非产物问题。
- 本轮新产物 SHA-256：HDF5 `c91b1983eb57043e26f51e4bce8d9b27e8f34c79e8f54b217d7acb51366cf192`；MP4 `4e073466c1326e6e88365a7e51532901f6d1906050a0eda237a408bf9bd5b4b2`；轨迹 PKL `4f14f3044fa5d7e2bcbca4cd46c29233e97e5370e6b5f1b82b22eeddf1a32b08`；指令 JSON `fa50d97a4f8e8f8f15b2ed7746a3e4352a4937600d418e9ac5a498ffbf269775`。
- 结论：规划 → 仿真 → SAPIEN 渲染 → 4 相机采样 → action/endpose → HDF5 → MP4 → 轨迹 → 指令的完整基础 pipeline 已在从零重建的 fvl05 环境实机通过。
- 下一步：最终复跑 `pip check`、核心导入、激活路径、源码 Git 与产物存在性，更新长期 workspace memory，再收尾本文档。

### 2026-08-10 01:49 CST：最终复核完成，环境交付状态为“可用”

- 最终复核通过：激活脚本 shell 语法正确；激活后的 Python、pip、nvcc、`CUDA_HOME`、sm_86 与各类缓存/临时目录均指向 `/nfs_share/lijunhui/Robotwin2`；`LD_LIBRARY_PATH` 未继承共享 CUDA；`pip check` 仍为 `No broken requirements found`。
- 核心版本与动态扩展再次核对无误：Python `3.10.20`、torch `2.4.1+cu121`、SAPIEN `3.0.0b1`、MPLib `0.2.1`、CuRobo `0.7.8`、PyTorch3D `0.7.8`；CuRobo 代表扩展和 `pytorch3d._C` 均为本轮针对 RTX A6000 `sm_86` 的新构建，并可正常加载。
- 三个固定源码仓库最终保持干净：RoboTwin `c3ddfa8b97d5519efa828b075999bd0006778e5e`、CuRobo `d64c4b005459db10c5dd867d8b30a87d5bda9bdb`、PyTorch3D `75ebeeaea0908c5527e7b1e305fbc7681382db47`。历史 `demo_clean_smoke` 的 3 集产物仍保留，本轮 fvl05 recheck 产物及四个 SHA-256 复核一致；`/home/lijunhui/.conda` 不存在。
- GPU 占用只代表查询瞬间：23:59 时 GPU 2–7 忙，01:37:30 时八张卡曾全部回到基线；01:48:45 再查时 GPU 3 已出现 PID `3861183`、约 7.5 GiB、P2，其他七张卡处于约 14 MiB/P8 且无计算进程。本轮没有停止或触碰 GPU 3 的任务；用于长 smoke 的 GPU 1 已在退出后回到基线。以后每次运行仍须临启动前重新检查，不能把某次空闲快照当作长期状态。
- 已把本轮确认的版本、固定 commit、Miniforge/Mamba 注册陷阱、fvl05 smoke 路径和验收边界写入 `/nfs_share/lijunhui/AGENTS.md`，供后续任务复用；没有 Git commit 或 push。
- 唯一激活入口：`. /nfs_share/lijunhui/Robotwin2/config/activate_robotwin2.sh`。基础 simulator 已完成环境重建与端到端验收；上游 XPolicyLab 源码/数据格式升级、大规模或多任务采集、训练及 policy 专用环境不属于本轮完成范围，应分别规划和验证。

## 最终结论

迁移来的不可靠 Conda/Python/CUDA 前缀已被精确删除并在个人 RoboTwin 目录内从零重建；fvl09 旧绝对路径已经清零，fvl05 RTX A6000 所需的 CUDA 扩展已重新按 sm_86 编译。依赖、CUDA tensor、全部任务导入、官方渲染及独立一集完整数据采集均已实际通过，因此当前 RoboTwin 2 基础环境可在 fvl05 正常使用。

### 2026-08-10 01:50 CST：GPU 瞬时状态补充说明

- 01:49:48 紧接着再次只读查询时，01:48:45 位于 GPU 3 的 PID `3861183` 已结束；八张卡均为 14–15 MiB、0%，compute-app 列表为空，但 GPU 3–7 尚处 P0。该分钟级变化再次说明 GPU 状态只能作为带时间戳的快照；任何后续测试都必须在命令启动前即时复查，不能因本轮某次查询为空闲就预约或默认占用某张卡。
