---
title: F1–F4 正式数据构造实时日志
date: 2026-08-27
last_updated: 2026-08-27
status: active_append_only
design_version: controlled_multi_future_f1_f4_v1_2
document_structure_revision: merged_master_v1
implementation_status: audit_in_progress
stage0_readiness: blocked_pending_code_asset_mapping_and_pilot_budget
formal_collection_started: false
stage0_started: false
stage1_started: false
stage3_started: false
tags:
  - research/data-construction
  - controlled-multi-future
  - F1-F4
  - live-log
---

# F1–F4 正式数据构造实时日志

> [!important] 日志职责
> 本文件是新一轮 `controlled_multi_future_f1_f4_v1_2` 实现审计、非正式 probe、代码准备、Stage 0 readiness 与后续获批采集的 append-only 实时记录。设计定义以 [[../Idea/项目核心Idea|项目核心Idea]] 为准，执行合同以 [[数据构造方案]] 为准；本日志只记录实际做了什么、机器证据、失败、恢复、状态变化和精确停止点，不替代规范文档。

> [!danger] 当前授权边界
> 当前只授权 A–I：环境／源码锁定、官方 task/asset/primitive 审计、机器可读 registry、有限 `audit_probe`／`nonformal_feasibility_probe`、additive skeleton/tests、`pilot_attempt_budget_v0` proposal、附录 D-A/readiness 更新与审计报告。当前没有授权 Stage 0、Stage 1、Stage 3、360 条 formal data、机制模型训练、compression 或 policy training。

## 记录规则

- 只在文件末尾按时间追加，不重写既有事实；任何更正以新条目说明。
- 明确区分 `planned`、`implemented`、`generated`、`verified`、`scientifically supported`。
- 每次 state-changing 操作前重新读取本日志末尾和相关工作树状态。
- 所有 probe 保存 purpose、formal/stage0=false、命令、timeout、有限预算、输入/输出路径、成功或失败、耗时和原因。
- 所有 GPU 命令遵守 `/nfs_share/lijunhui/AGENTS.md`：只允许物理 fvl05 GPU 0；每次运行前后记录实时 `nvidia-smi`、进程归属和释放状态；GPU 0 忙时不换卡、不共享。
- 不记录凭据、token、私钥、授权码或其他敏感信息。
- Git commit/push 需要用户另行明确授权。

## 1. 2026-08-27：新一轮 F1–F4 实现审计启动

- 用户指定本文件作为新一轮数据构造的实时日志，并要求同步写入 workspace `AGENTS.md` 的长期协作规则。
- 已完整读取当前 canonical 文档：
  - `Vault-on-Fvl09/Idea/项目核心Idea.md`；
  - `Vault-on-Fvl09/数据构造/数据构造方案.md`。
- 当前唯一设计标识：

```yaml
design_version: controlled_multi_future_f1_f4_v1_2
document_structure_revision: merged_master_v1
```

- 上一轮 Mouse MVP 活跃文件已可恢复地移入：
  `/nfs_share/lijunhui/Robotwin2/archive/mouse_three_destination_mvp_20260810_20260820/`；旧 Mouse 日志和机器证据只作历史依据，不定义 F1–F4 实现。
- 启动时 RoboTwin 官方仓库为 `/nfs_share/lijunhui/Robotwin2/project/RoboTwin`，branch `main`，HEAD `c3ddfa8b97d5519efa828b075999bd0006778e5e`，工作树干净。正式 `environment_and_source_lock` artifacts 尚未生成，本条不替代后续机器可读 source lock。
- 当前阶段：`implementation_audit_in_progress`。
- 当前停止线：`BLOCKED_BEFORE_STAGE_0`；code/asset/physical-property/verifier mapping 与 `pilot_attempt_budget_v0` proposal 尚待完成。
- 本条创建过程没有启动 GPU、仿真、planner、采集或训练，没有修改 RoboTwin 官方 tracked baseline，也没有执行 Git commit/push。

## 2. 2026-08-27：environment/source lock 完成，GPU0 忙而停止 GPU probe

- 已生成并 JSON parse-validate：
  - `数据构造/实现审计/environment_and_source_lock.md`，SHA-256 `3f5065fc863598aa116a535d28a3f947cf8022845d7e167a2d51ca9af699633b`；
  - `数据构造/实现审计/environment_and_source_lock.json`，SHA-256 `a674eb2615520377bd703c6e286ad188907a387471dfad6efa4130ff183b2c5a`。
- 服务器实际 RoboTwin repo/branch/commit 为 `/nfs_share/lijunhui/Robotwin2/project/RoboTwin` / `main` / `c3ddfa8b97d5519efa828b075999bd0006778e5e`，与文档记录一致，`source_lock_match=true`，worktree dirty count=`0`。
- CPU-only 环境闭包通过：Python `3.10.20`、torch `2.4.1+cu121`、SAPIEN `3.0.0b1`、MPLib `0.2.1`、CuRobo `0.7.8`、PyTorch3D `0.7.8`，`pip check` 无 broken requirements；没有重装或修改环境。
- 默认文件沙箱中的 `nvidia-smi` 因设备未直通失败；在获准宿主只读上下文重新取得 live snapshot。物理 GPU0 当时为 `45,696/49,140 MiB`、`100%`、`P2`，有两个外部 compute processes，明确不空闲。
- 决策：没有启动 CUDA tensor、render、planner、task construction 或其他 GPU child；没有使用 GPU1–7。继续 CPU-only 官方代码／资产审计，GPU probe 等待未来某次 fresh GPU0 idle Gate。
- `stage0_readiness` 保持 `blocked_pending_code_asset_mapping_and_pilot_budget`。

## 3. 2026-08-27：压缩 AGENTS.md 中的历史 Mouse 过程记录

- 按用户要求，将 `AGENTS.md` 中逐 WP、逐 attempt、逐 artifact 路径／hash 的 Mouse 历史长清单删除；文件由 218 行压缩为 166 行。
- 只保留简明长期事实：Mouse MVP 已归档、4 accepted roots／24 raw／144 derived、`core pass / temporal unresolved`、无 formal generalization/H-reveal/compression/policy-transfer，以及旧路径通过归档 `README.md` 解析。
- 完整过程仍由 Mouse 归档目录、归档 append-only 日志与 `成果复盘.md` 保存；未删除机器证据，也未改变新 F1–F4、GPU、环境或协作规则。
- 压缩后 `AGENTS.md` SHA-256 为 `d943ba6d378c418cb966b984de2db992d5aa443f20ba1468e7f693b436c25e42`。

## 4. 2026-08-27：F1–F4 静态代码／资产审计与 additive skeleton 完成

- 完整审计 16 个优先官方 env、`Base_Task`、robot APIs、actor constructors 和选定 asset metadata/GLB；未修改任何官方 tracked 文件。
- 机器 registry 已生成并 JSON parse-validate：
  - `实现审计/f1_f4_implementation_registry.md`，SHA-256 `d9ad6ae45b0828f18c4f6ba6560c2384453bd20fe0e8fb86973b19e5e19543cb`；
  - `实现审计/f1_f4_implementation_registry.json`，SHA-256 `c3fad7096b9417f3bc4cabea858f19b48d98b37582b71a3ac9d2d91238c37dae`；
  - static asset evidence `probe_outputs/audit_probe_static_asset_metadata_v1.json`，SHA-256 `64546bafdd1f3c4114069cfe5890901780b94c6292ab272624cbe4f58c3fabb5`。
- 关键裁决：
  - F1 不再沿用“红绿蓝罐头”；官方 `071_can` texture/static evidence 不支持该三色假设，首选 `blocks_ranking_rgb` 的同尺寸 procedural RGB blocks，container 候选为 `062_plasticbox/base3|5`；
  - F2 的官方 box/scale/pot-or-stand tasks 使用不同 main-object families；`071_can/base1|6` 只作为待同物体三关系 probe 的候选，当前 `unresolved`；
  - F3 官方 `shake_bottle_horizontally` 旋转 bottle 后仍沿 z displacement，不等于 project `H=±x_table`，`official_horizontal_task_is_equivalent_to_H=false`；
  - F4 官方 ranking targets 只是不可见坐标，`008_tray` 只有两个 functional points；三个 slot 必须使用明确标为 project scaffold 的 visible regions。
- 在官方仓库新增隔离、untracked 的 `controlled_multi_future/` 与 `tests/controlled_multi_future/`：fail-closed base interface、schemas、F1–F4 frozen program skeleton、pure verifier-signal adapters 和 tests。没有 collector、formal writer 或默认运行副作用。
- CPU-only test command：`python -m unittest discover -s tests/controlled_multi_future -p 'test_*.py' -v`；7/7 passed，14 个 package Python 文件 compile passed，`git diff --check` passed。

## 5. 2026-08-27：budget proposal、readiness 与 CPU 阶段安全停止点

- `pilot_attempt_budget_v0_proposal.{md,json}` 已生成；JSON SHA-256 `454b65eb09a482a46fffabee3f0898dcd05191ba5c651921e290e810a7b25703`，严格状态为 `proposed_for_user_review / approved=false / frozen=false`。因新 runtime probe 尚未执行，wall-time/cost 范围标记 low confidence，不能据此启动 Stage 0。
- `stage0_readiness_report.{md,json}` 已生成；JSON SHA-256 `3c694662c213be3ce417e47259ac19fb089300eac2a84aff7ca9f2c5af330b36`，decision=`BLOCKED_WITH_REASONS`。
- `implementation_audit_report.md` 已生成，SHA-256 `7aacfe911e5be766985c3deb8fe5597673c14e22bbda936d93c50ae084539690`。
- 按允许范围更新 `数据构造方案.md` frontmatter、D0.3 和 Appendix D-A.4 的真实实现状态／mapping／blockers；设计版本、F1–F4 programs、40/360、R=3 与 5/2/3 split 未改变。更新后 SHA-256=`8a2a422438b433e7c331fa3782ccd4a7d69af0caf007fb36b2671e9bed8620b0`。
- CPU 工作完成后第三次 fresh GPU snapshot：物理 GPU0 仍有外部 compute process，`33,637/49,140 MiB`、`76%`、`P2`；GPU1/3 虽瞬时 idle，但本项目禁止使用。累计没有启动任何 task-owned GPU PID。
- GPU blocker evidence 更新为 `probe_outputs/gpu0_availability_blocker_20260827.json`，SHA-256 `ea7a66661cbb093786b340ed469673d82c590f422f1ab355ea926ed689bb85df`。
- `AGENTS.md` 同步当前 F1–F4 CPU audit/skeleton 与 GPU0 blocker 后 SHA-256=`b8840c7dae1489493991b54ad4a8f2d76aa511a48f68ef8a3db811a80faa4e11`；§3 的旧 hash 是压缩操作完成当时的中间值，本条为当前值。
- 精确停止点：A–D、F–I 的 CPU 可执行部分完成；E 的 runtime probes 全部保持 `pending_gpu0`。下一步只在 future fresh GPU0 idle Gate 后按 `environment → F1 → F2 → F3 → F4` 的有限 probe 顺序继续；随后再次更新 registry/budget/readiness，并在 Stage 0 前停止等待用户批准。
- 本轮没有正式 Stage 0/1/3 数据、没有训练、没有 Git commit/push。

## 6. 2026-08-27 17:38 CST：GPU0 仍忙，未恢复 runtime probes

- 应用户查询重新取得 fresh live snapshot：物理 GPU0 为 `45,949/49,140 MiB`、`60%`、`P2`，仍有 1 个外部 compute process，明确不满足 idle Gate。
- GPU4–7 当时处于约 12–14 MiB、0%、P8，但本项目设备范围只允许物理 GPU0，因此没有换卡、共享或启动 child。
- 本任务累计 task-owned GPU PID 仍为空；F1–F4 runtime probes 继续保持 `pending_gpu0`，Stage 0 readiness 不变。

## 7. 2026-08-27：用户将 GPU 范围改为物理 GPU4–7并允许并行

- 用户最新明确授权覆盖此前 GPU0-only 规则：本轮 F1–F4 可使用物理 fvl05 GPU4、5、6、7；GPU0–3 不在当前范围。
- 允许在多张 eligible 卡分别空闲时运行独立并行 jobs；每张卡仍必须在其 child 启动前单独 fresh 检查 memory/utilization/P-state/compute processes，显式绑定对应 UUID，不同 jobs 不得共享同卡。
- 每个并行 job 单独记录 PID/PGID、timeout、purpose、输出路径和 pre/post release；任何卡变忙就不在该卡启动，也不干扰外部进程。
- 当前授权仍只覆盖 `audit_probe`／`nonformal_feasibility_probe`，不授权 Stage 0、Stage 1、Stage 3 或训练。
- `AGENTS.md` 已同步；后续以本条和最新 `AGENTS.md` 为准，§2/§5/§6 的 GPU0 blocker 保留为当时历史事实。

## 8. 2026-08-27：GPU4–7 parallel environment 与 scene inspection 闭包

- 四张 eligible 卡分别在 child 内 fresh precheck 通过：GPU4=`12 MiB/0%/P8`，GPU5–7=`14 MiB/0%/P8`，compute process 均为空；每个 child 只看对应 UUID。
- environment certification 并行结果全部 `passed_nonformal_environment_certification`：
  - GPU4 child PID `896052`，52.94 s，receipt SHA-256 `2923e885dbfb9cad8f3ceccb40aedcf03d4d5316fb996e992a4f3c896a098ea5`；
  - GPU5 PID `896054`，53.13 s，SHA-256 `3891f7445a6525245ccace107027d2fa87cb4cb8dcb1d0485fbb81a5965c123a`；
  - GPU6 PID `895946`，53.34 s，SHA-256 `a6f5f769b0babba7612bb716b1c9cc9fd370acce7e262ee886f95dd99e396154`；
  - GPU7 PID `895919`，54.83 s，SHA-256 `4b5b8483c1802dadc413f496b043658fdf8479bfa23034fbf59531cb225001ab`。
- 四项均完成 CUDA tensor、CuRobo planner import 和官方 `script/test_render.py = Render Well`；退出后 GPU4–7 全部回到 12–14 MiB、0%、P8、无 process。
- deterministic joint-scene inspections 分别在 GPU4/5/6/7 并行通过，child PID=`931605/931935/931828/931937`，耗时约 `47.63/45.77/44.59/44.70 s`；F1–F4 receipt SHA-256 分别为 `c885c8bf...5805`、`78d5e261...06e1`、`c8c94009...b3a`、`77972456...b1d9`。每项保存 320×240 head/front PNG、runtime PhysX body/mass/collision evidence，均为 nonformal。
- 人工 visual review：F1–F3 primary head current roles 可辨认；secondary front view 有遮挡。F4 v1 的 white common X 在白桌面上低对比，判定 hard fail 并保留原图/receipt；没有覆盖。
- additive v2 只把 X 从白色改为高对比黄色，在 fresh-idle GPU7 新 namespace 运行；child PID `972425`，35.06 s，receipt SHA-256 `9cdaa89400b11a9388a75af2093cf5f3443d0ed24018c296bcf5bfb1b9a5acf3`，head/front visibility 通过，GPU7 释放至 14 MiB/0%/P8。visual review 当前 SHA-256=`2048fcfe34a1f75647f321a94599e156bd02d978940153910575dfacb923e5c8`。

## 9. 2026-08-27：首轮 F1–F4 nonformal action probes 完成并保留失败

- 四项具名 probes 在 fresh-idle GPU4–7 并行启动，task shell PGID=`1042549/1042445/1042531/1042714`；对应 child PID=`1042635/1042466/1042551/1042762`。每项 attempt=1，均正常返回且没有 timeout/orphan；最终 GPU4–7 全部为 12–14 MiB、0%、P8、无 compute process。
- F1 / GPU4：`failed_nonformal_action_probe`，32.52 s，receipt SHA-256 `dbb0f4d0bc6365b7be494b89f5c8dd9024b746e4e86865edf43a64c70e10632f`。grasp/lift 到 1,181 steps，red z 从 0.762 m 到 0.8602 m；green displacement≈`4.30e-7 m`、blue=`0`；block→box place planning 失败，未原地重试。
- F2 / GPU5：88.87 s，receipt SHA-256 `b4df59676da6be8a2fcbc97d6e64929799d7a1214c58c6ebe0a3a25db289380b`。完全相同 `071_can/base1` + left arm：inside passed（XY≈2.08 mm，2,638 steps）、on passed（≈0.44 mm，2,675 steps）；beside 在 1,242 steps 后 place planning 失败。总体保留为 partial/fail，不用不同物体替代。
- F3 / GPU6：39.71 s，receipt SHA-256 `c5b259a42884ccfe6cad1842a31b41acda913265067be66d05543a93bf07f2a1`。single V、single H、V→H 全部完成；四个 event contact fraction=`1.0`，正负幅度≈45–53 mm，off-axis≈4.7–7.4 mm，central return≈2.7–6.7 mm。最后 return-to-original-pad place planning 失败，故 motion core supported、完整 F3 unresolved。
- F4 / GPU7：`passed_nonformal_action_probe`，34.15 s，receipt SHA-256 `bddd619ab445f8e564ae13ddde41ac66a9768f0f08ad9768a1eab0143ca1bbce`。single A neutral block 3,258 steps，slot XY error≈1.89 mm，neutral return≈5.56 mm，gripper open；full common-X/program/noninterference/reorder 未测试。
- 所有 realized traces 以 immutable NPZ 保存并 hash；失败原因进入 registry/readiness，首轮具名 probes 不原地重试。下一步只能先做 CPU-only versioned repair design。
- 更新后 registry MD/JSON SHA-256=`c66612f9...1bb7` / `9c4d4a54...7cda`；readiness decision 仍为 `BLOCKED_WITH_REASONS`，JSON SHA-256=`8ac4cf32...8a1`；budget 仍 `proposed_for_user_review`，JSON SHA-256=`ea3902bf...046`；总审计报告 SHA-256=`badbdf5e...5d28`；`数据构造方案.md` SHA-256=`ce4e333e...27fe`；`AGENTS.md` SHA-256=`8508455d...108`。
- 本阶段没有 Stage 0/1/3 正式数据、没有训练、没有 commit/push。

## 10. 2026-08-27：准备 Vault GitHub 审阅发布

- 用户明确要求提交并推送 `Vault-on-Fvl09`，随后让外部 GPT 直接读取 GitHub 仓库审阅；本授权只适用于 Vault，不包含官方 RoboTwin repo。
- 为让 GitHub 审阅覆盖实现代码，将 active `controlled_multi_future/` 与 `tests/controlled_multi_future/test_static_contracts.py` 逐文件复制到 `数据构造/实现审计/代码审阅快照/`，并增加 README 明确其只读、非 active、非 Stage0 代码身份。
- 20 个 source/test 文件经 `cmp` 与 active RoboTwin files 逐字节一致；使用 snapshot-root `PYTHONPATH` 运行 7/7 tests passed。第一次未设置 `PYTHONPATH` 的 Vault test invocation 在 import discovery 阶段失败，未执行测试逻辑；README 已记录正确命令，失败没有被隐去。
- `git fetch origin main` 首次在默认网络沙箱连接失败、无状态变化；获准联网后成功。当前 Vault `HEAD` 与 `origin/main` 均为 `0b065241a17e22073a1040d0ca958056152f0b91`，ahead/behind=`0/0`。
- 新审计目录约 1.8 MiB，最大单文件约 216 KiB；敏感信息模式扫描无命中。commit 前仍需 staged rename/diff/JSON/Markdown/媒体范围终审。

## 11. 2026-08-27：Vault 主审计 commit 推送成功

- staged 终审：106 files，40 renames、65 additions、1 modification；`git diff --cached --check` passed；无 unstaged/untracked；无 `__pycache__/.pyc`；snapshot tests 7/7 passed；所有 audit/receipt JSON parse passed；敏感信息模式扫描无命中。
- 创建 commit `23fd65a33f601c8a5e69346d30e33fb71e19244e`：`docs: publish F1-F4 audit and archive prior materials`，106 files changed / 7,246 insertions。
- `git push origin main` 成功，远端从 `0b065241a17e22073a1040d0ca958056152f0b91` 前进到 `23fd65a33f601c8a5e69346d30e33fb71e19244e`；随后本地 HEAD 与 `origin/main` 精确相等，工作树干净。
- 该 commit 已包含 canonical Idea/data guide、历史归档/环境目录整理、全部实现审计文档、GPU/scene/action receipts、PNG、NPZ realized traces，以及 external-GPT 代码审阅快照。
- 本条将作为只含 push closeout 的第二个 commit 再次推送，确保 GitHub 实时日志也记录发布结果。

## 12. 2026-08-27：Stage 0 前置修复与最小管线工作启动

- 用户授权继续修复实现，但仍明确禁止 Stage 0/1/3 正式采集、360 条 formal data、模型训练、compression 与 π0.5；科学设计、三分支程序、40/360、R=3 和 5/2/3 split 均保持冻结。
- active source 仍为 `/nfs_share/lijunhui/Robotwin2/project/RoboTwin/controlled_multi_future/`；Vault `代码审阅快照/` 只在 active code 完成并验证后做 byte-equal 同步，不在快照中独立开发。
- 启动前复核：官方 RoboTwin HEAD=`c3ddfa8b97d5519efa828b075999bd0006778e5e`；tracked baseline 无改动；现有 additive code/tests 仍为 untracked；Vault HEAD=`bcaaca21c656776178ee9c1691595486be4dd4f9` 且工作树干净。
- 当前执行顺序：先修 cleanup/trace/current registry 与 CPU tests，再设计 versioned F1/F2/F3 repairs 和 F4 顺序 probes；只有 CPU Gate 通过后，才在 fresh-idle physical GPU4–7 上按有限预算运行 nonformal probes。
- 当前停止线保持 `BLOCKED_WITH_REASONS`。本条只记录启动，没有启动 GPU、planner、采集或训练。

## 13. 2026-08-27：CPU 修复、trace 与 Stage-0-shaped 管线 Gate 通过

- active source 新增／更新 fail-closed 公共实现：`candidate_freezer.py`、`current_hasher.py`、`anchor.py`、`raw_writer.py`、`receipts.py`、`attempt_state_machine.py`、`finalizer.py`、`model_view.py`、`pilot_pipeline.py`，以及 cleanup-safe probe lifecycle、dense trace v2 和 synthetic pipeline adapter。
- `action_feasibility.py` 已改为 versioned variants：F1=`fp1/interior`，F2=`sector1/sector2`，F3=`pad_center/bottle_fp`，F4=`common/A/B/C/common_ab/ABC/ACB/BAC`；每个 named output 只执行一次，旧 evidence 不覆盖。
- dense trace v2 当前包含 250 Hz step/timestamp、26-D effective setpoint、requested/planner fields、qpos/qvel、EEF/actor pose 与速度、gripper command/aperture、指定执行臂 gripper-link names、body pairs/count/impulse（接口可用时）与 event markers。F1/F4 非目标 displacement 改为 initial→final 实测。
- cleanup receipt 当前显式包含 `scene_created/scene_cleanup_attempted/scene_cleanup_succeeded/cleanup_error/partial_output_status/gpu_postcheck/orphan_process_count`；scene cleanup 不确定会变为 `failed_cleanup_uncertain`。
- CPU tests：`python -m unittest discover -s tests/controlled_multi_future -p 'test_*.py' -v`，17/17 passed；32 files 内存 compile passed；`git diff --check` passed。
- 第一次额外执行 CPU-only `python -m controlled_multi_future.probes.action_feasibility --help` 时，模块提前导入 CuRobo 并在无 GPU sandbox 触发 CUDA import failure；未启动 task-owned GPU process。随后改成 scene/CuRobo lazy import，同一 `--help` check passed；失败没有被隐去。
- 新增 current registry 与 CPU repair design：`f1_f4_implementation_registry_v1_current.{md,json}`、`repair_probe_design_v1.{md,json}`；历史 registry 与首轮失败 evidence 保留。
- 当前仍为 `BLOCKED_WITH_REASONS`；下一步才进入 physical GPU4–7 fresh-idle Gate。没有 Stage 0/1/3 数据或训练。

## 14. 2026-08-27 19:27 CST：GPU4–7 fresh-idle Gate 未通过

- 启动任何 repair child 前取得 fresh host snapshot。GPU4/5/6/7 分别为 `28873/29613/32320/39622 MiB`、`72/99/65/100%`、全部 `P2`，外部 compute process count=`1/1/2/2`；四张 eligible 卡均明确不空闲。
- 遵守不共享、不替换到 GPU0–3、不干预外部进程规则：本轮 task-owned GPU PID/PGID 为空，没有启动 CUDA、SAPIEN、CuRobo、planner 或 action probe。
- blocker evidence：`实现审计/probe_outputs/gpu4_7_availability_blocker_20260827_192744.json`。继续完成 CPU-only synthetic pipeline dry-run、budget/schema 与审阅证据；之后仅再做 fresh recheck，若仍无卡空闲则停止并报告。

## 15. 2026-08-27：nonformal synthetic pipeline dry-run 通过

- 命令：`python -m controlled_multi_future.probes.pipeline_dry_run --output .../probe_outputs/nonformal_pipeline_dry_run_v1_20260827`；CPU-only，未初始化 CUDA/SAPIEN/CuRobo。
- 结果 receipt status=`accepted`，但明确 `formal_data=false / stage0_data=false / synthetic_only=true`。完整经过：planned slot → 3 candidates feasibility → candidate/task-tree freeze → prefix freeze → 两个独立 adapter scene lifecycles → same-current hash → anchor equivalence → rollout → raw save → verifier → cleanup/orphan audit → finalizer。
- raw contract：4×26 actions、5 states、250 Hz、`controller_effective_setpoint_v1`；scene cleanup 2/2，orphan=0。receipt SHA-256=`3c56bf6260df6592c5c806acaebc6b4b37377fa2a5ffec41289b89194f352763`，raw NPZ SHA-256=`7905fa4d099f5a3aa3fa7e4ec329f8f0557c2d33029509664d43241ad57ae709`。
- 该 probe 只证明管线控制流/schema 的 CPU integration，不证明真实 SAPIEN fresh-scene reconstruction、family 动作或 Stage 0 readiness。
- `pilot_attempt_budget_v0_proposal` 已修正四类 count semantics、Stage 0/Stage 1 root budget、`max_slot_spec_generation_attempts=1` 与 fresh reconstruction requirement；wall-time/GPU-hour 仍不批准，status 仍 `proposed_for_user_review / approved=false / frozen=false`。

## 16. 2026-08-27 19:30 CST：GPU blocker 复核与本轮停止

- 第二次 fresh host snapshot：GPU4/5/6/7=`28873/29627/32140/39440 MiB`、`65/75/94/100%`、全部 P2，external compute process count=`1/1/2/2`。仍无 eligible idle card。
- task-owned GPU PID/PGID 始终为空；没有启动 repair child，也没有 post-run cleanup 欠账。evidence：`probe_outputs/gpu4_7_availability_blocker_20260827_193021.json`。
- 因此 F1 `fp1/interior`、F2 `sector1/sector2`、F3 `pad_center/bottle_fp`、F4 natural program probes 均保持 `designed_not_run`；旧 failure evidence 不覆盖。
- active code 已机械同步到 Vault byte-equal 审阅快照；最终仍需运行 snapshot tests/hash checks。Stage 0 readiness 保持 `BLOCKED_WITH_REASONS`，停止线为等待 GPU4–7 fresh-idle 后续跑已设计的有限 probes；不得启动 Stage 0。

## 17. 2026-08-27：最终 CPU/快照/机器证据闭包

- active source 与 Vault 审阅快照分别运行同一 unittest discover：均为 17/17 passed；两组 `diff -qr` 均零差异；active/Vault `git diff --check` 均 passed；active source、tests 与 snapshot 均无 `__pycache__/.pyc`。
- 8 个本轮关键 JSON parse passed；synthetic raw NPZ 复核为 actions=`(4,26)`、realized qpos states=`(5,14)`，满足 N/N+1。第一次合并校验命令因 shell 嵌套单引号使 NPZ key 变成未定义变量，第二次又因命令行 path quote 把后续 `&&` 吸入参数；二者均是只读 validation-command failure、没有修改 artifact。随后拆分为无嵌套引号的 JSON/NPZ check 和独立 diff commands，全部通过；失败未隐去。
- active additive Python source/tests 共 32 files、2,219 lines；官方 tracked baseline 仍零修改，RoboTwin `git status --short` 只有 `?? controlled_multi_future/` 与 `?? tests/`。Vault 保持未提交改动；本轮没有 commit/push 授权，也没有执行。
- 关键当前 hash：readiness JSON=`d4cac4581e8f986701cf5666092d34ddcdb06ab132cb656d83ac1b2b46aa7cac`；current registry JSON=`623487eceda927fd2de88cfe106ef4613cd298e93775c2fee0b39225013f8844`；budget JSON=`9e735df0b2a4aef9d16c795304b2af9023f9c02389cf4430ec1187205efc2daf`；repair report=`5b330edd40d759ea41a3fcc116e0abe8e80bb971046096a9f64b0956a600ca91`。
- `/nfs_share/lijunhui/AGENTS.md` 已增加稳定状态：cleanup/trace/pipeline/17 tests/synthetic pass 与仍待真实 repair/SAPIEN integration；GPU4–7 长期执行规则未改变。
- 最终 decision=`BLOCKED_WITH_REASONS`。本轮没有 Stage 0、Stage 1、360 条正式数据、机制模型/VLA/π0.5/compression 训练。

## 18. 2026-08-27：用户扩展 GPU 范围并启动十分钟监控

- 用户最新明确授权覆盖此前 GPU4–7-only 范围：当前 F1–F4 versioned nonformal repair work 可使用 physical fvl05 GPU0–7 中任意一张或多张 independently fresh-idle 卡。
- 监控间隔约 10 分钟；idle 标准仍是 near-baseline memory、无 compute process、低 utilization/P8、无其他用户活跃证据。发现候选后必须在 child 启动前再次即时复核，并显式绑定该卡 UUID；不同 jobs 不共享同卡。
- 已建立持续监控目标；发现 idle card 后继续尚未完成的 F1/F2/F3 repairs 与 F4 natural probes。授权范围没有扩展到 Stage 0/1/3、formal collection 或训练。
- 22:00 snapshot 仍显示 GPU0–7 全部有外部 compute process；本条创建时没有 task-owned GPU child。

## 19. 2026-08-27 22:05 CST：十分钟监控第 1 轮仍无 idle GPU

- fresh snapshot：GPU0–7 memory used=`45949/32752/46482/40549/34815/35047/32668/39354 MiB`，utilization=`64/100/100/96/67/97/100/67%`，全部 P2；每张卡仍有 1–2 个外部 compute processes。
- 没有候选卡满足 near-baseline memory + no compute process Gate；task-owned GPU PID 为空，没有启动 repair probe，也没有共享或干预外部作业。
- 监控目标保持 active；约十分钟后继续 fresh 检查 GPU0–7。

## 20. 2026-08-27 22:16 CST：十分钟监控第 2 轮仍无 idle GPU

- fresh snapshot：GPU0–7 memory used=`45949/32806/46482/41593/34815/35057/32356/42150 MiB`，utilization=`92/100/67/96/76/75/100/97%`，全部 P2；每张卡仍有 1–2 个外部 compute processes。
- 22:05:50 曾出现一次仅相隔约 30 秒的自动 immediate recheck；它不计为十分钟采样，也没有触发任务。本条为实际等待约十分钟后的有效第 2 轮。
- 没有候选卡满足 idle Gate；task-owned GPU PID 仍为空，未启动、共享或干预任何 GPU job。监控目标继续保持 active。

## 21. 2026-08-27 22:27 CST：十分钟监控第 3 轮仍无 idle GPU，目标转 blocked

- fresh snapshot：GPU0–7 memory used=`45949/32776/48344/41497/34815/37301/32474/42166 MiB`，utilization=`94/92/100/88/74/97/93/100%`，全部 P2；每张卡仍有 1–2 个外部 compute processes。
- 连续三轮有效 goal turns 的 blocking condition 相同，当前无法在不等待外部 GPU 释放的前提下推进任何 repair；task-owned GPU PID 始终为空。
- 按持续目标 blocked audit 规则，本轮记录后将监控目标标为 `blocked`，不让它表面 active 却无法进展。用户后续说“继续监控”即可作为新的 resumed audit 重新启动；Stage 0 仍未授权。

## 22. 2026-08-27 22:43 CST：GPU1–3 释放，用户限定 GPU0–3并授权继续

- fresh status query：GPU0 仍为 `45949 MiB / 67% / P2 / 1 external compute process`；GPU1/2/3 分别为 `14/14/15 MiB / 0% / P8 / no compute process`，满足候选 idle 条件。GPU4–7 同时空闲，但用户最新明确要求“用0-3就好了”，因此立即将 current scope 收窄为 physical GPU0–3，GPU4–7 不使用。
- 计划在 child 启动前分别再次复核 GPU1/2/3，并行运行：F1 `fp1`、F2 `sector1`、F3 `pad_center`；每项 1 个 execution attempt、repair place/return 段最多 2 planner queries、旧 evidence 不覆盖。GPU0 忙，不使用。
- F4 `common` 等待 GPU1–3 中一张完成并 post-release 后再启动。仍无 Stage 0/1/3、formal collection 或训练授权。

## 23. 2026-08-27 22:47–22:48 CST：首批 GPU1–3 attempts 在 scene setup 前失败并安全释放

- 原子 guard precheck 均通过：GPU1/2/3 分别为 `14/14/15 MiB, 0%, P8, no compute process`；child PGID=`1568490/1568498/1568641`，对应 F1 `fp1`、F2 `sector1`、F3 `pad_center`。
- 三项在 Base_Task scene setup 的 `together_open_gripper()` 阶段触发同一 implementation error：`DenseTraceMixin.take_dense_action()` 在 `initialize_trace()` 前访问不存在的 `_gripper_command`。因此 `execution_attempt_count=0`、`planner_query_count=0`，不能解释为 family physical/planner failure。
- 三份失败 receipt/guard/stdout/stderr 均保留在各自首次 namespace；scene cleanup attempted/succeeded=true，guard timeout=false，task-owned orphan=0。postcheck GPU1/2/3 memory=`14/14/15 MiB`、no compute process；P-state 短暂为 P0/P5，不影响无进程和显存释放事实，后续启动仍需 fresh P8 Gate。
- 最小修复：trace 未初始化时委托官方 `Base_Task.take_dense_action()`；新增 pre-trace delegation regression test。CPU suite 从 17 增至 18 tests，18/18 passed；下一次使用新 `attempt2_tracefix` namespaces，不覆盖本条失败。

## 24. 2026-08-27 22:50–22:51 CST：attempt2 结果与下一有限分支

- 三张卡 fresh guard precheck 再次通过，attempt2 child PGID=`1576020/1576244/1576239`；三项 timeout=false、scene cleanup succeeded=true、orphan=0，GPU1/2/3 post memory=`14/14/15 MiB`、no compute process。
- F1 `fp1`：所有 planner calls 完成，repair place queries=2，green/blue displacement=`5.28e-7/0 m`，gripper open；但 realized final block pose=`[-0.1874,-0.2158,0.7620]`，OBB inside=false。旧 receipt 仅按 `plan_success` 写成 passed，当前人工/机器语义复核裁决为 failed_semantic；按设计进入唯一剩余 `interior` variant。
- F2 `sector1`：execution=1，trace 1,234 samples，place planner failure；按设计进入唯一剩余 `sector2`。
- F3 `pad_center`：execution=1，但在错误改成 rest-central 后 `repair_V_1` planner failure，尚未进入 return。该实现偏离已验证 central `[-0.08,-0.05,0.95]`；修复为恢复该 central，并保存 rest→central plan，在 return 后反向复用，使 return 仍只新增 2 planner queries。下一 namespace 标为 implementation `v1_1_central_restore`，保留本失败。
- probe status 判定已修正为 semantic verifier Gate，不再只看 `plan_success`；新增回归测试后 CPU suite=19/19 passed。下一步并行运行 F1 interior、F2 sector2、F3 v1_1 pad_center；F4 继续等待一张卡 release。

## 25. 2026-08-27 22:53–22:54 CST：F1/F2 有限方案用尽，F3 进入 return blocker

- 下一批 guard precheck GPU1/2/3 均为 14–15 MiB、0%、P8、no compute process；child PGID=`1582071/1581968/1582286`。全部 timeout=false、cleanup succeeded=true、orphan=0，post memory 回到 14–15 MiB/no compute process。
- F1 `interior`：planner success、repair place queries=2、OBB inside=true、gripper open；但 non-target green/blue displacement=`0.05633/0.01967 m`，semantic_probe_pass=false。与 `fp1` 合并后两个预定方案均未满足完整 F1 verifier，停止 F1 repair，不增加第三 target/container/arm。
- F2 `sector2`：place planner failure，trace 1,240 samples。stand sector1/2 均失败，停止 `f2_beside_clearance_repair_v1`；按预定流程进入独立 CPU `f2_beside_reference_pot_audit_v2`，仍固定 `071_can/base1 + left arm + beside`，不得换物体或语义。
- F3 `v1_1_central_restore/pad_center`：恢复已验证 central 后完成到 return 阶段，失败于 return `control_pre` planner query；这是真正的方案 A return blocker。按有限设计只剩 `bottle_fp` 方案 B。
- 下一步：GPU3 运行 F3 `bottle_fp`；GPU1 运行 F4-01 `common`；同时 CPU 审计 pot 官方来源与联合场景 clearance。仍不启动 Stage 0。

## 26. 2026-08-27：用户取消固定 GPU 编号限制

- 用户在获知安全审批层仍引用旧 GPU0-only 规则后再次明确授权：物理 GPU0–7 无论编号，只要该 child 启动前 independently fresh-idle 就可以使用。该规则覆盖 GPU0-only、GPU4–7-only 和刚才的 GPU0–3-only 历史范围。
- `AGENTS.md`、current registry、readiness、implementation report、canonical data guide 的当前说明已统一为 any-index fresh-idle GPU0–7；历史 append-only entries 和历史 probe 实际用卡不改写。
- 被审批拒绝的 F3 `bottle_fp` / F4 `common` 并行请求没有启动任何 guard/child；22:55:58 复核 GPU1–7 仍为 12–15 MiB/0%/P8/no compute process，GPU0 仍忙。现在可在新的即时 Gate 后重新提交。

## 27. 2026-08-27 23:00–23:01 CST：全卡再次被外部任务占用；pot CPU audit 完成

- 重新提交前，GPU1/3 guard 分别发现外部 compute processes（约 16.7/19.1 GiB）并 exit 42；转查 GPU4/5 也在原子 Gate 被外部任务占用并 exit 42。四次均没有 child、scene、planner 或 cleanup 欠账。
- 23:00:42 和 23:01:52 全卡 snapshot 均显示 GPU0–7 有外部 compute processes；GPU0 owner=`wangyouze`，GPU1–7 owner=`tangziyao`（`kvzip-mechanism-study`）。本任务未干预。
- CPU `f2_beside_reference_pot_audit_v2` 完成：官方 source=`move_can_pot`，reference=`060_kitchenpot` IDs 0–6；选择 base0/目录 `100015`，scaled extents=`[0.21710,0.10006,0.14599] m`。官方原 can 是不同的 `105_sauce-can`，本项目继续固定 `071_can/base1 + left arm`。
- project joint-scene wrapper：pot XY=`(0.18,0.02)`、fixed root；same can target=`(0.0,0.02,0.79)`，到 box/scale/pot center clearance=`0.255/0.190/0.180 m`。新增 `f2_beside_reference_pot_audit_v2.{md,json}` 和 `pot_left` runner；CPU suite 20/20、33 files compile、diff check passed。
- 等待任意 GPU0–7 fresh-idle 后，剩余 GPU work 为 F2 `pot_left`、F3 `bottle_fp`、F4 `common`。F1 已保持 unresolved，不再加方案。

## 28. 2026-08-27 23:07–23:10 CST：剩余 bounded GPU probes 全部达到 terminal status

- 23:07:11 GPU5 fresh-idle=`14 MiB/0%/P8/no process`；F3 `bottle_fp` child PGID=`1665843` 运行 49.18 s。V/H bottle/EEF amplitudes≈48–52 mm，selected-gripper contact fraction=1.0、break=0；planner 全完成并释放，但 final bottle position/orientation/rest errors=`0.25152 m/0.99689/0.38621 m`、stable speed=`0.06392 m/s`，semantic_probe_pass=false。cleanup=true、orphan=0、post GPU5=14 MiB/no process。F3 两 return variants 用尽。
- 23:08:45 GPU5 再次 fresh-idle；F4 `common` child PGID=`1733117` 运行 38.60 s，失败于 `place_common_X` planner，trace 1,647 samples。按 ordered Gate 不运行 B/C、common-X+AB、ABC/ACB/BAC 或 strict reorder。cleanup=true、orphan=0。
- 23:09:53 GPU5 再次 fresh-idle；F2 `pot_left` child PGID=`1735734` 运行 42.58 s，固定 `071_can/base1 + left arm`，target clearance 与 CPU audit 一致，但失败于 `beside_pot_left` place planner，trace 1,232 samples。stand sector1/2 + pot_left 全部用尽。cleanup=true、orphan=0，23:10:45 post GPU5=14 MiB/0%/no process。
- 本阶段累计 task-owned GPU children=12、timeouts=0、scene cleanup failures=0、orphans=0。新增 `bounded_repair_execution_report_20260827.{md,json}`；最终停止 GPU probing，等待 family-level impact review 和新 implementation version。Stage 0 remains unauthorized。

## 29. 2026-08-27：bounded repair 最终机器闭包

- active source 与 Vault byte-equal snapshot 分别运行 20/20 tests passed；最终 34 个 Python files；两组 `diff -qr` 零差异；active/Vault `git diff --check` passed；active/tests/snapshot 无 pycache。
- 5 个 current JSON parse passed；16 个 GPU guards 中 12 个真实 child runs、4 个 `blocked_precheck_not_idle`，所有 guards 均 `formal_data=false/stage0_data=false`。12 个 child receipts 全部 scene cleanup succeeded、orphan=0；所有 realized traces 含 26-D effective setpoint、qpos/qvel、EEF/object、selected-gripper contact、contact pairs 与 event markers。
- 一次合并 final validation command 因中文路径的 shell quote 把第二条 `diff` 吸入参数；JSON/receipt/trace 部分已先通过，随后拆成两个独立 `diff` commands 均通过。只读命令错误未修改 artifact，已保留说明。
- current hashes：bounded report=`8652f2158c371e8ea756a47fa611de06361ecf95a83367642f703dbed759ab10`；registry=`a350e7ad63f48a7054f38f7b00bb728b1c22b4293b69ab432b21bf49a60915d3`；readiness=`8060f6487c2952c23936c9030086b4af5099ec19a0d5127d12457068da384bfa`；budget=`39c495d5199b5771f773300d89163969566df55aedd7ff6856cde4574a381c82`；pot audit=`371ca8ba674d2deb65462112bbc8919baefcea8d38d9a3d4af1c8f5724558df1`。
- 最终 readiness=`BLOCKED_WITH_REASONS`；下一安全动作不是继续找 GPU，而是 family-level impact review + approved new implementation version。没有 Stage 0/1/3、360 formal trajectories、模型训练、compression 或 π0.5。

## 30. 2026-08-27：准备 Vault GPT 审阅发布

- 用户明确授权将当前 Vault 全部改动 commit 并 push 到 `origin/main`，并要求在仓库内提供自包含 GPT 交付说明。本授权不包含 commit/push RoboTwin active source；active code 仅以 byte-equal Vault snapshot 发布供审阅。
- 新增 `数据构造/实现审计/GPT_REVIEW_HANDOFF_20260827.md`，列出 canonical docs、最终 bounded results、readiness/registry/budget、代码快照、关键 receipts、旧 status 误读风险与希望 GPT 回答的问题。
- push 前仍需完成：staged 范围/大文件/敏感信息/JSON/NPZ/测试/diff 终审；创建 content commit 后把精确 SHA 回填 handoff，再创建发布 closeout commit。没有 Stage 0 或训练动作。
- 第一次 staged snapshot test 从 Vault 根运行时因 test 直接 import runtime-bound `action_feasibility/runtime_trace` 而缺少官方 `envs` 失败；未执行 GPU 或修改 evidence。已把纯 `VARIANTS/result_passed` 移入 dependency-free `probe_contracts.py`，并移除 `runtime_trace` 对 `envs.ArmTag` 的 import；随后要求 snapshot 在 Vault 中 standalone 20/20 passed。该失败不隐去。

## 31. 2026-08-27：Vault content commit 创建

- staged 终审：115 entries、0 unstaged、0 untracked；115 files changed、5,101 insertions、300 deletions；61 JSON parse passed；standalone snapshot 与 active source 均 20/20 tests passed；active/snapshot byte-equal；敏感模式无命中；最大新增文件约 2.8 MiB。
- 创建 content commit `3340882145cc86911c974757cae8458f02f70242`：`docs: publish F1-F4 bounded repair evidence`。
- `GPT_REVIEW_HANDOFF_20260827.md` 已回填该 content commit。本条与回填将形成小型 closeout commit，随后一次 push 两笔 commit 到 `origin/main`。

## 32. 2026-08-27：Vault GPT 审阅发布成功

- closeout commit=`ae354ed9fc459e604bfd9fcd73c308eeab76a464`，message=`docs: record F1-F4 GPT review handoff`。
- `git push origin main` 成功：远端从 `bcaaca21c656776178ee9c1691595486be4dd4f9` 前进到 `ae354ed9fc459e604bfd9fcd73c308eeab76a464`；随后 local HEAD 与 `origin/main` 相等，ahead/behind=`0/0`，工作树干净。
- handoff 已加入 content/handoff 两个精确 SHA。本条将作为最后的 publication receipt commit 再次 push；RoboTwin active repo 仍未 commit/push。

## 33. 2026-08-28：GPT impact review 收到，runtime-v2 CPU/static 修复启动

- 外部 GPT 审阅维持最终裁决 `BLOCKED_WITH_REASONS`：四个 family 尚无任何一个完成三个分支的真实闭环；synthetic pipeline 不能替代 SAPIEN same-current/fresh-scene/raw 验证。审阅要求先修复 F1 true inside/运输碰撞、F2 actor-to-EEF beside、F3 actor-pose/actual-rest return、F4 common-X/tray mapping，以及 trace/raw 语义字段。
- 科学设计保持 `controlled_multi_future_f1_f4_v1_2`，没有修改 4 families、40 roots、360 trajectories、3 intents、R=3、5/2/3、F3/F4 programs。建立实现版本 `controlled_multi_future_runtime_v2` 与 family 子版本：`f1_transport_and_true_inside_v2`、`f2_actor_to_eef_beside_mapping_v3`、`f3_return_equivalence_v2`、`f4_common_prefix_mapping_v2`。
- 用户本轮最新 workspace 规则把当前 GPU scope 收窄为 physical GPU0 only；GPU0 不 fresh-idle 时不得替换 GPU1–7。本阶段只做 CPU/static，没有查询或初始化 GPU，也没有获得新的 runtime-v2 GPU probe 授权。

## 34. 2026-08-28：runtime-v2 代码、状态裁决与 raw-v2 CPU evidence

- 新增纯几何合同：RoboTwin `[xyz,qwqxqyqz]` pose 变换、冻结 `T_eef_object` 后由目标 actor pose 反推 EEF、world-z waypoint、OBB/cavity、support footprint、swept AABB、quaternion angular velocity 与必须有真实 planner `Success` 的 candidate selection。
- F1 使用 `062_plasticbox/base3` collision mesh 和官方 inside-bottom points 建立 actor-local conservative cavity；最终 audit 对 34 个 convex pieces 做 25³=15,625 点 half-space sampling，0 点落入 collision piece。该结果仍明确要求 runtime contact/realized-pose 验证。
- F2 固定 `071_can/base1 + left arm + 074_displaystand/base3`，删除硬编码 `left_arm_reach_provisional=true`，预注册 3 个 upright yaw；只有 swept clearance、workspace sanity 与真实 nonexecuting planner preflight 都通过才可执行。
- F3 不再把 actor center 当 functional-point target，也不再反向播放旧 control 冒充回 rest；改为 exact original actor pose、world-z pre-place/release/retreat、直接规划 `left_original_pose`，Gate 同时看 position/orientation/rest/stable window/pad contact/gripper。
- F4 保持原 yellow-X/tray/RGB/slot layout，不做需 impact review 的设施搬移；当前只实现 common-X actor-to-EEF、high waypoint、tray footprint/support、A/B/C phase displacement 与 neutral Gate。common 未通过前不运行后续 blocks/programs。
- Trace/raw 修复：26-D 顺序冻结为 left-pos、right-pos、left-vel、right-vel、left/right gripper；effective 从 drive targets 读取、requested 来自 `control_seq`、planner target 来自 move API；保存完整双臂 qpos/qvel 与双 EEF，angular velocity 由 quaternion 差分，object velocity 带 measured/derived mask，所有关键字段有 source/status。Trace row0→raw state0，后续 N rows→N actions+N resulting states。
- GPU guard 仅接受 physical GPU0；child 启动后 receipt 缺失会终止为 `failed_missing_child_receipt`。四个 family planner-query limit=`12/12/18/12`，execution attempt 均为 1，超限 fail closed；旧 bounded `action_feasibility.py` CLI 禁止重跑。
- 新建 F1 immutable semantic adjudication receipt：原 fp1 `status=passed` 只因 planner success，但同一 receipt `inside=false`；当前 final semantic status=`failed_nonformal_action_probe`，不修改旧 receipt。新建 F2 pot supersession receipt：旧 `GPU pending` 被实际 pot_left `failed_planner` 状态替代，原 audit 不覆盖。
- 新 synthetic raw-v2 dry run=`probe_outputs/nonformal_pipeline_dry_run_runtime_v2_20260828_cpu1/`：receipt accepted，但 `formal_data=false/stage0_data=false/synthetic=true`；actions=`(4,26)`、requested=`(4,26)`、planner target=`(4,14)` 且 unavailable 用 NaN、qpos/EEF states=`5`。receipt SHA-256=`bef28e0ff16186c0cc483b81b2bd0386649448692c6e37a7daa0072a07968a7e`，raw SHA-256=`a068c665529b75ce1d21fbc9d73d43b3339f169017f3466a2dff85f6a65ba639`。
- 第一次 `cp -a` 同步 Vault snapshot 时，NFS 拒绝保留目标 permission metadata，命令 exit 1；随后 `diff -qr` 证明内容已 byte-equal。最终改用不保留权限的普通递归复制，active/snapshot 两组 `diff -qr` 均零差异。该命令问题未隐去，也未损坏证据。
- Active source 与 Vault standalone snapshot 均为 30/30 CPU tests passed；38 个 Python files 通过无写入 compile；active/tests/snapshot 无 `__pycache__/.pyc`。

## 35. 2026-08-28：runtime-v2 当前交付与停止线

- 新增 implementation plan、runtime current registry、budget addendum 与 readiness report；状态分别保持 `proposed_for_user_review`、`cpu_static_implemented_gpu_runtime_unverified`、`approved=false/frozen=false`、`BLOCKED_WITH_REASONS`。
- 当前关键 hashes：implementation plan=`37ff6fce4de19312a1aade6019b43742cadc87f4bc679b869ee3c22f94d8c37c`；registry=`e421624e58757da519edfee46c4785c7e0ced1a53f0ffe9c6cf79a3aa48f380e`；readiness=`8e69ff7ab784302f6fa6beab0a4583eb1d9cead24d3aa22342ab50303225cc0c`；budget addendum=`a070348b9cdd2dbd2b8435cefc1f44a05612692ac06abc82d65d605627bef1c0`；current static audit v2=`52c6575296d01d8fce752cc9efda2213f72123eb43df4d9a6c849c4da525a5a5`。
- 10 个本轮关键 JSON parse/invariant checks passed；9 个 registry code hashes 与 active source byte-equal；raw-v2 shape/source checks passed；敏感模式扫描无命中。第一次 static audit 在 planner-query hard limit 加入前生成，保持历史不覆盖；`runtime_v2_cpu_static_audit_20260828_v2.json` 是 current evidence。
- 仍未完成：F1–F4 runtime-v2 GPU probes、F4 complete natural programs、真实 SAPIEN fresh-scene current/anchor/raw integration。下一步必须先由用户审阅并明确批准或拒绝 `controlled_multi_future_runtime_v2_implementation_plan.md/json` 的四条 bounded nonformal probes。
- 本轮没有 Stage 0、Stage 1、360 formal trajectories、机制模型/VLA/π0.5/compression 训练；未 commit/push Vault 或 RoboTwin active source。

## 36. 2026-08-28：第二轮 completion audit 与 CPU pre-probe 最终闭包

- 在等待用户授权期间继续做 requirement-by-requirement 反证审计，发现并修复 7 类 CPU 可证明缺口：F2/F3/F4 稳定窗口此前位于撤离前；F3 final pass 未纳入 realized V/H；F1 把 collision-free lower bound 混作 support-inclusive semantic cavity lower bound；rest/neutral 未完整检查 orientation/EEF stationarity；F4 common receipt 缺少完整边界状态；GPU guard 未覆盖 pre/post snapshot failure 与 baseline release；raw writer 未强制 object/contact audit 和 250 Hz cadence。
- F1 现分离 semantic containment lower=`[-0.05,0.018,-0.04]` 与 collision-free core lower=`[-0.05,0.024,-0.04]`。Free-core 继续对 34 个 official convex collision pieces 做 15,625 点采样并通过；semantic success 另要求最终连续 box-support contact，避免把正确贴底物体误判 outside。
- F1/F2/F3 现均在 arm retreat + actual rest 后再运行最终稳定窗口，并检查 rest position、orientation、EEF linear/angular stationarity；F4 在 neutral 后再检查 common-X support/stability、A/B/C 位移及完整 start/end EEF pose/velocity/gripper/object pose。
- F3 final semantic Gate 现同时要求 EEF+bottle 正/负主轴幅度、off-axis、central return、bottle orientation drift、selected-left-gripper contact fraction 与 contact break count，以及最终 original-pad/rest equivalence。所有阈值仍标为 provisional，未冒充 Stage-2 frozen 数值。
- GPU guard schema 升为 `cmf_gpu_guard_v2`：precheck snapshot/UUID 失败会写 guard receipt 并禁止 child；postcheck snapshot 失败或显存、利用率、compute process 未回 pre-run baseline 会终止为 `failed_cleanup_uncertain`。新出现但 ownership 不确定的进程只记录、不 kill，并停止 GPU0 后续任务。
- Raw writer 现强制 primary streams finite、component masks 为 bool、timestamps 精确 250 Hz；audit 至少含 `(N+1,7)` object pose 与 `(N+1,)` all-contact count，且每个 audit array 都有非 placeholder source/status。新 immutable synthetic namespace=`probe_outputs/nonformal_pipeline_dry_run_runtime_v2_20260828_cpu2/` 通过；receipt SHA-256=`dd7ed40edec1680532c50afb7b382f9438cc91864028986c162790d7be0b7c95`，raw SHA-256=`ad0c1587cfe2090752a61a6196d53341cd8216f7946f4c032fac2b7e3d91a7f0`。cpu1 保留为第二轮 raw-audit 强化前的历史证据。
- Current static audit=`runtime_v2_cpu_static_audit_20260828_v3.json`，SHA-256=`a1a9da3cdda843e487e8f661c542edd81224e4a25e288fdd95aaefbdcfc92942`；新增 `runtime_v2_completion_audit_20260828.{md,json}`，明确 `cpu_preprobe_completion=PASS` 与 overall readiness=`BLOCKED_WITH_REASONS` 并存。
- Active source 与 Vault byte-equal snapshot 均为 31/31 tests passed；38 Python files compile passed；registry 与 completion audit 的 9 个 code hashes 均匹配 active source；JSON、cpu2 raw、v3 static audit、diff-check、no-pycache 与敏感模式扫描全部通过。
- 更新后关键 hashes：implementation plan=`83f72b1aecc6bd99c0cfa30b8ecc4c4e4bfbe196029bf72bc8d6a7a0b20d08b4`；registry=`05302ecfbe8c3f52825258339e72b6e4f741224e7e9bc31e9c2cfc2d7c0109cb`；readiness=`2d4b6424fd3c12d5b3d112d99f5b436ded6484c9835bd89003bfd5aa464ccc99`；budget addendum=`8fe356fc3b1e97909fe75a7d4a32b5896b2883292ed02231a9c2f79b204a211c`；completion audit=`305fac198ad57e06d09b56a0642063b7e44ab5c7e8f024dc352a682bffee42c0`。
- 上一行 hashes 是 append 本条前的对应 current files；本日志 append 不修改这些五个文件。仍未运行任何 runtime-v2 GPU probe、Stage 0、formal collection 或训练，也未 commit/push。剩余 blocker 全部需要用户新授权或真实 GPU/SAPIEN evidence。

## 37. 2026-08-28：用户批准四条 bounded runtime-v2 nonformal probes

- 用户对上一条精确授权语句回复“批准”。授权仅覆盖 physical GPU0 上顺序运行 F1 `transport_true_inside`、F2 `actor_to_eef_stand`、F3 `return_equivalence`、F4 `common_prefix_mapping`；每 family execution attempt=1，planner-query limits=`12/12/18/12`，timeouts=`1200/1200/1800/1800 s`，无自动 retry/recovery。
- 新增 `runtime_v2_probe_authorization_receipt_20260828.json`，SHA-256=`077357fc333bcd743e80476b383e0672aba047cf52d540f2cf3767e40dd39480`。Stage 0/1、360 formal trajectories、F4 common 后续 blocks/programs、训练与 compression 仍明确未授权。
- Active authorization bit、family audit status、implementation plan、runtime registry/readiness 和 probe budget addendum 已更新；Stage 0 readiness 仍为 `BLOCKED_WITH_REASONS`。Active 与 Vault snapshot 重新 byte-equal，双方 31/31 CPU tests passed，38 files compile passed。
- 授权后 current static audit=`runtime_v2_cpu_static_audit_20260828_v4_authorized.json`，SHA-256=`b2dffa0ec86bc74afec45323aa8e15d7d64199f8bfd477baeb3f2a8275766e51`；它仍是 CPU evidence，不是 family runtime pass。
- 一次只读历史 guard 路径查询因猜错文件名返回 not found；没有创建/修改 artifact，也没有启动 child。下一步先 fresh 查询 physical GPU0；只有 near-baseline memory、0% utilization/P8、无 compute process 时才进入原子 guard。

## 38. 2026-08-28 01:34 CST：F1 首次原子 precheck 被外部 GPU0 作业阻止

- 沙箱内 `nvidia-smi` 因无法访问 driver 返回错误；按规则没有据此启动 child。随后在获准的沙箱外只读检查中，GPU0=`33,677 MiB / 100% / P2`，UUID=`GPU-2c620e6c-9639-2022-b573-9847dfa33769`，已有外部 compute PID 3760440；GPU1–7 也忙，但当前 device scope 禁止替代 GPU0。
- 运行 F1 guard v2 做原子 immediate recheck：GPU0=`36,709 MiB / 100% / P2`，同一外部 PID 3760440 使用 `36,692 MiB`。Guard status=`blocked_precheck_not_idle`、exit=42；child/PID/PGID/scene/planner/execution 均未产生，F1 output directory 未创建。
- Guard receipt=`probe_outputs/f1_transport_and_true_inside_v2_20260828_gpu0_run1_guard.json`，SHA-256=`84be21db9bf5a78026868f083129cd45a1116797e901d1e2ffcd544f23ece5ae`；`formal_data=false/stage0_data=false`。本任务未共享、signal、kill 或干预外部进程。
- 此 precheck blocker 不消耗 F1 execution/planner budget，也不视为 family physical failure。授权继续有效；current status=`approved_waiting_for_fresh_idle_gpu0`。后续只能再次 fresh-check GPU0，不得改用 GPU1–7。

## 39. 2026-08-28：授权后 GPU 状态复核仍无空闲卡

- Fresh all-card snapshot：GPU0–7 memory used=`37709/38861/39824/24120/38645/33505/29033/30447 MiB`，utilization=`96/100/97/100/100/69/71/89%`，全部 P2；每张卡均有外部 `kvzip-mechanism-study` compute process。
- 当前 GPU0 UUID=`GPU-2c620e6c-9639-2022-b573-9847dfa33769`，外部 PID=`861287`，使用约 `37,694 MiB`。因此没有 eligible GPU；本任务未启动 guard child、未共享或干预任何进程。
- 当前 scope 仍只允许 physical GPU0，故即使其他编号后续先释放也不能替代。Probe authorization 保持有效，F1 execution/planner budget 未消耗。

## 40. 2026-08-28：用户扩展 device scope，GPU1 空闲并修复 child CUDA 环境

- 用户最新明确覆盖 §37–§39 的 GPU0-only device scope：当前四条 bounded probes 可使用 physical GPU0–7 中任一 independently fresh-idle 卡；全局仍顺序执行、一卡一 job、每个 child 启动前即时 guard。新增 `runtime_v2_probe_authorization_scope_update_20260828.json`，SHA-256=`cb47ee004bca410f17ffa4ed1e0dc96cc2462decbec03fd7bdf8560fc2c2f5`。
- Fresh snapshot 发现 GPU1=`14 MiB/0%/P8/no compute process`，UUID=`GPU-414c52ba-72c6-fc45-95d6-1e9750bbc21b`；其他卡忙。Guard/parser/AGENTS 与 tests 更新为 any-index scope，31/31 当时回归通过。
- F1 首个 GPU1 child 在 scene import 前失败：调用者继承 host `/share/apps/cuda/12.2` 的 `LD_LIBRARY_PATH`，读取 `libnvJitLink.so.12` 发生 NFS I/O error。Receipt execution=0、planner=0、scene_created=false；guard cleanup/release verified，故不消耗 F1 family attempt。
- Guard 随后内建 `robotwin_workspace_cuda12_1_v1` child environment：unset `LD_LIBRARY_PATH`，固定 workspace env/CUDA12.1/miniforge PATH、CUDA_HOME 与 cache/tmp。Sanitized CPU import 验证 `torch 2.4.1+cu121 / CUDA 12.1`；新增 regression 后 active/snapshot=32/32 tests passed。

## 41. 2026-08-28 12:33–12:38 CST：四条 runtime-v2 bounded probes 终态

- 全部实际 family runs 使用 GPU1，依次经过独立 immediate precheck=`14 MiB/0%/P8/no process`；每次 postcheck 均回到 `14 MiB/0%/no process`，post-release verified=true。Actual family executions=4，timeouts=0，scene cleanup failures=0，task-owned orphans=0。
- F1 `transport_true_inside`：PASS，execution=1、planner=9/12、trace=3,013。红方块 true-cavity OBB、50-frame box support、gripper open、full rest/stationarity 全通过；绿/蓝在所有阶段最大位移=`4.22e-7/0 m`。Receipt/trace hashes=`e20efe9e.../9015bc7c...`。Claim 仅限红分支；绿/蓝 target branches 未运行。
- F2 `actor_to_eef_stand`：terminal `aborted_with_reason`，execution=1、planner=7/12、trace=1,304。固定 `071_can/base1 + left arm + stand`；三个预注册 yaw 均通过 workspace/swept geometry，但真实 planner preflight 均=`Fail`，不得增加第四姿态或换物体/臂/设施。Receipt/trace hashes=`cffa48fc.../13f9fa5f...`。
- F3 `return_equivalence`：terminal semantic FAIL，execution=1、planner=14/18、trace=4,376。V/H 的 EEF+bottle amplitude/off-axis/central-return/orientation-drift/selected-gripper continuity 全部 PASS，contact fraction=1.0、break=0；最终 bottle position/orientation errors=`0.04096 m/0.15587`，return-equivalence FAIL。Receipt/trace hashes=`145f1497.../5cd08f74...`。
- F4 `common_prefix_mapping`：terminal `failed_planner`，execution=1、planner=6/12、trace=1,887。Swept geometry PASS，但 `safe_horizontal_waypoint` planner FAIL；按 ordered Gate 未运行 A/B/C 或 ABC/ACB/BAC。Receipt/trace hashes=`606da80f.../4ef07568...`。
- 新增 `runtime_v2_bounded_probe_execution_report_20260828.{md,json}`。本轮最终为 1 个红分支 pass / 3 family terminal blockers；runtime-v2 probe addendum=`executed_and_exhausted`，剩余 planner headroom 不是 retry 授权。Stage 0 继续 `BLOCKED_WITH_REASONS`。
- 授权后/any-GPU current static audit=`runtime_v2_cpu_static_audit_20260828_v5_any_gpu.json`，SHA-256=`3d613488f88e0da4f78cd9ef40c3a49aa56e28d10e3ff9db48f33cb190abba74`。Active 与 byte-equal Vault snapshot 32/32 tests passed，38 files compile passed。
- 没有 Stage 0、Stage 1、formal collection、模型训练、compression 或 π0.5。下一安全动作是停止 GPU probing，等待对 F2/F3/F4 与 F1 绿/蓝扩展做新 impact review/implementation version。

## 42. 2026-08-28：runtime-v2 bounded round 最终机器闭包

- 机器复核四个 current family receipts/traces：F1 semantic pass=true；F2/F3/F4=false；planner counts=`9/7/14/6` 均未超限，trace samples=`3013/1304/4376/1887`。所有 receipts/guards 均 `formal_data=false/stage0_data=false`，scene cleanup succeeded、orphan=0、post-release verified。
- Current registry 的 9 个 code hashes 与 active source 完全匹配；active/snapshot `diff -qr` 零差异，双方 32/32 tests passed，38 files compile passed，JSON/forbidden-flag/diff-check 全通过。
- Guard 父进程启动时生成两个 task-owned `__pycache__` 目录；已按精确路径删除并复核 active source/tests 无 `__pycache__/.pyc`。这不是 GPU process orphan。
- Current hashes：execution report=`3d54b6af0d54b6cee398a96d25f71475b0de5b05a0968f6fa6b34c49dfa6c399`；implementation plan=`1e2c4d0548ac56f45d57715d8ffe8c8e1326dbfd2c362f9925fed713657e88fb`；registry=`1ccd5f5ecbc0b46631777216b5ae3b0f304bacf25734dafd461fc067e5ebded2`；readiness=`08009d029a288dd43b7bfa8eacc78c1f0267d2ed0d1d1eef93713534c749709f`；runtime-v2 budget addendum=`0c27910ed72af34cfc24e97df8e2187f36b9f55cedf519a33b18a80f50661073`。
- 最终 decision=`BLOCKED_WITH_REASONS`。本实现版本的四个 execution budgets 已用尽，不得把未使用的 planner headroom 解释为 retry 权限；停止 GPU probing并等待新 review。

## 43. 2026-08-28：准备发布 runtime-v2 GPT 审阅包

- 用户明确要求总结并 push Vault，同时提供可直接转发给 GPT 的说明。本授权仅适用于 Vault；RoboTwin active source 仍不 commit/push，只通过 byte-equal Vault snapshot 发布。
- 新增自包含入口 `数据构造/实现审计/GPT_REVIEW_HANDOFF_RUNTIME_V2_20260828.md`，列出 canonical docs、四个 family 终态、receipts/traces、代码快照、claim boundary 和 GPT 下一版 review questions。
- Push 前终审：active/snapshot 32/32 tests passed；101 JSON、6 份本轮相关 NPZ parse passed；byte-equal/diff-check/no-pycache/sensitive scan passed；无超过 20 MiB 文件。Staged scope=89 files、6,614 insertions、87 deletions。
- Content commit=`3a98c079930e3c7f12b9633893b152db09156d07`，message=`docs: publish runtime-v2 bounded probe evidence`。Handoff 已回填该 SHA；本条与回填将形成 closeout commit，然后一次 push 两笔 commit 到 `origin/main`。

## 44. 2026-08-28：runtime-v2 GPT 审阅包首次发布成功

- Handoff closeout commit=`15d62ebf33e7896fd99d357a596e698a0a4acbfa`，message=`docs: record runtime-v2 GPT review handoff`。
- `git push origin main` 成功：远端从 `7350099da8402aca2df63eed4ca3f6efbce7024d` 前进到 `15d62ebf33e7896fd99d357a596e698a0a4acbfa`；`git ls-remote`、local HEAD 与 `origin/main` 完全一致，ahead/behind=`0/0`，工作树干净。
- 本条作为 publication receipt 将形成最后一个小 commit 并再次 push；不会改动任何 probe evidence、readiness 结论或 RoboTwin active source。

## 45. 2026-08-28：完整 GPT review 收到，runtime-v3 CPU/static 启动

- GPT 维持 `BLOCKED_WITH_REASONS` 并明确禁止 Stage 0；冻结科学设计仍为 4 families、40 accepted roots、360 formal trajectories、same-current/fresh-scene/root atomicity，`H_reveal=null`，无 compression/policy transfer。
- Review 将下一实现定为 `controlled_multi_future_runtime_v3`：F1 `f1_three_branch_coverage_v3`、F2 `f2_workspace_reachability_v4`、F3 `f3_release_dynamics_diagnosis_v3`、F4 `f4_segmented_common_carry_v3`、Pipeline `real_sapien_pilot_root_orchestrator_v1`、Raw `controller_effective_setpoint_v1_layout_v2_1`。
- 当前只授权 CPU/static 实现；runtime-v3 GPU budget 必须保持 `proposed_for_user_review / approved=false / frozen=false`。本阶段未调用 GPU，也未授权 Stage 0。
- 修正 current 文档：readiness Markdown 不再写 GPU0-only 或“四条 probe 未运行”；action runner docstring 更新为 any fresh-idle GPU0–7；F1 机器表述收紧为 `true-inside + stable + continuous box body-pair contact`，不再把未验证法向的 contact 升级成严格盒底 support。

## 46. 2026-08-28：runtime-v3 raw、family 与 root contracts CPU 结果

- Raw v2_1：显式保存 `state_timestamps[N+1]`、`action_interval_start/end[N]`，并验证 `state[k] -- action[k] on [start,end) --> state[k+1]`；`planner_target` 改名 `planner_goal_eef_pose`；`gripper_aperture` 改名 `gripper_drive_target_readback`，新增 actual left/right gripper joint qpos N+1 streams。
- Current hash 现强制 head RGB、左右 wrist RGB、robot state、actual gripper state、object role/layout、camera config/version、scene seed/generator version。
- F1 contract：red→green→blue 固定顺序、target-neutral common prefix、每分支 fresh scene、同 scene/current/prefix hashes、仅 3/3 pass。F2：固定 6 complete candidates、同 planner seed/start state、preplace+release planner、禁止换臂/换物体/无限 yaw。F3：before-release 与 1/5/10/25/50/125/250/after-rest diagnostics，按 pre-release offset vs post-release dynamics 条件式分流。F4：asset-derived minimum height、固定 Route1/Route2、每段 endpoint preflight、tray immutable。
- 新增 root orchestrator：pristine current/anchor 在 feasibility 前捕获；三个 candidate 使用 disposable scenes；candidate/task-tree/prefix freeze 一次；三个 rollout 使用 fresh scenes；root finalizer 要求 3/3 receipts。Branch failure 会保留并继续后续预注册分支，cleanup uncertainty 才停止。
- 第一次 runtime-v3 suite 因测试 helper 命名为 `run()` 覆盖 `unittest.TestCase.run()` 而失败；重命名 `run_root()` 后通过，失败未隐去。
- Raw cpu3 暴露 manifest schema 仍为旧 `cmf_raw_attempt_v2`；修正为 `cmf_raw_attempt_v2_1` 后生成 cpu4。随后 same-current hash 加入 gripper/camera components，最终 current raw evidence 为 cpu5：receipt SHA-256=`d1018661f1503c653c64bace7edd11772e85df6455ec02d50320d9107a7c6ba7`。
- Root dry-run cpu1 之后因 current hash 强化而被 cpu2 supersede。Current root-cpu2：pristine capture、3 feasibility scenes、freeze_call_count=1、3 rollout scenes、3/3 finalizer passed；root receipt SHA-256=`ff9645657f271bb4683c04ea15cc50cc173e5b5528db45e4273ba0df9a7e432e`。这些均为 synthetic CPU evidence，不证明真实 SAPIEN。
- Active 与 byte-equal Vault snapshot 45/45 tests passed，43 Python files compile passed。新增 runtime-v3 proposal、budget proposal、CPU registry、readiness 与 static audit；所有状态保持 GPU/Stage0 unauthorized。

## 47. 2026-08-28：runtime-v3 CPU/static 最终机器闭包

- Active 与 Vault snapshot 分别 45/45 tests passed；43 Python files compile passed；两组 `diff -qr` 零差异，无 `__pycache__/.pyc`。10 个 runtime-v3 code hashes 同时匹配 active source、v3 registry 与 v3 static audit。
- Raw cpu5 复核：schema=`cmf_raw_attempt_v2_1`，layout=`controller_effective_setpoint_v1_layout_v2_1`；action start/end 分别严格等于 state timestamps 的前/后切片；actual left/right gripper qpos 与 drive-target readback 均为 N+1。Current hash 包含 gripper/camera components。
- Root-cpu2 复核：status=accepted、pristine current/anchor 先于 feasibility、3 disposable feasibility receipts、freeze_call_count=1、3 fresh branch receipts、3/3 root finalizer accepted。仍明确 `formal_data=false/stage0_data=false/synthetic=true`。
- Current artifacts hashes：runtime-v3 proposal=`48670a8d2d9c0bb410b1d5bfec56eb7689f63077964015381e16bb149f3f3738`；budget proposal=`e985ca292a4e146d0ce925d7d5d73c959c4061deefb8c1be0ecde401926cbc7b`；v3 registry=`6db2e9de81ae65c7339edc0fcb2ee854b1538e2cc78d730a651aa496cee3e567`；v3 readiness=`6e633dff43c218e6a0744bc2deb8e937c420e2fe72fc6190e05ee4df959adf5e`；v3 static audit=`84244717898f60515d4e018941b2dc2c7514b3d1b4cc1ff3869b831040f04ff8`。
- 最终状态仍为 `BLOCKED_WITH_REASONS`。Runtime-v3 budget=`proposed_for_user_review / approved=false / frozen=false`，real SAPIEN adapter 未实现，未运行任何 runtime-v3 GPU probe，也未启动 Stage 0、formal collection 或训练。

## 48. 2026-08-28：准备发布 runtime-v3 CPU/static GPT 审阅包

- 用户明确授权 commit/push Vault，并要求生成可直接转发给 GPT 的反馈。本授权仍只覆盖 `/nfs_share/lijunhui/Vault-on-Fvl09`；RoboTwin active source 不 commit/push，只发布 byte-equal 代码审阅快照。
- 新增自包含入口 `数据构造/实现审计/GPT_REVIEW_HANDOFF_RUNTIME_V3_CPU_20260828.md`，明确区分 runtime-v2 immutable 真实物理证据、runtime-v3 CPU/static contracts、synthetic evidence 和尚未实现的 real SAPIEN adapter；当前仍为 `BLOCKED_WITH_REASONS`，runtime-v3 GPU 与 Stage 0 均未授权。
- 发布前双方 unittest 均为 45/45 passed；43 个 Python 文件无写入 compile passed；active/source snapshot 两组 `diff -qr` 零差异；10/10 runtime-v3 code hashes 同时匹配 active、snapshot 与 static audit。
- Current raw-cpu5 复核通过 `cmf_raw_attempt_v2_1`、26-D、N/N+1、action interval timestamps 与 actual gripper streams；current root-cpu2 复核通过 pristine-before-feasibility、3 disposable feasibility scenes、freeze once、3 fresh branch receipts 与 3/3 synthetic finalizer。Authorization/readiness Gates 全部保持 closed。
- 临时发布校验器前三次分别因自身错误假定 tests 相对路径、NPZ 无 namespace key、root receipt 顶层存在 `synthetic` 而失败；按实际 registry/raw/root schema 修正后完整 PASS。项目代码、机器 artifacts 和 readiness 未因这些校验器错误而修改；临时脚本随后删除，失败未隐去。
- 代码审阅快照 README 的 current implementation 标记从陈旧 runtime-v2 修正为 runtime-v3，并补充 `runtime_v3_contracts.py`、`root_orchestrator.py` 与 raw-v2_1 边界。下一步是 staged diff/sensitive/size review、content commit、handoff commit 与 remote verification；本阶段不运行 GPU。
- Content commit=`dec39d65ce1607740cef311ca5a4b66572c12ce7`，message=`docs: publish runtime-v3 CPU preparation`，共 82 files、6,363 insertions、83 deletions。Handoff 已回填该 SHA；本条回填将形成一个小的 closeout commit，随后推送并核对远端。

## 49. 2026-08-28：runtime-v3 CPU/static GPT 审阅包首次发布成功

- Handoff closeout commit=`97bff47fb9e99635b260db5b24a22337780aa192`，message=`docs: record runtime-v3 GPT review handoff`。
- `git push origin main` 成功：远端从 `89721bc8ebc716f9ee72088a81d7ecc36ab985c9` 前进到 `97bff47fb9e99635b260db5b24a22337780aa192`；随后 `git ls-remote origin refs/heads/main` 返回同一 SHA，本地 HEAD、remote-tracking ref 与 GitHub remote 一致。
- 本条只记录发布回执，不修改 proposal、budget、registry、readiness、code snapshot 或 probe evidence。它将形成最后一个小 commit 并再次推送；runtime-v3 仍未获 GPU/Stage 0 授权。

## 50. 2026-08-29：完整 runtime-v3 review 收到，启动 v3_1 CPU/code 加固

- 用户明确说明此前审阅文本不完整，以 2026-08-29 完整版为准；GPT 继续裁决 `BLOCKED_WITH_REASONS`，不批准 runtime-v3 GPU budget，更不批准 Stage 0。
- 新实现版本固定为 `controlled_multi_future_runtime_v3_1`；科学设计仍为 `controlled_multi_future_f1_f4_v1_2`，不改 4 families、40 roots、360 trajectories、R=3、5/2/3 split、F3/F4 programs 或 primary 26-D layout。
- 本阶段只授权 CPU/code 修复、Vault 同步和用户随后明确要求的 commit/push；没有 GPU 查询、SAPIEN scene、A0、action probe、Stage 0 或训练。

## 51. 2026-08-29：root/raw/current/anchor 与真实 adapter 加固

- 新增 `root_orchestrator_v1_1.py`：task/physical audit 与 post-freeze planner solvability 分账；cleanup uncertainty 使用专门异常立即终止；每场 scene 必须有唯一 scene-bound cleanup receipt；planned spec、program、task-tree/prefix input 与 planner variant 均 detached-copy 并做返回/异常路径 hash 复核；feasibility 前写 provisional artifacts；raw 保存后先写 `raw_saved_verifier_pending`；finalizer 直接复核 branch current/anchor 和 actual executed-prefix bytes/steps/start/end anchors。
- Raw schema 升为 `cmf_raw_attempt_v2_1_1`，primary layout 仍为 `controller_effective_setpoint_v1_layout_v2_1`。真实 trace 从 `SAPIEN Scene.get_timestep()`读取 timestep，要求 0.004 s 和每 action 一个 step；planner goal 保存 query ID/active/source/start/end 并与 query table 交叉验证；NPZ、trace source 与 manifest file 均有 integrity hash。Manifest 自身 file hash 使用 sidecar `manifest.sha256.json`，避免自引用哈希悖论。
- 新增 `current_context_hash_v2` 与 `physical_anchor_v2`：实际 camera config 和完整 physical entity schema 强制；model-visible/hidden-physical 分 hash；hidden physical 禁止作为模型输入；anchor 保存 actor velocity/sleep、facility pose、drive target、gripper qpos、physics/source commit，并使用 q/-q invariant angular comparison。
- 新增 concrete lazy-import `RoboTwinRealSapienPilotRootAdapterV1_1`、F1 root runner、F2/F3/F4 repair runners、F2/F4 fresh planner variants、A0 zero-action entry point、F1-only root entry point、F2/F3/F4 single-repair orchestrator/entry point，以及 content-hashed authorization receipt Gate。当前不存在授权 receipt，所有 CLI 均 fail closed。
- F3 当前只实现下一 repair 的 V→H release diagnosis；完整 VVHH/VHVH/VHHV 明确 `full_f3_program_complete=false`。F4 当前只实现 common-X Route1/Route2 repair；A/B/C 与 ABC/ACB/BAC 明确 `full_f4_program_complete=false`。二者即使 repair signal 通过也不能被 root finalizer 接受。

## 52. 2026-08-29：CPU tests 的失败链与终态

- 第一轮新增测试为 58/61：三个失败均来自历史测试仍硬编码旧 `F1-green_block` 和 runtime-v2 family version；改成动态 program ID，并同时验证 runtime-v2 evidence 常量保留、current family version 已升 v3_1、GPU Gate=false。
- Root/raw/current/anchor/adapter tests 随后整体通过。新增 active planner-goal 测试第一次因 fixture 把 goal 改为可用却仍标 `unavailable` 而失败，证明 validator 会拒绝 metadata/value 不一致；第二次因 synthetic `planner_goal_source` 使用 `U1` 导致 `left_move_to_pose` 被截断而失败，改为 `U64` 后通过。
- 新增 F4 repair orchestration tests：Route1 terminal non-cleanup failure 后 Route2 使用不同 fresh scene；Route1 cleanup uncertainty 立即终止且 Route2 不创建。
- Active 与 byte-equal Vault snapshot 最终均为 69/69 tests passed；57 Python files 无写入 compile passed。测试和校验全程未导入真实 scene entry、未调用 GPU。

## 53. 2026-08-29：runtime-v3_1 synthetic evidence 与快照同步

- 新增 cpu1 root dry-run，随后在进一步强化 task-tree/prefix/variant input immutable exception-path checks 后保留 cpu1 不覆盖，并生成 current cpu2：`probe_outputs/nonformal_root_pipeline_dry_run_runtime_v3_1_20260829_cpu2/`。
- CPU2 root receipt SHA-256=`f90e54de74d419a3f0cef5ed62a4e27d330d8de76d948cbed3c5a85bb8427b25`；status=accepted synthetic-only；freeze=1；task/physical=3、planner=3、rollout branches=3；10 个 unique cleanup receipts 全 pass/orphan=0；三条 raw integrity 3/3；actual-prefix finalizer pass。
- 第一次独立 cpu2 校验命令漏设 `PYTHONPATH`，在 import integrity helper 前退出，artifact 未修改；补上 active source `PYTHONPATH` 后完整通过。
- 首次 `cp -r --no-preserve=...` 同步内容成功但因 NFS 拒绝 chmod 返回非零；立即 `diff -qr` 证明 active/snapshot 零差异，随后使用 `rsync -r --checksum --no-perms --no-owner --no-group --omit-dir-times` 完成无权限元数据操作的干净同步。该同步错误未隐去。
- 新增 runtime-v3_1 proposal、budget、registry、readiness、static audit 与 GPT handoff；budget 仍 `proposed_for_user_review / approved=false / frozen=false`，readiness 仍 `BLOCKED_WITH_REASONS`。`数据构造方案.md` 总览中的陈旧 `not started` 已按 GPT 建议修正为 `runtime implementation partial / blocked`，不改变科学设计。

## 54. 2026-08-29：F2 total-query budget 反证修复与 cpu3 current evidence

- 最终 budget cross-check 发现 F2 最初 concrete runner 对每个 pose candidate 规划完整 grasp→rest 链，六 candidates 最坏会超过 GPT 指定的 `16 queries total`。该问题在任何 GPU 前被 CPU 审计捕获。
- F2 candidate planner audit 改为准确的两段链：统一 reset start→preplace、preplace terminal qpos→release；grasp/lift/retreat/rest 只在选中 candidate 的一次真实 rollout 中执行。于是 inside=2、on=2、六个 beside candidates≤12，root 最坏总计严格≤16。Root 与 single-repair orchestrator 均新增 `planner_query_count_total` 和 F2 `>16 → failed_budget_exhausted` Gate。
- 新增行为测试证明单个 F2 candidate 恰好两次 chained queries；active suite=70/70 passed。由于 code/root receipt 字段变化，cpu2 保留不覆盖，生成 current cpu3=`probe_outputs/nonformal_root_pipeline_dry_run_runtime_v3_1_20260829_cpu3/`。
- Cpu3 status=accepted synthetic-only、freeze=1、planner_query_count_total=0（synthetic planner）、cleanup=10/10、raw integrity=3/3、root finalizer pass；root receipt SHA-256=`ab251b16bc10850ced93768b278c2b1c3513d31b324784f5ad84f3871cb87be6`。Active/snapshot 重新 rsync byte-equal；仍无 GPU/A0/Stage 0。

## 55. 2026-08-29：planner count 语义分流与 cpu4 current evidence

- 最终 receipt review 将 post-freeze planner-audit queries 与真实 rollout planner queries 分开：root/family repair receipts 使用 `planner_solvability_query_count_total`，每条 branch/repair 另存 `rollout_planner_query_count`。避免把前者误读为已经包含执行阶段。
- 修改后 active tests 仍为 70/70。Cpu3 保留不覆盖，current cpu4=`probe_outputs/nonformal_root_pipeline_dry_run_runtime_v3_1_20260829_cpu4/`：status=accepted synthetic-only、freeze=1、planner_solvability_query_count_total=0、三 branch rollout planner counts=`0/0/0`、cleanup=10/10、raw integrity=3/3、finalizer pass。
- Cpu4 root receipt SHA-256=`8286b87be8e6b4116abc0517e60cbc01d5651fcf63071dd8396f632ce766a3e9`。Current docs/registry/readiness/AGENTS 与代码快照均改指 cpu4；仍无 GPU/A0/Stage 0。

## 56. 2026-08-29：runtime-v3_1 CPU/static 最终机器闭包与发布准备

- Active 与 Vault snapshot 各 70/70 tests passed；57 Python files 无写入 compile passed；两组 `diff -qr` 零差异。Registry 18/18 code hashes 同时匹配 active 与 snapshot。
- 25 份 runtime-v3_1/current-evidence JSON parse passed；cpu4 root/raw independent integrity passed；Git diff check、no-pycache、无 >20 MiB 新 evidence、敏感凭据模式扫描均 passed。Handoff 中唯一 placeholder 是待 content commit 回填的 `PENDING_CONTENT_COMMIT`。
- Current proposal/budget/registry/readiness/static audit 均保持 `gpu_probe_authorized=false`、`stage0_authorized=false`；budget=`proposed_for_user_review / approved=false / frozen=false`；Stage 0/1/formal counts=0，`H_reveal=null`，无 training/compression/π0.5。
- 用户已明确要求完成后 commit/push Vault 并提供可转发给 GPT 的回复。本授权仅覆盖 Vault 发布；active RoboTwin official tracked baseline 仍不 commit/push，只通过 byte-equal review snapshot 发布。
- Content commit=`46da72f02166756034080133d7114002e1fc88df`，message=`docs: publish runtime-v3.1 CPU hardening`，共 129 files、17,164 insertions、80 deletions。Handoff 已回填该 SHA；回填与本条将形成 closeout commit，然后推送并核对 GitHub remote。

## 57. 2026-08-29：runtime-v3_1 GPT 审阅包首次发布成功

- Handoff closeout commit=`ea983ff3c10f65e2aa8567e75d3a68094265076b`，message=`docs: record runtime-v3.1 GPT review handoff`。
- `git push origin main` 成功：远端从 `957807acced63b8d2077e0fc785fd509594f3217` 前进到 `ea983ff3c10f65e2aa8567e75d3a68094265076b`；`git ls-remote origin refs/heads/main` 返回同一 SHA，本地 HEAD 与 remote-tracking ref 一致。
- 本条只记录发布回执，不修改 implementation、budget、registry、readiness、snapshot 或 evidence；将形成最后一个小 commit 并再次 push。GPU/A0/Stage 0 仍未授权。

## 58. 2026-08-29：第二轮 completion audit 发现并修复 prefix/current/verifier 缺口

- 在上一审阅包发布后继续按完整目标做反证审计，发现 F1 rollout 在 prefix 前读取 target role/target-specific execution spec，且 `first_post_prefix_divergence_step` 由 adapter 自报。修复后 prefix 使用固定 audit actor、固定 neutral 公式与固定 planner reset；target role只在 prefix end anchor 后读取。Root 直接从 raw effective actions重算 prefix hash和逐步 suffix hashes，横向求首次分叉；三 suffix 永不分叉会失败。
- 第一轮新 divergence tests 发生 4 个失败：一个静态测试误把函数签名参数名视为提前读取；三个 synthetic adapters 缺少新 post-prefix hashes。根因修复为 orchestrator从 raw统一派生，而不是给夹具补自报字段；synthetic actions 改为前两步相同、边界后三角色分叉。失败未隐去。
- Same-current 职责重新校准：精确 Gate 只覆盖 model-visible current + reconstruction spec；hidden pose/velocity hash 仅作 audit，由 `physical_anchor_v2` position/orientation/velocity/sleep tolerance 比较。Anchor 另精确锁定每实体 asset/body/mass/friction/collision、simulation config、official commit 与 additive source aggregate hash。
- F1/F2/F3/F4 verifier 加严：F1 从 prefix 前 baseline 做 staged non-target displacement 并检查 support/rest/stationarity；F2 增加 geometry-only fit/clearance、真实 full-qpos preplace→release chain和 support/rest；F3 增加 50-frame initial pad stability、release speed/contact normal/impulse/gripper qpos、final rest Gate；F4 增加 A/B/C staged noninterference、neutral/rest Gate，gripper envelope改为 runtime selected-link poses推导。

## 59. 2026-08-29：raw trace integrity 与 atomic GPU guard 加固

- Real scene 若支持 `save_trace`，root/family repair 在 raw 前独立保存 `trace_source.npz`，provenance 写相对路径和文件 SHA；integrity verifier实际重哈希该文件。Manifest payload hash不再只比较字段，而是删除 self-hash 字段后重新 canonical hash；sidecar 文件自身 SHA进入 branch receipt。
- Planner terminal 从 CuRobo 6-D arm path 合并回完整 articulation qpos，保留夹爪关节后再作为下一段 `last_qpos` 和 chain hash；新增交错 arm/gripper joint-order 单测。
- GPU guard 在 launch 前写 `precheck_passed` receipt，并传入 guard path/index；child 必须匹配当前 parent PID、UUID/index、P8/≤100MiB/≤1%/无 compute process，且 snapshot age≤60秒。A0/root/family entry 均写顶层 `receipt.json` 供 guard postcheck；scene/planner orphan 与外层 process-group orphan 分开记录后合计，任何一侧非零均 fail closed。
- 新增 valid/busy/stale/missing atomic guard tests、top-level receipt postcheck test、independent trace tamper test、manifest payload recompute test、runtime gripper-envelope test和 F2 full-qpos merge test。Current active/snapshot suite=79/79 passed，57 Python files compile passed。

## 60. 2026-08-29：cpu5→cpu6 current evidence 与 hardening-v2 审阅准备

- Cpu5 首次证明 root 从 raw 得到 one prefix hash、three first-suffix hashes、computed divergence step=2。后续 guard/family/current 加固不覆盖 cpu5，生成 current cpu6=`probe_outputs/nonformal_root_pipeline_dry_run_runtime_v3_1_20260829_cpu6/`。
- Cpu6 status=accepted synthetic-only、freeze=1、task/physical=3、planner=3、rollout=3、cleanup=10/10、raw integrity=3/3、prefix hash count=1、first suffix hash count=3、computed divergence step=2；root receipt SHA-256=`6aca38c2f49f8fad59834e45c6aad47dc3da7db0535b63af2d321d520dea8bcb`。
- 新增 current handoff `GPT_REVIEW_HANDOFF_RUNTIME_V3_1_CPU_V2_20260829.md`、static audit v2，并更新 proposal/budget/registry/readiness/AGENTS/snapshot README。状态仍 `BLOCKED_WITH_REASONS`；没有 GPU/A0/Stage 0/formal data/training。

## 61. 2026-08-29：runtime-v3_1 hardening-v2 最终机器闭包

- Active 与 Vault snapshot 各 79/79 tests passed；57 Python files compile passed；两组 `diff -qr` 零差异。Registry 20/20 current code hashes同时匹配 active 与 snapshot。
- Cpu6 root/raw independent validation passed；26 份 current/new-evidence JSON parse passed；Git diff check、no-pycache、无 >20 MiB 新 evidence、敏感凭据模式扫描均 passed。
- Current status保持 `BLOCKED_WITH_REASONS`；budget=`proposed_for_user_review / approved=false / frozen=false`；不存在 runtime-v3_1 GPU authorization receipt；Stage 0/1/formal counts=0，`H_reveal=null`，无 training/compression/π0.5。
- 用户此前明确授权完成后 commit/push 并要求可转发 GPT 的回复；本轮将发布新的 V2 handoff，不改动 active official RoboTwin tracked baseline，不运行 GPU。
- Hardening-v2 content commit=`207d566fa67b61de23142baa4b1d1be552d58924`，message=`docs: publish runtime-v3.1 hardening v2`，共 75 files、7,169 insertions、140 deletions。V2 handoff 已回填该 SHA；将形成 closeout commit 后推送。

## 62. 2026-08-29：runtime-v3_1 hardening-v2 审阅包首次发布成功

- Handoff closeout commit=`fd6511c4e1d85eebf97d62babf367eb333ec320e`，message=`docs: record runtime-v3.1 hardening-v2 handoff`。
- `git push origin main` 成功：远端从 `d56d96ccdf19404bbb90799baf4bf3f76b14d267` 前进到 `fd6511c4e1d85eebf97d62babf367eb333ec320e`；`git ls-remote` 返回同一 SHA。
- 本条只记录发布回执；将形成最后一个小 commit 并再次 push。GPU/A0/Stage 0 仍未授权。

## 63. 2026-08-29：F3 conditional correction 状态机补齐

- Completion audit 发现 F3 budget 虽规定“1 diagnosis + 条件满足时 1 correction”，但此前 concrete runner只有 diagnosis 和纯合同，没有完整 conditional execution state machine。
- 新增 `f3_conditional_repair_orchestrator_v1_1.py`：先执行一次 fresh diagnosis；仅当 classification=`pre_release_systematic_offset`、grasp transform stable、EEF tracking正常时，使用 measured EEF/bottle/target poses生成唯一 content-hashed correction spec。公式固定为 `T_world_actor_target @ inverse(T_eef_actor_measured_before_release)`，阈值不可放宽。
- Correction 使用独立 fresh scene，重新比较 diagnosis/correction reference current 与 physical anchor；最多执行一次。Grasp slip、post-release physics、tracking failure、cleanup uncertainty、spec hash failure或current/anchor mismatch均禁止/终止 correction；无自动 retry。
- F3 release samples 新增实际 EEF pose、bottle pose、target pose与 commanded release pose；family repair receipt暴露 reference current/anchor hashes。Real F3 CLI 已接入 conditional orchestrator，顶层 guard receipt汇总两次 attempt cleanup。
- 新增 correction deterministic/one-attempt、grasp-slip-no-correction、current-mismatch-fail tests。Active suite=83/83 passed；Python files=59。

## 64. 2026-08-29：cpu7 与 hardening-v3 审阅准备

- Cpu6 保留不覆盖；在 F3 conditional code 完成后生成 current cpu7=`probe_outputs/nonformal_root_pipeline_dry_run_runtime_v3_1_20260829_cpu7/`。Cpu7 status=accepted synthetic-only、cleanup=10/10、raw integrity=3/3、prefix hash count=1、first suffix hash count=3、computed divergence step=2；root receipt SHA-256=`91396a9b07af1c98d2bc566a56aa8af0a6141278c6d04bca987986cc2d383657`。
- Current revision 升为 `runtime_v3_1_cpu_hardening_v3`；新增 V3 handoff/static audit，更新 proposal/budget/registry/readiness/AGENTS/snapshot README。真实 GPU/A0/Stage 0 仍为 0/未授权。

## 65. 2026-08-29：runtime-v3_1 hardening-v3 最终机器闭包

- Active 与 Vault snapshot 各 83/83 tests passed；59 Python files compile passed；两组 `diff -qr` 零差异。Registry 22/22 current hashes同时匹配 active 与 snapshot。
- Cpu7 root/raw validation passed；27 份 current/new-evidence JSON parse passed；Git diff check、no-pycache、无 >20 MiB current evidence、敏感凭据模式扫描均 passed。
- Current decision仍为 `BLOCKED_WITH_REASONS`；budget unapproved/unfrozen；无 authorization receipt；Stage 0/1/formal counts=0；无 GPU/A0/training/compression/π0.5。
- 用户已授权完成后 commit/push；将发布 V3 handoff，active official RoboTwin tracked baseline保持不提交。
- Hardening-v3 content commit=`0c74faae237430d931c16e1c08e2913e100d22c5`，message=`docs: publish runtime-v3.1 hardening v3`，共 41 files、3,620 insertions、43 deletions。V3 handoff 已回填该 SHA，随后形成 closeout commit 并 push。

## 66. 2026-08-29：runtime-v3_1 hardening-v3 审阅包首次发布成功

- Handoff closeout commit=`9247d4b860b23d70e7c639576a0cd08682acd215`，message=`docs: record runtime-v3.1 hardening-v3 handoff`。
- `git push origin main` 成功：远端从 `6e33fb9970502c2adb1ae7bdd5692e866d98cf3c` 前进到 `9247d4b860b23d70e7c639576a0cd08682acd215`；`git ls-remote` 返回同一 SHA。
- 本条只记录发布回执；将形成最后一个小 commit 并再次 push。GPU/A0/Stage 0 仍未授权。

## 67. 2026-08-29：A0 adapter-agnostic zero-action orchestrator

- Completion audit 发现 A0 四场景逻辑仍直接嵌在 GPU CLI 中，无法在不初始化 SAPIEN/GPU 时独立反证。新增 `a0_orchestrator_v1_1.py`：固定 `A0_pristine + A0_fresh_1/2/3`，逐场捕获 current/physical anchor/activity audit，使用唯一 scene-bound cleanup receipt；current、anchor、planned spec、cleanup 或 orphan 任一异常立即终止。
- A0 activity audit 强制 `planner_query_count=0`、planner records=0、controlled action=0，并单独记录 canonical setup settle，不把物理 settle steps冒充 action。真实 A0 CLI 现在只负责内容哈希授权、atomic guard、UUID/index 绑定和 concrete adapter 装配。
- 新增 5 项 adapter-agnostic tests：四场景成功、current mismatch、anchor mismatch、cleanup uncertainty、planner activity violation。首次目标测试误用 package-style unittest 路径，产生 loader error；改用 repository discovery 后 5/5 通过。完整 active suite=88/88。
- 机器闭包校验有两次只读命令修正：第一次 shell 将换行作为字面 `\\n` 导致 SyntaxError；第二次误假设 branch manifest 含 `integrity_verification` key 导致 KeyError。改为调用正式 `verify_raw_artifact_integrity()` 后，JSON、registry 与 cpu8 raw validation 均通过。这些均未修改 evidence。

## 68. 2026-08-29：cpu8 与 hardening-v4 机器闭包

- Cpu7 保留不覆盖；生成 current cpu8=`probe_outputs/nonformal_root_pipeline_dry_run_runtime_v3_1_20260829_cpu8/`。Cpu8 status=accepted synthetic-only、freeze=1、task/physical=3、planner=3、branch=3、cleanup=10/10、raw integrity=3/3、prefix hash count=1、first suffix hash count=3、computed divergence step=2；root receipt SHA-256=`465f7af2467d28f1d6348a802422f1b155aa1e6888b744bc78225399eb207829`。
- Current revision 升为 `runtime_v3_1_cpu_hardening_v4`。Active 与 byte-equal Vault snapshot各 88/88 tests passed；61 Python files compile passed；registry 24/24 hashes 同时匹配 active/snapshot；JSON parse、cpu8 root/raw integrity、Git diff check、无 >20 MiB current evidence、敏感凭据模式扫描均通过。
- 测试过程生成的 active/snapshot 四个 task-owned `__pycache__` 已按精确路径移除并复核无残留；它们是可再生缓存，不是研究证据。
- A0 real SAPIEN、family GPU probes、Stage 0/1/formal collection均未运行；budget保持 `proposed_for_user_review / approved=false / frozen=false`，不存在授权 receipt，readiness继续 `BLOCKED_WITH_REASONS`。
- 用户已明确要求完成后 commit/push Vault 并提供可转发 GPT 的回复。本授权只覆盖 Vault 发布；active RoboTwin official tracked baseline仍不 commit/push。

## 69. 2026-08-29：runtime-v3_1 hardening-v4 content commit

- Hardening-v4 content commit=`2eae140ff560475bcc62cc3bda02693643b21857`，message=`docs: publish runtime-v3.1 hardening v4`，共 38 files、3,839 insertions、140 deletions。
- V4 handoff 已回填该 content SHA；本条与回填将形成 closeout commit。此时尚未声称 GitHub 发布成功，需等待 push 与 remote SHA 核对。
- GPU/A0/Stage 0 仍未授权、未运行；本次 commit/push 只发布 CPU/code 审阅材料。

## 70. 2026-08-29：runtime-v3_1 hardening-v4 审阅包首次发布成功

- Handoff closeout commit=`d298f7fcd92db3ecc79b9adf232bd28c3d7146b0`，message=`docs: record runtime-v3.1 hardening-v4 handoff`。
- `git push origin main` 成功：远端从 `7294efbfca7cb3a2d80299131af9b41b9caf27eb` 前进到 `d298f7fcd92db3ecc79b9adf232bd28c3d7146b0`；`git ls-remote origin refs/heads/main` 返回同一 SHA，本地 HEAD、`origin/main` 与 GitHub remote一致。
- 本条只记录发布回执，不修改 implementation、budget、registry、readiness、snapshot 或 evidence；将形成最后一个小 commit 并再次 push。GPU/A0/Stage 0 仍未授权。
