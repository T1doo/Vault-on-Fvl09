**明白。这次先让 Astra 修好已发现的问题，并把 F1 全部 90 条的方案、代码、预算和执行包一次准备完整；Astra 本轮不启动 GPU。等它交付后，再把整族执行任务交给 Luna。**

我刚核对了仓库，当前仍是 **`11b34925a607b92987b233cb50a4cd3fe3950852`**，现有执行包确实只覆盖两个 F1 root，授权也仍为 false。下面的任务会明确替换“只准备 18 条”的终点，但保留首批自动检查：**未来 Luna 获得全族授权后，前 18 条通过就自动继续余下 72 条，不再回来申请第二次批准。**fileciteturn708file0L2-L2 fileciteturn709file0L2-L2

下面整段可以直接复制给 Astra。

---

```text
请接着你刚刚交付的“正式入口实现与独立检查”继续工作，
将本轮设置为：

【F1全族批量生产准备：定向修复、完整90条规划与Luna执行交接】

注意：用户尚未向你下达上一轮聊天中的首波执行授权。
本条只授权CPU修复、检查、完整计划和执行包准备，
不授权启动GPU、SAPIEN物理任务或正式采集。

本次不要只准备F1首波18条。
请把F1全部10个主root、4个有序reserve、
每个有效root九条、最终90条目标数据，
作为一个连续生产任务完整准备好。

本轮由你Astra完成实现和准备。
后续由用户审阅并将完整任务交给Luna执行。
当前所有实际GPU/物理执行授权保持false。

==================================================
一、当前基线与成果保护
==================================================

已知发布基线：

11b34925a607b92987b233cb50a4cd3fe3950852

工作区：

/nfs_share/lijunhui

Vault：

/nfs_share/lijunhui/Vault-on-Fvl09

RoboTwin源码：

/nfs_share/lijunhui/Robotwin2/project/RoboTwin

启动先检查实际Git status、最新提交、日志和活动任务。
若基线后已有更新，先核对差异，避免重复修复。
不reset、不clean、不覆盖所有权不明的文件。

保留上一轮已有成果：

- 四族具体场景生成、九条矩阵和reserve框架；
- 成熟F1 primitive、adapter和已有真实成功证据；
- F2/F3/F4已通过的数据和成功profile；
- 四族独立副本迁移小样；
- 已有CPU检查、历史失败与预算记录；
- F1-red/path已经补清的历史来源。

本轮不重新研发抓取，不重新设计四族，
不重新审计全部旧历史，
不重采任何已有pilot数据。

本轮重点是：
修清五处已指出的实际接口问题，
把真正的F1全族入口接通，
交付一个后续Luna可以连续完成90条的任务包。

==================================================
二、读取顺序与当前任务入口
==================================================

先读：

1. 工作区AGENTS.md及Vault同步副本。

2. 数据构造/实现审计/formal_entry_20260910/
   EXECUTION_HANDOFF.md
   INDEPENDENT_REVIEW.md
   SCENE_AND_RESERVE_CONTRACT.md
   CALL_CHAIN_BUDGET.json
   PLANNED_SLOTS_V2.json
   RESOLVED_PRIMARY_SCENES.json

3. F1_FIRST_WAVE/manifest.json
   两份首波spec和authorization文件。

4. 真正执行链：
   execution_cli.py
   first_wave_launcher.py
   family_entry.py
   native_f1.py
   native_f1_factory.py
   native_f1_orchestrator.py
   f1_disk_verifier.py
   f1_portable_export.py
   portable_v2.py
   及这些文件实际依赖的合同、计数和验收模块。

5. 数据构造/数据构造方案.md与Idea/项目核心Idea.md中
   F1语义、正式root结构、realization、prefix、
   场景难度、split、reserve及输入字段相关章节。

不要将旧P2/P3/P4单次授权、耗尽状态或暂停点
重新作为本任务CPU实施的阻塞。

更新AGENTS的“当前任务”段：
现在是F1全族生产准备；
不是重新执行旧P4；
也不再以“只准备两root首波”为最终任务范围。

==================================================
三、先完成五项定向修复
==================================================

逐项核对当前代码；若已有后续修复，
给出对应实现和回归证据即可，不重复改写。

【修复1：正式spec、物理验收和变体验收口径一致】

当前首波spec中的non_task_position_m为0.003，
F1独立验收却读取旧PROVISIONAL阈值0.010。

必须明确：
这两个字段是否用于同一个信号、同一阶段、同一family。
如果适用范围不同，明确分开；
如果适用范围相同，实际验收统一读取冻结正式合同。

不要任意选择“更严的一项”就当解决，
也不能为了更容易通过选择更宽的一项。
从现有有效科学定义确定适用规则；
存在真正无法消解的定义冲突时集中列为一个待用户决定项，
其他无冲突工作继续完成。

正式verifier不得隐式退回旧provisional默认阈值。
将使用的阈值、单位、参考帧、阶段和信号写入验收证据。

F1释放、支撑、非目标保持等检查，
使用既定物理证据，不把gripper_command当成实际开爪，
不把body pair出现直接当成真实承重。

motion不能只凭总轨迹更长就证明规定hold已执行。
path不能只凭整体距离不同就证明规定运输段发生了变化。

从实际阶段标记和控制记录验证：
规定位置的额外hold；
规定运输段的path变化；
其他关键事件、执行臂、意图和采样率保持。

至少补以下反例：
- 非目标位移处于3mm与10mm之间；
- 整条更长但没有规定的额外hold；
- 只在无关阶段改变路径；
- 开爪命令存在但实际仍未分离。

这些用于落实既有合同，不新增模糊验收门。

【修复2：独立单条检查及时进入真实采集路径】

当前新独立finalizer主要在九条全部执行后运行，
部分共享数据问题可能到最后才暴露。

把单条已经能够判断的检查接入实际cell完成路径：
- 原始RGB/state/anchor齐备；
- capture与trace row0对应；
- 文件身份与来源正确；
- 该条任务语义、释放和稳定；
- 阶段记录；
- 数据出口能从真实磁盘加载。

单条失败后停止受影响的后续采集，
不能让缺图、字段错配等共享问题继续跑完整root。

跨分支current/anchor、三条r_pc公共前缀、
realization配对和九格完整性仍在root级检查。
不要要求首条提前证明整组通过。

动作前的capture写入和读回失败，应在动作前停止。
只在动作后才能判断的问题，完成当前安全收尾后记录失败。

测试必须经过真实cell保存和独立验收接口。
允许替换模拟器后端，
但不能把被测capture/finalizer/exporter全部mock成成功。

【修复3：真实launcher能够有界恢复与copy-only恢复】

底层resume功能必须从实际启动命令可达。
不能一方面允许恢复，另一方面让FAILED状态和非空输出目录
永久阻止合法恢复。

保留fail-stop，但增加明确恢复状态机：

失败终端与消耗已保存
→ 核对owned cleanup和预算已结算
→ 判断失败类型及剩余次数
→ 核对source/spec和已通过数据兼容性
→ 新attempt
→ 复用通过cell，仅执行缺失部分。

正常启动与恢复启动要明确区分。
不得直接删FAILED标记或清空输出目录强行重跑。
恢复不能重置预算、次数或原失败记录。

已通过数据必须验证实际文件和绑定，
不只信一个旧pass字段。
修改源代码后，不默认全部旧cell兼容，
也不默认必须全部重采；依据实际受影响合同判断。

root采集和独立验收已通过，仅seal/copy/index失败：
使用CPU-only恢复入口，
不得重新创建机器人场景或重新采九条。

通过实际launcher/family_entry路径测试：
- 部分成功后失败，再恢复缺失部分；
- 同一个恢复命令重复调用；
- 恢复遇到错误source/spec；
- 已完成root只修副本；
- 原成功raw字节保持不变。

【修复4：跨cell anchor等价与副本验收使用一致规则】

执行侧compare_anchors使用物理等价，
副本发布不能另加“九份anchor文件SHA完全相同”的要求。

区分：

单文件完整性：
源文件与副本严格hash一致。

跨cell状态等价：
按同一版本化字段白名单、单位、参考系和容差判断。

每条实际anchor原件保存，不改成统一文件。
JSON格式不同但物理内容相同不应误拒；
超出物理容差、缺状态或换root必须拒绝。

NPZ同理：
文件复制检查与数组科学等价检查分开，
不能随意互相替代。

副本中包含独立读取和关联检查需要的最小规则，
禁止原目录回退。
测试应覆盖：
同内容不同JSON格式、
允许范围内状态差异、
超过容差、
缺字段、
不同来源冒充同root、
源文件被替换。

【修复5：所有异常路径都保留lease原始证据】

取得lease后立即持久化：
job/attempt、GPU UUID、host/clock身份和起点。

child结束、自身清理、lease释放或未证实释放的状态，
在正常和异常路径均独立保存。
之后才解析usage、ledger settle和发布总状态。

post snapshot、usage解析、settle、发布异常，
不能再导致已经测得的时间丢失。

有可靠实测就按实测结算；
未知则保留真实未知状态并执行原异常规则，
不能伪造结束时间，
也不能把整个root预留默认当作CLI错误的真实消耗。

timeout、pre/post、cleanup和reservation相容。
恢复attempt只能使用全族账本剩余额度，
不能每次重新获得一份完整预算。

注入：
取得lease后写状态失败；
child退出但usage解析失败；
post snapshot失败；
settle失败；
释放未证实；
协调者中断后恢复。

只修当前启动器，不再开发另一套Guard或调度平台。

==================================================
四、一次准备F1全部90条，不只准备18条
==================================================

正式目标：

10个主root槽位；
4个有序reserve；
每个最终有效root三意图×三真实realization；
10×9=90条目标数据。

每root包括：
r_pc三条；
r_inv_path三条；
r_inv_motion三条。

reserve是替换失败主槽位的候选，
不是自动额外计入目标的36条。
最终按原planned slot和替代关系统计，
不能要求主root全通过又同时强制把reserve全采完。

旧pilot不自动计入90条。
不将F1_000001/000002之外的八组留给Luna临时设计。

为全部十个主槽位准备具体、可机读的resolved spec，
四个reserve按冻结规则确定性生成和继承。
尚须运行才能得到的current/anchor/prefix和实际测量hash
保持待生成，不填假值。

每root的计划表至少包括：

- root ID、slot ID、generator/source版本；
- split和difficulty；
- 三个主动对象的资产、可见identity、实际位置与朝向；
- 共用容器、相似干扰物和背景物；
- 相机合同；
- 对象—目标程序及候选呈现规则；
- 共同前缀、path/motion规则；
- 对应阈值、资格和独立验收；
- 九个目标cell；
- 原始运行目录与副本目录；
- 预算和失败处理。

场景变化真正进入factory、目标计算和verifier。
不能只换seed、root名称或无效备注。

保留已有F1三对象选一放入共同容器的科学语义。
对象identity位置轮换、间距与难度、干扰物和可见性
按现有正式规则落实。
不为数量突然引入未经计划的大批新资产或尺寸随机化。

保留全体已冻结的split/难度/近重复规则。
默认每族5 train、2 validation、3 test，
具体难度配额按当前有效规范。
不能造完后因失败改split或挑选较容易的标签。

首批使用预定的两个train root。
不得用validation/test结果调配方后，
又把这些已用于开发的场景声称为未见测试数据。
若修复影响测试盲态或当前规则确有冲突，
如实登记并集中报告，不暗中修饰。

==================================================
五、首18条是自动放量检查，不是任务终点
==================================================

未来Luna的一个Goal覆盖整个F1：

全族CPU预检
→ 前两个root共18条
→ 自动检查
→ 通过后直接继续余下八个root共72条
→ 整族验收和副本清点
→ F1完成。

不要在代码或交接书中保留：
“18条完成后，必须再申请其余F1授权”。

本次Astra准备完成后仍等待用户一次性批准整个F1执行包。
该批准发生以后，18条处只做机器检查，
不要求用户或GPT在线回复。

首批内部：
第一root的首个真实cell本地验收通过后，
可允许第二root按冻结规则并行。
两个首批root完整9/9、来源/副本/资源检查通过，
自动进入后续任务队列。

首批root正常不可行时，
按预注册有限重试和reserve映射处理，
不因worker完成先后改变备用分配，
不隐瞒失败分母。

首批出现共享代码/数据接口错误时，
停止受影响新派发，保留在途收尾和证据，
普通范围内修复后按源码兼容性恢复。
不因为失败就直接无条件启动余下八组。

首批通过仅说明允许在冻结规则内继续，
不声称所有未运行场景已物理可行。

全族后续并发也应具体：
最多多少GPU job、多少CPU finalizer、多少copy worker；
依显式配置和资源检查决定，
不是看到八张卡就无条件全部占用。

同一时段只进行F1物理采集。
F1结束后将结果交回用户，
不自动启动F2/F3/F4。

==================================================
六、一次性生成F1全族预算
==================================================

预算必须覆盖：

1. 全部10个主root的正常资格与九条采集；
2. 有限cohort/cell恢复；
3. 最多4个reserve的激活与实际必要运行；
4. 故障收尾及恢复开销；
5. 副本和CPU/I/O另列，不冒充GPU工作。

保留当前真实调用链的核算方法，
不要简单将旧“首波最大预算”乘五后就称为完整预算。

当前代码的无恢复每root基数是：
33 fresh、21 action、9 collection、192 solver上限。
它只能作为核对起点；
若本次入口改变调用次数，要从实际调用链重新生成。

不为了降低预算而在本轮重构成熟的资格流程。
先把真实成本列完整，
首批取得实测后再判断是否值得优化重复步骤。

必须区分：

正常完成基数；
有限恢复和reserve管理余量；
全族总cap；
单job/attempt预留；
运行timeout与清理余量；
实际消耗；
事故预算核销。

首18条属于90条，不再额外加一份。
已执行失败主槽位的成本不会因reserve替代而消失。
恢复复用已通过cell不重复采集，也不重复记账。

预算为全F1共享，不按卡、root窗口或会话重复创建。
在当前已批准总额内阶段调配可以预先允许，
扩大总额才需要用户决定。

GPU时间按真实lease定义。
普通copy若在释放GPU后执行，计CPU/I/O；
确实占用着GPU lease的时间照实计算。
不改口径制造较低成本。

存储预算核对：
原始数据、副本、staging、失败暂存、文件数量、空间及配额。
共享空闲容量不能直接当成个人配额保证。
无法读取个人quota就明确说明事实与可用的风险控制，
不虚构“配额已确认”。

最终生成：
F1全族统一预算请求；
首波、后续、reserve和恢复的派发规则；
每job的有限预留模板；
总额与分项的机器校验。

本轮所有执行授权字段保持false。
不要提前扣预算、建立GPU reservation或占卡。

==================================================
七、数据副本必须随全族采集流程工作
==================================================

用户要求：
原始运行目录不动；
在lijunhui下另外保留条理清楚、可独立迁移的数据副本。

正式副本根：

/nfs_share/lijunhui/CVPR_FutureIntent_Data/releases/formal_v1/F1/

按：
root → intent → realization
组织。

建议结构：

F1/
  FAMILY_MANIFEST.json
  dataset_index.csv
  F1_000001/
    group_manifest.json
    common/
    intent01_对象身份/
      r_pc/
      r_inv_path/
      r_inv_motion/
    intent02_对象身份/
      ...
    intent03_对象身份/
      ...
  F1_000002/
    ...

最终intent名称对应真实语义和候选绑定，
不用只有执行者才理解的retry目录作为查询入口。
原始job/attempt/路径和失败历史通过provenance映射保留。

保留每条实际current捕获及身份。
可做无损内容去重，但不能丢掉每次实际捕获证据。
inputs、supervision、audit分开。

副本必须包含读取所需的实际文件，
不能只是原路径索引、软链接或硬链接。
包内相对路径可用，
原receipt中的绝对路径只作为历史审计信息。
不修改原文件字节来改路径。

源索引预期hash、实际源hash、副本hash必须对应。
copy对象必须已经封存，避免一边写源一边复制。

每个完整root验收后同步副本：
独立staging → 逐文件验证 → root发布 → 索引登记。
整族结束再清点十个最终root、90条目标数据。

copy失败：
保留原始成功数据，只恢复copy和登记，
不重新做机器人动作。

现有真实迁移小样保留。
本轮可做少量CPU fixture或有界真实文件测试，
不全量复制旧历史、不删原数据、不重渲染高清。

至少测试：
中途复制失败；
rename前后中断；
发布完成但索引缺失；
重复恢复；
已发布源版本冲突；
原目录禁用后能从副本读取；
规则允许的anchor等价不会被文件SHA相等条件误拒。

同一NFS副本用于查询迁移和误操作恢复，
不能宣称异地或硬件故障备份。

==================================================
八、用两轮针对性检查收口，不无限扩大审查
==================================================

第一轮：
实现者修上述问题，运行实际函数之间的CPU集成测试。

第二轮：
独立检查过程读取最终冻结源和配置，
重点检查五项修复和全族自动推进，
不把第一轮的pass当成正确性前提。

有子代理能力时可分工只读复核：
验收/场景合同；
恢复/预算；
副本/全族状态机。
只有一个主协调者合并修改和发布。
没有子代理则如实分开检查，不伪称多人独立审阅。

必须覆盖：

1. 合法输入通过，错误阈值/阶段/来源被拒绝。
2. 首条缺RGB等共享问题，不继续无效采集。
3. 部分成功后恢复，原成功raw不重写。
4. FAILED不是无条件重跑，也不是永久无路可恢复。
5. root采集通过但copy失败，恢复不创建场景。
6. anchor物理等价与文件完整性规则一致。
7. 各异常路径保留已测计时和真实消耗。
8. 完整10root队列由真实launcher/state机消费。
9. 前两root通过，自动放行后八root，无需新的人工批准事件。
10. 前两root有未解决共享错误，不提前派发后八组。
11. reserve乱序完成、重启和失败不会重复激活或改split。
12. 十个有效root九条齐全，才宣布90条完成。
13. F1完成后不会自动派发F2。
14. execution_authorized=false时，
    真正入口在GPU租用和原生初始化前拒绝。

全族自动推进测试可以用明确synthetic模拟器边界，
但计划解析、调度状态机、预算、恢复、验收发布与副本接口
应运行真实代码。
CPU测试结果不能进入正式接受数据，也不能标成新场景物理成功。

不以79变成100或200项作为完成目标。
不要重新审全仓、全部历史视频、所有无关family。
发现真正共享问题时修最小范围并补受影响回归。

==================================================
九、最终交给Luna的执行包必须完整
==================================================

继续复用现有目录和模块，不需要另造平行框架。
可以新建清楚命名的F1_FULL_PRODUCTION包，
不要让Luna误用旧F1_FIRST_WAVE两root限定包。

一次交付：

1. 修复关闭说明
   五项问题：实际位置、改法、测试与剩余限制。
   不只写“已修复”。

2. F1完整生产计划
   十个主root、四个reserve；
   90格矩阵；
   实际场景参数、split、difficulty和变体；
   首18条自动放量条件；
   失败、替代、恢复与整族完成条件。

3. 机器可读全族执行包
   specs、planned slots、reserve规则；
   九条合同；
   完整source/config依赖；
   job队列、全族manifest；
   预算请求与逐job规则；
   所有实际执行授权保持false。

4. 定向实现和独立检查结果
   实际命令、环境、覆盖范围；
   fixture与真实历史样本明确区分；
   已知未验证的物理假设集中列出。

5. 数据目录与副本交接
   原始位置、独立副本位置、命名和relative mapping；
   原始数据与副本身份对应；
   迁移读取和copy-only恢复命令；
   存储空间事实及限制。

6. 一份唯一LUNA_F1_FULL_PRODUCTION_HANDOFF
   新窗口从哪里开始读；
   CPU预检命令；
   未来全族授权批准后如何启动；
   如何恢复中断；
   首18条后怎样自动继续72条；
   什么时候停止或请求用户；
   如何验证90条和副本完成。

Luna不应需要读完整聊天历史才能知道这些事项，
也不应在执行时临时设计余下八组或自己补总预算。

源码、spec、manifest和依赖hash由最终文件重新生成，
不手抄旧哈希。
授权文件正式获批时按明确流程重新封存绑定，
当前不擅自置true。

==================================================
十、本轮Astra的完成条件与未来Luna的完成条件分开
==================================================

Astra本轮完成：

- 五项具体问题已修并有针对性验证；
- 全F1十主四备方案和真实入口准备完成；
- 全族预算一次列完整；
- 18→90自动推进与恢复已做CPU验证；
- 副本和迁移规则已接入；
- 独立检查完成；
- 唯一Luna交接文件可直接执行；
- 所有实际GPU授权仍为false；
- 成果已commit/push并核对远端SHA。

当前未解决的原生相机行为、新场景物理可达、
抓取与接触、实际RSS/时间等，
明确列为未来真实首批需要检验的假设。
不能因此宣称CPU准备毫无意义，
也不能伪称这些已经被CPU证明。

本轮不要停在“新计划已写好”，
必须完成上述实际代码和执行包。
但也不要为了完成Astra的CPU Goal提前开GPU。

未来Luna在用户一次性批准完整F1范围和预算后，
才以“完成F1全90条及副本”为Goal。
首18条只是内部检查点，
不是需要回来申请余下72条的人工终点。

未来Luna结束时必须具备：
10个有效root×9唯一cell；
正确split、难度和reserve来源；
独立cell/root验收；
原始观测和模型输入字段完整；
独立副本可读、索引与hash正确；
实际预算结清、无自身残留；
阶段总结和远端发布完成。

本次不自动授权F2/F3/F4，
不启动模型训练、H-reveal、compression或高清展示。

普通CPU修复、检查、文档和必要小样测试自主推进。
阶段由单一发布者提交私有Vault/main并核验远端；
不force-push，不提交凭据或原始大数据，
不修改其他用户任务。

完成后请给用户：
一页结果摘要、唯一交接入口、全族预算、
仍需用户一次性确认的少量事项，以及最终commit。

现在开始落实，不再把工作退回泛泛的方向讨论。
```

---

**这版的分工就是：Astra 把问题修好、把 F1 全部规划和执行入口准备好；你确认完整预算后，再让 Luna 用一个 Goal 连续完成 F1 的 90 条及数据副本。前 18 条只是程序自动检查点，不再成为一次新的交接和审批循环。**

---

If you want, I can:

- 继续生成交付给Luna的执行包详情
- 详细说明五项修复的测试与验证方法
- 列出F1全族预算的具体计算步骤与核对标准