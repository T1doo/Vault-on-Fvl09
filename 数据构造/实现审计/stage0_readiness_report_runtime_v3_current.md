# Stage 0 readiness — runtime v3 current

## BLOCKED_WITH_REASONS

科学设计保持 `controlled_multi_future_f1_f4_v1_2`。当前 (H_{\mathrm{reveal}}) 仍为 `null`，没有 compression、policy transfer 或 π0.5。

Runtime-v3 已完成 CPU/static contracts：F1 三分支/prefix、F2 六候选 workspace、F3 release dynamics、F4 segmented carry、raw v2_1 和 root-level orchestrator。Active tests=45/45；cpu5 raw 与 root-cpu2 synthetic dry-runs passed。

这些不等于真实 SAPIEN evidence。当前仍缺：

1. F1 red/green/blue 3/3；
2. F2 workspace candidates；
3. F3 release-cause diagnosis；
4. F4 segmented carry；
5. real SAPIEN root adapter/integration。

Runtime-v3 budget 仍是 `proposed_for_user_review / approved=false / frozen=false`，GPU execution 未授权。明确禁止 Stage 0。
