# Workspace Boundary

- `/nfs_share/lijunhui` is the user's working directory on the current server, fvl05.
- All work, including reading, searching, creating, editing, moving, deleting, and running commands that access files, must stay within this directory.
- Do not access or modify files outside this directory.
- If completing a task would require access outside this directory, stop and ask the user for explicit permission first.
- The workspace was copied from fvl09 after that server failed. Treat paths, symlinks, environment prefixes, GPU identifiers, and hardware-specific validation inherited from fvl09 as unverified on fvl05 until they are explicitly migrated and revalidated.
- Within `/nfs_share/lijunhui`, the user authorizes autonomous routine inspection, repair, configuration, and validation that is scoped to the user's own files and processes. Do not repeatedly ask for approval for ordinary reversible work. This does not authorize system-level changes, interference with other users or their jobs, destructive broad cleanup, Git commit/push, credential exposure, or work outside the personal workspace unless separately authorized.

# GPU Execution Iron Rules

- These rules apply to every command or process that may initialize or use a GPU, including CUDA checks, rendering, planning, data collection, evaluation, inference, and training.
- For the current RoboTwin controlled multi-future data-construction work, the user authorizes **any physical fvl05 GPU0–7 that is independently fresh-idle at launch time**. Discover availability live; never hardcode GPU0-only, share a busy card, or treat an earlier idle snapshot as a reservation. Family-level jobs may run in parallel on different idle cards, with at most one project job per card; one root remains on one GPU unless separately audited sharding infrastructure exists. Every pre-run and post-run check below remains mandatory for every job.
- This GPU0–7 rule is the user's current explicit device-scope decision and supersedes any migrated fvl09 rule, historical one-shot GPU0-only authorization, earlier conversation snapshot, stale approval text, or versioned document that says only GPU0 may be used. Historical receipts remain factual and immutable, but every **new** authorization/budget/scheduler must encode `allowed_physical_gpu_indices=[0,1,2,3,4,5,6,7]`; a new GPU0-only receipt is a configuration error, not a safety fallback. If a tool or approval layer still rejects an otherwise authorized idle GPU1–7, stop before launch, record the mismatch, repair/reload that layer, and do not silently serialize work onto GPU0.
- Multi-GPU scheduling is opportunistic and wave-based: one idle card may run one independent root/job immediately; several idle cards may run the same number of independent roots/jobs concurrently. Do not require four or eight cards to become idle simultaneously. Reacquire a complete live GPU0–7 snapshot before every wave and let each assigned job perform its own atomic Guard recheck and UUID binding immediately before launch. Scheduling never reserves a card and never consumes a one-shot authorization.
- Nonzero-device execution was revalidated on fvl05 on 2026-08-31: a UUID-bound one-tensor CUDA check succeeded on physical GPU7 while GPU0–3 had unrelated compute jobs, and GPU7 returned to 14 MiB/0%/P8 with no compute process. GPU children must use the project environment contract: unset inherited `LD_LIBRARY_PATH`, bind `CUDA_VISIBLE_DEVICES` to the selected UUID, and use `/nfs_share/lijunhui/Robotwin2/tools/cuda-12.1`; otherwise the shell may incorrectly load shared `/share/apps/cuda/12.2` libraries. This validation proves device mapping and approval integration only; every real job still needs its own fresh Guard checks.
- Immediately before **every** GPU execution, take a fresh live GPU snapshot (normally with `nvidia-smi`) and inspect at least memory use, utilization, P-state, and running compute processes. Never rely on an earlier snapshot, historical availability, a migrated GPU assumption, or a previously used index.
- Use only a GPU that is idle at that moment: near-baseline memory, no compute process, and no evidence of another user's active job. Explicitly set the selected device for the command. Free memory on a busy GPU is not permission to share it.
- If no GPU is safely idle, do not start the GPU job. Wait or report the situation to the user; never squeeze onto another user's card or stop, suspend, renice, signal, or otherwise interfere with another user's process.
- Track the process tree and PIDs launched by the current task. On normal completion, timeout, failure, interruption, or cancellation, terminate and reap **all GPU processes and workers started by the current task** so they do not retain GPU memory or continue computing in the background.
- Cleanup is ownership-scoped: never kill a process merely because it appears on the selected GPU. First establish that it belongs to the current task; if ownership is uncertain, leave it untouched and report it.
- Immediately after every GPU execution, take another fresh live GPU snapshot and verify that the selected GPU has returned to its prior idle/baseline state and that none of the current task's compute processes remain. If a confirmed task-owned process remains, clean it up and recheck until release is verified; do not claim completion before this post-run check.
- Record the selected GPU and the pre-run and post-run verification in the applicable project live log or experiment record when the GPU run is part of durable work.

# Research Notes Repository

- `/nfs_share/lijunhui/Vault-on-Fvl09` is the user's long-term work notebook and research log repository.
- Use this repository for research project notes, experiment records, progress logs, decisions, ideas, and other durable working knowledge when the user asks to record or organize them.
- Treat existing notes and history as valuable records. Preserve their meaning and chronology; do not overwrite, reorganize, or delete them without the user's explicit request.
- The private GitHub remote is `https://github.com/T1doo/Vault-on-Fvl09.git`, named `origin`.
- The default branch is `main`.
- The repository-local Git identity is `旦猪 <T1doo2006@gmail.com>`.
- Creating or editing notes does not by itself authorize committing or pushing them. Only commit or push when the user explicitly requests it.
- The research idea was reset after the user's new review with their senior. Material from the former executed-action-sequence／functional-intent (A2FI) and long-action-memory direction is archived under `Vault-on-Fvl09/Idea/归档/2026-08-09_旧版A2FI与长动作理解路线/`. Preserve it as historical context only; do not cite, summarize, or use it to make current design decisions unless the user explicitly asks for the archived line of work.
- On 2026-08-27, the canonical research entry points were reset to two byte-identical imports from `T1doo/T1doo-Research` main commit `943954cd789005ba09e8f446c8038aead0bc1810`:
  - `Vault-on-Fvl09/Idea/项目核心Idea.md` is the authoritative calibrated research definition, claim boundary, current Mouse evidence summary, and next-stage F1–F4 research basis. Its upstream Git blob is `04f64a69035c2dcb95effb5b22c2b57cdbe8fe5e`.
  - `Vault-on-Fvl09/数据构造/数据构造方案.md` is the authoritative detailed F1–F4 data-construction and acceptance guide. Its frozen data protocol is `controlled_multi_future_f1_f4_v1_2`; the mechanism-training and π0.5 sections remain deferred and unapproved. Its upstream Git blob is `c51d7347519ac631c5378797ccacea2ba3f4e557`.
- The three former current Idea documents were moved unchanged to `Vault-on-Fvl09/Idea/归档 2026.8.27/`. They are historical design and survey records only and must not override `项目核心Idea.md` or `数据构造方案.md` unless the user explicitly asks to revisit them.
- All data-construction documents and the 24-video bundle that predate the new guide were moved unchanged under `Vault-on-Fvl09/数据构造/归档 2026.8.27/`. The archived append-only log and machine artifacts remain authoritative for what the Mouse work actually executed, but they do not define the new F1–F4 plan.
- The two imported canonical Markdown files remain byte-identical to upstream. Some of their source-repository wikilinks refer to materials not imported into this Vault; do not rewrite the canonical files merely to repair those links without an explicit request.
- Current one-sentence definition: at an anchor time `t`, use the current observation and robot state plus an action segment from `t` forward—`o_t + s_t + a[t:t+H]`—as training-time privileged evidence to infer the trajectory-grounded, simulator-verifiable behavioral goal or ordered task-operation program. Never revert this to `past/executed actions -> what already happened`, and never describe future action as the physical cause of intent; the real generation direction is intent/program -> trajectory, while the learned diagnostic direction is future-conditioned inference.
- “Intent” here means a verifiable structured behavioral goal, subgoal, or ordered operation program with objects, relations, sequence, constraints, and simulator predicates. It does not mean an unverifiable psychological motive, free-form explanation, simple motion direction, or a label leaked by the normal task instruction.
- Future actions are privileged inputs only for training, diagnosis, or a future-aware teacher. Normal deployment remains `current observation + current robot state + normal instruction -> action chunk`; it must not receive expert/oracle future actions, future RGB, future object poses, success predicates, planner goals, branch IDs, or other hidden simulator state.
- Keep the core quantities distinct: `H` is how much future from the real anchor is visible; `P` is the exact shared future prefix of a named `prefix_controlled` cohort; `K` is the compressed action-summary token count for fixed `H/P`; `R` is the number of successful same-intent realizations; and `L` is the current diagnostic suffix length after the branch point. `H`, `P`, `K`, `R`, `L`, policy action horizon, raw trajectory length, representation footprint, and encoded rate are not interchangeable.
- The fundamental data unit is a controlled multi-future root group: one byte-identical current context and one physically equivalent anchor snapshot branch into at least three feasible structured intents, while each intent has multiple successful execution realizations. Historical smoke/pilot groups may have 1–2 realizations, but every accepted root in the new formal F1–F4 denominator requires exactly `R=3`; infeasibility makes the root incomplete and triggers the frozen reserve rule rather than lowering R. `P` belongs to a named comparison cohort, not unconditionally to the root group; free-path `trajectory_invariance` cohorts stay separate from strict H/P curves.
- The mechanism main task is same-group candidate retrieval/ranking with a shared candidate-conditioned semantic scorer. Candidates are structured programs, not fixed local class IDs; candidate order must be randomized and permutation-tested. At every `H`, training and primary evaluation use the compatible-intent set derived from a versioned, input-observable task tree; oracle task-tree nodes are audit-only. A separate global structured-program decoding task measures compositional/generalization behavior and must not be mixed with the group-ranking headline metrics.
- Preserve the causal/claim boundary of future replacement: hold the current context fixed, replace future A with another real same-group future B, and synchronously change the target to intent B. This is a future-conditioned prediction-switch or counterfactual input-dependence test, not proof that future actions cause intent. Only score branch-specific switching at horizons where the observable compatible set is a singleton.
- The fixed research order is gated and falsifiable: **future content → task-conditioned temporal identifiability → anchor-based H/P → K/semantic compression → policy transfer**. F3/F4 must pass the preregistered Temporal Identifiability Gate before compression, π0.5 joint training, or policy rollout may begin. If an earlier Gate fails, repair data, action semantics, time alignment, labels, controls, or leakage before scaling the next stage.
- Dataset construction must be raw-first and auditable: choose the scene before intent; freeze the task/physically feasible candidate universe before primary planner rollout; separate task feasibility from planner solvability; restore or reconstruct the same full anchor for every branch; save the current context once and reference it from all branches; preserve command, target, realized state, timestamps, simulator/verifier truth, failures, software/config hashes, and derivation provenance. Do not compress at collection time.
- Split and statistics operate at full root-group or stricter scene-family level. All intents, realizations, branches, cohorts, H/K/mask/replacement views, and augmentations derived from one root group remain in the same split. Derived rows are not independent samples; use group/scene-family clustered uncertainty and keep planner-OOD on fully held-out groups.
- The completed Mouse MVP is historical pipeline/diagnostic evidence. The next frozen phase-1 mechanism dataset has four families: F1 nearby same-class object selection, F2 same-object target–relation bundles, F3 equal-endpoint low-level event-order programs, and F4 equal-final-state high-level subtask-order programs. Its formal target is 40 accepted roots and 360 raw trajectories (`4 families × 10 roots × 3 intents × R=3`), plus 16 ordered reserve planned slots; this design scale is not collection authorization.
- The archived RoboTwin survey's “30%” figure is only a historical public-scale planning reference. It does not show that ordinary released episodes form the controlled dataset and does not authorize clean 750, randomized 7,500, or any other expansion. The current phase-1 mechanism denominator is the separately frozen F1–F4 plan of 40 accepted roots and 360 formal trajectories; later expansion requires new evidence, power analysis, protocol versioning, and authorization.
- Current implementation status: the earlier Mouse lineage remains historical evidence with four accepted roots and 24 formal raw trajectories; it does not establish temporal order, H-reveal, compression, or policy transfer. The F1–F4 `controlled_multi_future_f1_f4_v1_2` scientific protocol remains frozen. Stage 0 is now authoritatively sealed by `controlled_multi_future_stage0_smoke_v1_2` as `STAGE0_COMPLETED_WITH_FAILURE_EVIDENCE`: 12 active slots, 15 retained historical terminal receipts, 5 successes, 7 failures, 5 raw trajectories and 5 required MP4s; formal accepted roots/trajectories remain zero. F1 passed 3/3. The three v1.1 F2 infrastructure slots were replaced without deletion: F2-inside ended `FAILED_EXECUTION_WITH_EVIDENCE` at the unchanged ReleaseSafetyGateV10, while F2-on and F2-beside passed with raw/video/verifier evidence and intended-layout same-current/anchor lineage. A hash-bound overlay corrected only an erroneous v1.8 raw provenance label; no raw/video bytes or physical attempt were changed or rerun. F3 retains its shared pre-V grasp/stationarity/support failure, and F4 retains its no-solvable-corridor failure. Do not reopen or rerun Stage 0. Next work is post-Stage-0 F2/F3/F4 development impact review; Stage 1, 360 formal trajectories, training, H-reveal, compression and π0.5 remain prohibited pending new approval.
- As of 2026-09-03, the versioned `V2.3.1a_PRE_SMOKE_OPERATIONAL_HOTFIX` is sealed as `PHASE0_SEALED_AWAITING_EXACT_WAVE_APPROVAL`. Runtime source is frozen at Vault commit `a9ee20c74a522120182009897ede5c0f12e6fc40`; final Phase-0 publication HEAD is `7a24721671673d82cd8a0fff24f0753543cbfe43`. The F4 planner manifest now uses 12 target-construction + 30 chain = 42 queries/program and 1008 maximum panel queries; the smoke remains a proposal capped at 152 queries/9 scenes/0 physical/0 trajectories. No operational wave approval, job authorization, real planner/GPU scene, physical attempt, or new trajectory has been created. The next single Gate is independent exact approval of `PLANNER_WIRING_SMOKE_V1`; do not run it or advance to the 1696-query panel, physical qualification, Stage 1, formal 360, training, H-reveal, compression, or π0.5 without the corresponding later authorization.
- On 2026-08-27, at the user's request, all active Mouse three-destination MVP code, tests, scripts, official Mouse smoke outputs, controlled datasets/visualizations, models/checkpoints, the `wp_g0_vision_v1` environment/wheels, and Mouse-specific temporary evidence were moved without deletion to `/nfs_share/lijunhui/Robotwin2/archive/mouse_three_destination_mvp_20260810_20260820/`. Its `README.md` records the original-to-archive path map and reproducibility boundary. The official RoboTwin tracked worktree is now clean at `c3ddfa8`; do not restore, import, or treat archived Mouse modules as the F1–F4 implementation unless the user explicitly requests a versioned restoration/reuse review. Historical absolute paths in later Mouse records should be resolved through this archive mapping; the base `Robotwin2/env`, CUDA/toolchain, assets, and non-Mouse environment smoke evidence remain active and were not reconfigured.
- `/nfs_share/lijunhui/Vault-on-Fvl09/数据构造/正式数据构造日志.md` is the active append-only handoff and progress log for the new F1–F4 implementation audit, nonformal probes, Stage 0 readiness, and any later explicitly authorized collection. The archived Mouse log is historical only. Before every F1–F4 edit, state-changing command, probe, or run, re-read the current tail of this log and the relevant `git status`; append factual chronological entries for scope, exact status, commands/inputs/outputs, finite timeout and attempt budget, successes, failures, recovery lineage, artifact paths/hashes, and the next safe stop. Keep `planned`, `implemented`, `generated`, `verified`, and `scientifically supported` distinct; never rewrite earlier entries, omit failed probes, record secrets, or use the log to imply Stage 0/formal authorization. Every GPU entry must identify the selected physical GPU index and UUID, fresh pre/post snapshot, task-owned PID/process tree, cleanup, and verified release; if no GPU0–7 card is independently fresh-idle, record the blocker and wait.
- Do not copy `Diary/` or `Environment/` content from that public repository into the vault or reuse it: it holds transient chat/setup logs and exposed credential material (an SSH private key and plaintext passwords) that the user has been advised to remove and rotate.

# F1–F4 Frozen Protocol

- Keep status words separate: `planned`, `implemented`, `generated`, `verified`, and `scientifically supported` are different states. In a scope conflict, follow the user's/senior's latest explicit correction, then `项目核心Idea.md` for the research definition, `数据构造方案.md` for F1–F4 execution, and formal artifacts plus later append-only machine records for what actually ran. Stop and report any unresolved conflict instead of changing candidates, schema, split, budgets, thresholds, or claims.
- The four family contracts are fixed. F1 selects one of three nearby same-class objects and places it in one common container while object identities and positions rotate across roots. F2 uses one main object with the three bundled programs `box+inside`, `scale+on`, and `pot_or_stand+beside`; it supports bundle grounding only, not relation/facility disentanglement or compositional generalization. F3 uses table-frame closed-loop V/H events with `V=±z_table`, `H=±x_table` and programs `VVHH`, `VHVH`, `VHHV`, sharing the first V. F4 completes common X first, then runs `ABC`, `ACB`, or `BAC` with the same object-slot mapping and equivalent final world state.
- Each formal root contains exactly three structured intents and three real successful realizations per intent: `r_pc`, `r_inv_path`, and `r_inv_motion`. `r_pc` supplies the strict shared-prefix cohort; `r_inv_path` changes approach/grasp/transport path or planner seed while keeping the executing arm, program, event order, and 250 Hz primary stream fixed; `r_inv_motion` is a separate real rollout that changes speed, event duration, order-preserving time warp, reasonable amplitude, or light pauses while also keeping the arm/program/order/250 Hz stream fixed. Derived resampling, copied trajectories plus noise, or arm switching cannot count toward the fixed `R=3`; optional `r_inv_arm` is a separate OOD diagnostic.
- A formal root is atomic and accepted only when all 9 trajectories, 9 receipts, 9 family verifiers, current/anchor equivalence, failure/recovery history, fresh-scene lifecycle, orphan audit, balance/leakage checks, and—on F3/F4—9 final-state-equivalence checks pass. Otherwise the root remains `incomplete`; partial successful trajectories never enter the formal balanced denominator.
- Root planning uses two immutable specifications. Freeze `planned_root_slot_spec` before any formal feasibility audit, including slot ID, family, split/difficulty or inheritance rule, seed, generator, origin, rank, budget, and stop condition. Generate the scene and provisional programs deterministically from it; never silently resample an easier seed/layout. Only after all three provisional programs pass task/physical feasibility may the pipeline freeze candidate universe, observable/oracle task trees, canonical prefix and their hashes into `candidate_frozen_root_spec`, after which formal planner rollout may begin. One exact current may have only one three-candidate universe; repeated candidate subsets from the same current belong to one super-root, not independent roots.
- Before any Stage 0 rollout, finish an fvl05 code/asset/verifier registry with no `TBD`: source env/class/function and hashes, asset ID/source/license, body type/mass/friction/collision/support link, new family module/interface, realized verifier signals, expected failure codes, and estimated action-step/wall-time ranges. Official task IDs are only audit leads, not proof of a valid implementation mapping. Also freeze and hash `pilot_attempt_budget_v0`; do not guess paths/assets, substitute tasks, or retry without bounds.
- Stage counts are cumulative and authorization-gated. Stage 0 requires explicit approval and collects 12 smoke trajectories: one Pilot root A per family and its three `r_pc`. Stage 1 totals 48 pilot trajectories, not 60: for each family root A becomes `r_pc+r_inv_path`, while root B becomes `r_pc+r_inv_motion`, so only 36 are added after Stage 0. Stage 2 freezes the complete data and mechanism-evaluation procedure, including 40 primary plus 16 ordered reserve planned slots, split/difficulty, candidate/task-tree/prefix hashes, verifier/control tolerances, H-view grid, source locks, `formal_attempt_budget_v1`, and model/test rules. Stage 3 may generate/seal 360 formal trajectories only after all earlier Gates and a new explicit collection authorization.
- Reserve slots are ordered and inactive by default. An inactive reserve has only its planned slot with `candidate_freeze_status=pending_activation`; it must not have fabricated objects, candidates, current data, or hashes. On a terminal primary failure, preserve all evidence and activate the next rank through an append-only receipt that inherits the failed slot's split and difficulty. Never trial several reserves and keep only the winner, overwrite the failed slot, or add more reserves after exhaustion without a versioned manifest change and approval.
- The formal split is fixed per family at `5 train / 2 validation / 3 test`, with difficulty quotas `2 clear / 6 medium / 2 crowded` and split-by-difficulty `train=1/3/1`, `validation=0/2/0`, `test=1/1/1`; totals are 20/8/12 roots and 180/72/108 trajectories. A pilot root may enter formal train only through a Stage 2-approved promotion, unchanged raw semantics, completion to all 9 trajectories, and full revalidation; pilot roots can never become validation or untouched test. Roots/super-roots and every derived view remain split-atomic. A normal training batch may contain at most one sample from any root.
- Test collection is blind. Before the normalization/representation/model/checkpoint/metric seal, automated collection/verifier/finalizer may operate on test roots, but humans must not browse their current images, raw, derived views, or MP4s except through a logged access receipt. Any root used for debugging, QA, threshold/control changes, representation/model/checkpoint selection, or repeated post-failure tuning loses untouched-test status and must be handled by the preregistered reserve/replacement rule.
- Stage 2 freezes the normalization procedure, not fabricated numerical statistics. After formal train roots exist, fit numerical normalization on train roots only and seal its artifact/hash; then train models, use validation only for representation/hyperparameter/checkpoint/threshold selection, seal the selected checkpoint and metrics, and perform a one-shot test evaluation with full input/checkpoint/normalization/metric/access receipts. Do not reopen the same test after a failure and still call it one-shot.
- The formal mechanism task uses a shared candidate-conditioned scorer whose input includes current RGB/state, future effective setpoints through H, randomly ordered candidate program semantics, and visible referring expressions. Hide the normal answer-bearing instruction, selected intent/candidate ID, branch/file/local-class IDs, simulator instance IDs, planner target/path/phase, future RGB/object pose/contact, verifier/success truth, final state, and future masks/bboxes. The compatible set is supervision/evaluation state, not model input. The separate global structured-program decoder remains a distinct compositional/generalization task and must not be mixed into headline candidate-ranking metrics.
- All H views start at the real current anchor and are reproducible from immutable raw plus versioned rules. Preserve H0–H7 event-aligned views and any Stage 2-frozen raw-step grid with steps/seconds/events, cohort-level P and prefix type, observable/oracle nodes and compatible sets, and rule versions. Use observable compatible sets for training/primary evaluation; oracle nodes are audit-only. Non-singleton horizons use set probability/calibration or a preregistered set-marginal objective; branch-specific accuracy and prediction-switch apply only at singleton-eligible horizons.
- Temporal controls are family-specific. F1/F2/F4 require a material no-vision drop as a hard grounding Gate; F3 no-vision is diagnostic only. A valid reorder that maps to another frozen candidate must preserve the fixed prefix/boundary contract and synchronously switch its target. An invalid or scene-incompatible/OOD reorder gets `none_of_frozen_candidates` or OOD diagnostics, never the original one-hot; it affects a separate preregistered rejection Gate only if a reject head/dataset/threshold exist. Do not merge valid prediction-switch and OOD calibration into one score.
- F3 valid reorder holds the shared-first-V prefix fixed, permutes only the three suffix event blocks, and must pass frozen position/velocity/gripper/jerk boundary tolerances before scoring. F3 success is verified from hidden realized EEF/bottle motion and grasp continuity, not merely commanded V/H. F4 natural-sequence dependence requires natural future to beat endpoint-only, multiset/statistics-only to fail, a matched order-aware encoder to beat an order-insensitive set encoder, real-branch program replacement to switch correctly, same-intent realizations to hold, and leakage probes to fail. Strict neutral-to-neutral block reorder is an optional stronger control only when block start/end pose, velocity, gripper, world preconditions, boundary continuity, fresh-scene replay, and full verifier all pass. Report F4 natural-sequence dependence separately from strict block-order intervention; the former must never be upgraded to the latter claim.
- Root is the independent statistical unit. Report per-root metrics, root-level paired differences, cluster bootstrap by root, median-root, worst-root, and family-stratified results; never turn 9 trajectories or derived views into independent N. The phase-1 `5/2/3` split remains mechanism evidence and does not by itself establish strong task-family generalization.
- Fix and compare action representation before calling anything compression. At fixed cohort/H/P, report K, token dimension or vocabulary, dtype/quantization, representation footprint, model/compute cost, semantic retention, and only then auxiliary reconstruction. Use matched encoded-rate language only when both methods have a reproducible bitstream or held-out code-length estimate; equal K or equal float footprint is not equal information. Mechanism-model architecture, H sampling, seeds, checkpoint rule, thresholds, compute budget, and all π0.5 mixing/loss/loader values remain `TBD` and unauthorized until their stated Gates and a separate design approval.
- Changing a family, root/intent/R count, F3/F4 program, split/super-root rule, primary action stream/frequency/dimension, task-tree evidence rule, verifier semantics/tolerance, attempt budget/stop condition, pilot promotion, or model-visible fields requires impact review, approval, and a new protocol/design version. Collector fixes, receipt additions, performance improvements, reconstructible derived views, and inspection videos may use a new implementation version only if semantics, denominator, split, and Gates remain unchanged. Preserve old artifacts immutably under their original namespace.

# Git Environment

- The NFS-safe Git configuration is `/nfs_share/lijunhui/.config/git/config`.
- The GitHub CLI configuration is `/nfs_share/lijunhui/.config/gh`.
- The workspace GitHub CLI is installed at `/nfs_share/lijunhui/.tools/gh/bin/gh`.
- As rechecked on 2026-08-20, `/nfs_share/lijunhui/activate.sh` is absent on fvl05; do not try to source that migrated workspace-level path. Current Codex shells may already export `GIT_CONFIG_GLOBAL=/nfs_share/lijunhui/.config/git/config`, `GH_CONFIG_DIR=/nfs_share/lijunhui/.config/gh`, and the workspace GitHub CLI path, but verify them before Git/GitHub work and set those exact workspace paths explicitly if they are missing.
- The `cwork` shell function changes to `/nfs_share/lijunhui`; it does not change directly into the notes repository.

# Codex and Claude Code Environment

- Codex CLI was freshly installed for the `lijunhui` user on fvl05. Its live state is `/home/lijunhui/.codex`, and `/home/lijunhui/.codex/config.toml` trusts `/nfs_share/lijunhui`.
- As configured and parse-validated on 2026-08-27 with Codex CLI `0.150.1`, `/home/lijunhui/.codex/config.toml` sets `model_context_window = 1000000` and `model_auto_compact_token_limit = 900000` for `gpt-5.6-sol`, following Tibo's published Codex configuration. These values apply to newly started sessions; restart Codex and open a new session after changing them.
- `/home/lijunhui/activate.sh` is the current personal shell activation script. It sets the workspace root to `/nfs_share/lijunhui`, configures workspace Git/GitHub CLI paths, and sets `CODEX_HOME=/home/lijunhui/.codex`.
- The user has explicitly authorized read access to `/home/lijunhui` for configuration inventory and troubleshooting. Keep credentials, tokens, SSH private keys, authentication state, and session contents private and never reproduce them in logs or notes.
- Claude Code is not installed on fvl05 as of 2026-08-04: `/home/lijunhui/.claude` and `/home/lijunhui/.local/bin/claude` are absent. The user does not currently need it installed; do not install or configure it unless explicitly requested.
- The `claude()` helper still present in `/home/lijunhui/activate.sh` is a migrated stale helper and is not evidence that Claude Code is installed.

# Migrated fvl09 Claude Code Records

- The Claude installation log and user guide under `Vault-on-Fvl09/环境配置/claudecode安装配置/` describe the former fvl09 installation. Preserve them as historical records; do not treat them as the current fvl05 installation state.
- The durable installation log is `/nfs_share/lijunhui/Vault-on-Fvl09/环境配置/claudecode安装配置/Claude Code安装配置实时日志.md`.
- The user guide is `/nfs_share/lijunhui/Vault-on-Fvl09/环境配置/claudecode安装配置/Claude Code使用说明.md`.
- Keep the Claude Code live log factual and chronological during future installation, login, proxy, update, or troubleshooting work. Do not record credentials, authorization codes, API keys, or tokens.
- Editing these notes does not authorize committing or pushing the notes repository.
- CC-Switch CLI is installed as a fixed personal workspace tool at `/nfs_share/lijunhui/.tools/cc-switch-cli/bin`.
- The installed CC-Switch CLI version is `5.7.0`, from `SaladDay/cc-switch-cli` tag `v5.7.0` / commit `8c29cfe68c6373945307e9902a60c38edbe8146d`. The reviewed Linux x64 musl archive SHA-256 is `2131c2e49896f97872bbada056f9546d291a6a3c57733689ee40e5738d2df413`.
- `/nfs_share/lijunhui/.tools/cc-switch-cli/bin/cc-switch` is a personal wrapper that applies `umask 077`, sets `CC_SWITCH_CONFIG_DIR=/nfs_share/lijunhui/.config/cc-switch-cli`, and invokes the fixed binary `cc-switch-real`.
- Keep CC-Switch state and any provider credentials only in `/nfs_share/lijunhui/.config/cc-switch-cli`; keep the directory mode at `700` and sensitive files such as `cc-switch.db` at `600`.
- Do not use CC-Switch's one-line installer or built-in updater. Future updates must repeat the fixed-release source/install-script audit and GitHub Release SHA-256 verification, then update `cc-switch-real` intentionally.
- CC-Switch can modify live Claude/Codex configuration and can start a local daemon when provider switching or proxy takeover is enabled. Before any such action, back up and hash the affected live configuration, state the exact scope, and verify restoration afterward.
- Installing CC-Switch does not authorize adding a provider, storing an API key, switching the active provider, enabling proxy takeover, or starting its daemon. Those actions require the user's provider choice and explicit in-scope request.

# Agent Collaboration

- Codex is currently the installed coding agent. Claude Code may be installed again later and agents may then run concurrently in `/nfs_share/lijunhui`. Treat every pre-existing or newly appearing working-tree change as potentially owned by the user or another process; never discard, overwrite, revert, stage, commit, or otherwise absorb it without explicit scope and verification.
- `/nfs_share/lijunhui/AGENTS.md` is the authoritative shared long-term workspace memory. `/nfs_share/lijunhui/CLAUDE.md` is Claude Code's self-contained workspace entry point and must remain consistent with `AGENTS.md`; if they conflict, follow `AGENTS.md` and the user's latest explicit instruction.
- Before working in a project that another agent may be changing, read the applicable shared instructions, inspect `git status`, and re-read the tail of the project's live log. Repeat these checks immediately before editing or running state-changing commands because another agent's state may have advanced since the task began.
- Avoid concurrent writes to the same file, environment, source tree, cache, build directory, dataset, model directory, or running experiment. If another agent is actively working in that scope, default to read-only inspection or choose a clearly disjoint task; ask the user before taking over or interrupting work whose ownership is unclear.
- Project live logs are the handoff and progress source of truth. Append factual, chronological entries; do not rewrite earlier entries. Before appending, re-read the current tail so an entry does not duplicate, contradict, or interleave incorrectly with another agent's update.
- Keep coordination state separate from verified durable facts: active commands, partial downloads, compilation in progress, temporary failures, and immediate next actions belong in the live log, while only stable cross-task rules and verified environment facts belong in `AGENTS.md`.
- No Claude Code executor is active on fvl05. Before resuming RoboTwin 2 implementation, inspect the migrated live log and current working tree, repair migration issues deliberately, and revalidate the environment on fvl05 hardware.
- Neither agent may infer authorization to commit or push from the other agent's edits or log entries. Git commit and push still require the user's explicit request.

# Continuous Workspace Memory

- Before finishing every task, proactively review whether the work produced stable, cross-task knowledge that should be added to this `AGENTS.md` so future work follows a consistent workspace memory.
- Add durable facts, user-approved boundaries, canonical paths, verified environment conventions, and recurring workflow requirements when they will materially help future tasks.
- Do not add transient command output, one-off failures, speculative conclusions, secrets, credentials, or details that belong only in a project live log.
- Preserve existing instructions and chronology. Amend or remove an existing rule only when the user explicitly requests it or when a verified replacement is necessary; record contradictions as clarifications rather than silently rewriting history.
- When an `AGENTS.md` update is warranted, make the scoped update before the final response and briefly tell the user what durable knowledge was added. If no update is warranted, no file change or routine status statement is required.
- Updating `AGENTS.md` does not authorize a Git commit or push.

# RoboTwin 2 Environment Configuration

- The active RoboTwin 2 environment configuration project is rooted at `/nfs_share/lijunhui/Robotwin2`.
- Keep all controllable RoboTwin 2 files under that single root. Use the following layout unless the user explicitly approves a change:
  - `/nfs_share/lijunhui/Robotwin2/project/RoboTwin` for the official source repository.
  - `/nfs_share/lijunhui/Robotwin2/env` for the isolated Python/Conda environment.
  - `/nfs_share/lijunhui/Robotwin2/cache` for Conda, pip, PyTorch, Hugging Face, and related caches.
  - `/nfs_share/lijunhui/Robotwin2/datasets` for generated or downloaded datasets.
  - `/nfs_share/lijunhui/Robotwin2/models` for model weights.
  - `/nfs_share/lijunhui/Robotwin2/tmp` for build and runtime temporary files.
  - `/nfs_share/lijunhui/Robotwin2/tools` for project-local environment managers and helper binaries.
- Do not create RoboTwin 2 project, environment, cache, model, dataset, or temporary directories directly at the workspace root or outside `/nfs_share/lijunhui/Robotwin2`.
- Before running an upstream installation or download script, read and audit it for `sudo`, system package installation, absolute paths, Home-directory writes, cache locations, destructive commands, and modification of packages outside the isolated environment.
- Never run `sudo`, system package managers, or system-wide `pip` for this project. If Vulkan, NVIDIA, compiler, or other system dependencies are missing, stop and report the requirement to the user.
- Do not use the shared CUDA Toolkit under `/share/apps/cuda` or `/usr/local/cuda` for RoboTwin 2 builds. Install and use the required CUDA compiler, headers, and development libraries under `/nfs_share/lijunhui/Robotwin2/tools`.
- System NVIDIA kernel drivers and device interfaces cannot be installed inside the workspace; they may only be inspected with permission and must never be modified by this project.
- Pin all controllable caches and temporary paths inside the RoboTwin 2 root before invoking Conda, pip, PyTorch, Hugging Face, compilation, or asset-download commands.
- Distinguish read-only system inspection from configuration. Reading GPU, driver, Vulkan, OS, or toolchain information outside the workspace still requires the user's explicit permission under the Workspace Boundary.
- Configure and validate the base simulator before adding policy-specific training stacks. Keep mutually incompatible policy environments separate from the base simulator environment.
- Do not treat an upstream script as trusted merely because it is official. Record the reviewed commit and any local modifications.
- The durable research plan is `/nfs_share/lijunhui/Vault-on-Fvl09/环境配置/Robotwin2环境配置/RoboTwin2环境配置研究与实施草案.md`.
- The live configuration log is `/nfs_share/lijunhui/Vault-on-Fvl09/环境配置/Robotwin2环境配置/RoboTwin2环境配置实时日志.md`.
- The complete fvl05 clean-rebuild, migration repair, validation, failure, and artifact record is `/nfs_share/lijunhui/Vault-on-Fvl09/环境配置/Robotwin2环境配置/环境重新修正检查.md`.
- The canonical day-to-day operator guide for the configured fvl05 environment is `/nfs_share/lijunhui/Vault-on-Fvl09/环境配置/Robotwin2环境配置/RoboTwin2环境操作手册.md`. Use it for activation, health checks, live GPU selection, rendering, safe creation of task configs, collection, artifact inspection, troubleshooting, and maintenance. Keep it synchronized if the activation entry point, pinned stack, source commit, data layout, or validated operating procedure changes.
- The beginner-oriented report `/nfs_share/lijunhui/Vault-on-Fvl09/环境配置/Robotwin2环境配置/2026-07-26环境配置进度报告.md` records the former fvl09 state. Use it for project background and historical results, not as proof that fvl05 is operational.
- The only RoboTwin 2 activation entry point is `/nfs_share/lijunhui/Robotwin2/config/activate_robotwin2.sh`; do not use the workspace-level `activate.sh` as the project environment activator.
- As rebuilt and revalidated on 2026-08-10, the base stack is Miniforge/Conda `26.3.2`, Mamba `2.6.0`, Python `3.10.20`, project-local CUDA Toolkit `12.1.1` / nvcc `12.1.105`, PyTorch `2.4.1+cu121`, TorchVision `0.19.1+cu121`, SAPIEN `3.0.0b1`, MPLib `0.2.1`, Open3D `0.18.0`, NumPy `1.26.4`, and SciPy `1.10.1`. Setuptools must remain `69.5.1` for SAPIEN's `pkg_resources` compatibility.
- The current base source remains the clean official RoboTwin commit `c3ddfa8b97d5519efa828b075999bd0006778e5e`. CuRobo `0.7.8` was rebuilt from commit `d64c4b005459db10c5dd867d8b30a87d5bda9bdb`; PyTorch3D `0.7.8` was rebuilt from commit `75ebeeaea0908c5527e7b1e305fbc7681382db47`. Both use the personal CUDA 12.1 Toolkit and contain RTX A6000 `sm_86` cubins. Treat an upgrade to the newer upstream/XPolicyLab code and data layout as a separate source/data migration, not an environment repair.
- The verified Miniforge `26.3.2-2` installer hardcodes `~/.conda` creation and environment registration even when the project condarc is supplied; do not rerun it blindly. The current prefix was completed from the installer's verified fixed payload with registration disabled. Mamba `2.6.0` also ignored `register_envs: false` during prefix creation, so prefer the Conda CLI for future prefixes and explicitly verify that no unwanted `~/.conda/environments.txt` was created.
- On fvl05, the host has eight NVIDIA RTX A6000 48 GiB GPUs with driver 535.274.02. Their index/UUID mapping as verified on 2026-08-04 is: 0=`GPU-2c620e6c-9639-2022-b573-9847dfa33769`, 1=`GPU-414c52ba-72c6-fc45-95d6-1e9750bbc21b`, 2=`GPU-4306d28e-0eeb-2e26-bda4-b1b44058f63e`, 3=`GPU-d5b84492-c467-0080-206f-2456cef0c338`, 4=`GPU-6a2b7387-0c6e-f68d-4f88-92e859c27da7`, 5=`GPU-9dd3c02d-192d-3536-b12e-b1be3a605be2`, 6=`GPU-8678470b-2ef8-1672-7c4c-8b55d183216d`, 7=`GPU-4c836e67-fb8e-a993-002c-cb83b10a6ead`.
- GPU visibility differs by execution context: the default Codex filesystem sandbox does not expose `/dev/nvidia*`, so `nvidia-smi` and PyTorch CUDA fail there even though the host GPUs are healthy and usable. Perform GPU validation/work only in an explicitly approved host-device context, and always check live utilization first.
- Before the clean rebuild, an approved host-device check on then-idle fvl05 GPU 2 showed that the migrated environment could create a CUDA tensor and render; retain that only as historical migration evidence. After the 2026-08-10 rebuild, the new environment independently created a CUDA tensor, loaded `CuroboPlanner` and all 50 task modules, and the unmodified official `script/test_render.py` again reported `Render Well`.
- The fvl09 rule excluding physical GPU 2 and its UUID allowlist does not apply to fvl05. The migrated allowlist was removed from the activation script; do not restore it or assume any fvl05 GPU index is bad without fresh evidence.
- RoboTwin's `collect_data.sh` directly sets `CUDA_VISIBLE_DEVICES=${gpu_id}`. After fvl05 GPUs become usable, its `gpu_id` must be selected from a fresh utilization and health check; do not reuse fvl09 index assumptions.
- At the fixed RoboTwin commit, `collect_data.sh` also silently invokes a nonexistent `script/.update_path.sh`; the six generated embodiment configurations are already correct, and the post-rebuild full smoke was run through the verified direct entry point `python script/collect_data.py <task_name> <task_config>`. If the project root moves again, explicitly run the existing `script/update_embodiment_config_path.py` from the RoboTwin repository root and independently verify every generated absolute path.
- The `beat_block_hammer` clean-demo smoke collection historically verified the complete base pipeline on fvl09 physical GPU 3. The migrated artifacts remain valuable, but this does not validate the fvl05 runtime.
- The verified smoke artifacts are under `/nfs_share/lijunhui/Robotwin2/project/RoboTwin/data/beat_block_hammer/demo_clean_smoke/` and the smoke configuration is `task_config/demo_clean_smoke.yml`. Verified schema of collected episodes at this commit: `episodeN.hdf5` holds 4-camera RGB (JPEG bytes) with intrinsics/extrinsics, `joint_action` (6+1 per arm, 14-dim vector), and `endpose` (xyz+quaternion+gripper); `instructions/episodeN.json` holds seen/unseen instruction lists; `_traj_data/episodeN.pkl` holds per-planning-call joint trajectory segments; the HDF5 contains no `step_name` or per-frame semantic labels. Despite the HDF5 group name `joint_action`, the fixed commit's `get_left_arm_jointState()` / `get_right_arm_jointState()` read `joint.get_drive_target()` for the six arm values and append a RoboTwin Python normalized gripper-command variable. The arm arrays are position drive targets, not realized qpos, delta actions, or velocity commands; gripper arrays are command state, while realized qpos and qvel are absent. Preserve planner target, controller request, drive-target readback, and realized state separately in future data. They are intentionally retained pending the user's decision; do not delete, replace, or promote the smoke configuration to the official `demo_clean.yml` without explicit instruction.
- The independent post-rebuild fvl05 smoke configuration is `task_config/demo_clean_fvl05_recheck.yml`, with artifacts under `/nfs_share/lijunhui/Robotwin2/project/RoboTwin/data/beat_block_hammer/demo_clean_fvl05_recheck/`. On an immediately rechecked idle fvl05 GPU 1, seed 1 completed planning and a one-episode full pipeline: 1,510 frames of four-camera RGB plus camera calibration, exact 14-dim joint action, dual endpose, left/right trajectory segments, HDF5, H.264 MP4, scene info, seed, and 100 seen + 100 unseen instructions. Content-level validation and artifact hashes are recorded in `环境重新修正检查.md`. Retain this isolated validation output unless the user explicitly requests deletion or replacement.
- Detailed Mouse-specific task paths, WP/attempt chronology, hashes, failures, and model-run records are intentionally kept only in the archive `README.md`, the archived append-only log, and `成果复盘.md`; do not duplicate them here or use them as current F1–F4 implementation state. During the new audit, official task names are leads only—verify the actual selected asset and realized scene from code and machine evidence.

# Current F1–F4 Handoff Override (2026-09-04)

- The current authoritative post-Stage-0 state is Vault main `3e1bab4d51ad43ceb10b4b06f49a9551a0879c12` and `STAGE1_READINESS_AFTER_F2_F3_RUNTIME_V2_1_AND_F4_CPU_V2.{md,json}`, status `NOT_READY_F1_R_INV_INCOMPLETE_F2_ROUTE_GATE_INFRASTRUCTURE_FAILED_F3_SHARED_V_PHYSICAL_FAILED_F4_ROOT_AWAITING_EXTERNAL_APPROVAL`. This section supersedes older current/next-action wording above without altering historical receipts.
- F1 has five accepted nonformal development `r_pc` roots / 15 trajectories, but no `r_inv_path`, `r_inv_motion`, Stage-1-authorized, or formal data. F2 Run2 failed before the first planner query because its 137-byte Guard-derived TMPDIR exceeded Warp NVRTC's directory-name limit; the 82-byte short-TMPDIR Run3 is only an unapproved proposal. F3 V2.1 passed all three replacement Stage-A/B qualifications and complete 58-query/10-scene accounting, but physical shared-V failed 0/4 because CuRobo's approximate collision feasibility missed SAPIEN arm self/table/pad collisions; the proposal-only pre-close Gate rejects all four sealed traces before close and passes 20/20 CPU tests, but its four candidate slots remain pending. F4 Runtime V2.1 hardening passes 18/18 finalizer, 21/21 environment, 5/5 lineage, 3/3 executable-identity, and 11/11 POST_CHILD tests; its r01 proposal remains unapproved.
- The single current review entry point is `Vault-on-Fvl09/数据构造/实现审计/GPT_REVIEW_REQUEST_F2_F3_F4_POST_RUNTIME_V2_1_COMBINED_20260904.md`. Until a new external decision is imported and separately manifested, no F2/F3/F4 GPU job is authorized. Stage 0 remains sealed; Stage 1, formal 360, training, H-reveal, compression, and pi0.5 remain prohibited.

# Current F1–F4 Handoff Clarification (2026-09-05)

- This section supersedes the older current/next-action wording without changing historical evidence. The latest review entry point is `Vault-on-Fvl09/数据构造/实现审计/GPT_HANDOFF_F4_ROOT1_RECEIPT_RECOVERY_F2_F3_20260905.md`, with unified status in `STAGE1_READINESS_AFTER_F4_ROOT1_RECEIPT_SYNC_20260905.{md,json}`. Accepted development remains five F1 roots / fifteen trajectories; Stage 1 and formal accepted remain zero and unauthorized.
- The independently approved F4 V2.2 one-root run has been consumed and must not be rerun. Its three physical branches, raw/video integrity, same-current/anchor/prefix and final-state-equivalence checks passed, but the outer finalizer rejected a single late-finalization receipt synchronization mismatch. The raw-derived first divergence is 2926 while three disk branch receipts still contain provisional 2851; canonical P remains 2851. A CPU-only, single-field, hash-bound derived-view diagnostic passes the unchanged finalizer and preserves all original bytes, but is not adopted acceptance. Preserve the original failed job terminal; next review is append-only CPU receipt resolution, not another GPU root.
- F2 Run3 completed inside planner 5/5. Its beside-only continuation is conditionally approved after CPU repair, but the latest external review's collision-centre numerical correction conflicts with the live model_data-centre source. Do not silently switch geometry sources or relax tolerance; use the documented discrepancy review, preserve inside evidence, and do not rerun the full eleven-query gate. F3 V1.1 full-window Gate and exact r3063/r0861/r1401/r2526 freeze are implemented in a new CPU-tested candidate-bound micro runtime; it still requires narrow external execution approval. No new F3 GPU run is authorized by CPU tests or candidate freezing.
- The latest external review requires F2/F3/F4 GPU jobs to be serial, despite the general hardware permission to use any independently fresh-idle physical GPU0–7. This is a current workflow constraint, not GPU0-only permission. All per-job Guard/UUID/lease/pre-post/cleanup rules remain mandatory. Stage 0, Stage 1, formal 360, training, H-reveal, compression and pi0.5 boundaries remain unchanged.

# F4 Adoption and Latest Narrow Gates (2026-09-05)

- The latest user-imported review approved and the CPU implementation completed F4 root1 append-only receipt resolution. Authoritative acceptance is `/nfs_share/lijunhui/Robotwin2/datasets/f4_root1_receipt_resolution_v1_1/acceptance.json`, receipt `5416dc9e94cf8c534eb91e7b35fd9e9661c1f7bc142f96d6fcc6bf3f1b817723`. F4 now contributes one accepted development root / three existing trajectories; global accepted development is six roots / eighteen trajectories. This supersedes the preceding pending-adoption status. Preserve original failed job/Guard receipts, canonical P2851 and raw-derived divergence2926; no new GPU run or new trajectory was used. Re-audits must not duplicate the original root registration. Do not request redundant F4 adoption approval or rerun its physical root.
- F2's live metadata geometry source has been explicitly confirmed and its old collision-centre numeric example superseded. The approved one-shot beside-only run passed live target/table binding, then failed its first carry-hub query with `MotionGenStatus.IK_FAIL`; one query/one scene, no physical/raw/root, original inside5/5 unchanged, cleanup verified. Its authorization is consumed. Next work is a narrow first-hub/route impact review, not another geometry-source debate or automatic retry.
- F3 post-lift CPU runtime V1.1 is implemented with the new predeclared minimum20mm rise, 50-frame confirmation, continuous contact/off-support and full-window transform/collision/completeness audit. Thirty-five CPU tests and preflight pass; Guard/runner, exact candidates, seeds and52/12/4 caps are unchanged. It still requires narrow external micro execution approval; no new F3 GPU run has occurred. Current handoff is `Vault-on-Fvl09/数据构造/实现审计/GPT_HANDOFF_F4_ADOPTED_F2_IK_F3_POSTLIFT_20260905.md`, with `STAGE1_READINESS_F4_ADOPTED_F2_BESIDE_IK_F3_POSTLIFT_20260905.{md,json}`. Stage0 remains sealed; Stage1/formal/training/H-reveal/compression/pi0.5 remain unauthorized. Active collector/source bindings were not modified.

# Latest Completed Micro/Transit and CPU Handoff (2026-09-05)

- The next user-imported review authorized and both one-shot jobs have now completed with clean resource release. F3 V1.1 micro used 28 queries/8 scenes/2 physical attempts: two candidates passed pregrasp but failed grasp before close; no micro/shared-V/root passed. F2 bounded transit used 6 queries/2 scenes: current-pose D0 passed, fixed U/D and all three route first goals failed IK. Both authorizations are consumed; do not regenerate manifests to retry, add candidates/routes, or reopen F4.
- Same-evidence CPU audit identifies a specific coverage gap: the active CuRobo world initializer includes table only, while F3 physics observed pad/bottle contacts. This is model discrepancy evidence, not a validated repair or proof of overall task infeasibility. Next model/target changes and GPU verification need the corresponding exact decision, not more arbitrary candidate or hub sweeps.
- Stage profiles are explicitly distinct: development r_pc root=3 trajectories, Stage1 pilot A=r_pc+r_inv_path=6, pilot B=r_pc+r_inv_motion=6, formal root=9. Do not require formal9/9 before the48-trajectory pilot. Development accepted remains6 roots/18 trajectories; the48-cell eligibility manifest has9 potential-reuse cells but0 stage-authorized acceptances. Stage1/formal/training/H-reveal/compression/pi0.5 remain unauthorized.
- Isolated CPU code is in /nfs_share/lijunhui/Robotwin2/tmp/cmf_downstream_cpu_20260905, branch codex/cmf-downstream-cpu-20260905, commit05acc6625ee5e3d32c335ecac77c5939e3f912df. It includes a two-phase publisher integrated into an isolated original-collector copy, realization operation/IO interfaces,3/6/6/9 matrices,48-cell eligibility and40+16 pending schemas;26 CPU tests pass. Specific family scene/anchor/verifier bindings remain pending, and no new realization has been collected. Active controlled source remains unchanged; do not assume this isolated code is deployed or push it to the official public remote.
- Current single handoff is Vault-on-Fvl09/数据构造/实现审计/GPT_HANDOFF_F3_MICRO_F2_TRANSIT_DOWNSTREAM_20260905.md; current machine state is STAGE1_READINESS_AFTER_F3_MICRO_F2_TRANSIT_CPU_20260905.json. Source copies and generated CPU proposals are under downstream_cpu_source_v1_20260905 and downstream_cpu_artifacts_v1_20260905 in that audit directory. Existing GPU0–7 fresh-idle/serial scheduling and complete Guard/UUID/lease/pre-post/cleanup rules remain unchanged.

# Realization Source-Lineage Handoff (2026-09-05)

- The later conditional nine-realization V1 batch was published at Vault91c11fc and executed once. Its first F1-A cell stopped at same-current reconstruction-source mismatch:1 fresh scene/0 planner queries/0 physical rollout/0 raw;8 cells unattempted. Guard/scene cleanup and GPU2 release were verified. Preserve V1 dataset/manifest/receipts; no automatic replacement, retry, or bypass through another family after this integrity global-stop.
- F1-A/B original reconstruction hashes bind controlled source9873bbe87ed44f7d54003e831ddf9015159036da8078e5cab29ccdc9fcd9fc72; F4 and active source bind3ec56ec08c39b15615538e5bde48e485d535ae10e7e1f7962254f146d32943f7. Source hash is part of simulation/reconstruction configuration. Check parent/runtime lineage on CPU before GPU creation; never rewrite old reference hashes or claim source equivalence from unchanged images alone.
- The exact245-file F1 source is retained in Vault317387b and a clean detached sparse worktree /nfs_share/lijunhui/Robotwin2/tmp/cmf_f1_parent_317387b. The namespaced parent bridge and realization_batch_runtime_v1_1 under the current Vault audit directory are CPU-implemented;15tests pass, including source binding and full mismatch evidence preservation. Active source and old data remain unchanged. V1.1 rejects old execution authorization; new recovery manifest/Guard binding and live validation remain pending.
- Current handoff: Vault-on-Fvl09/数据构造/实现审计/GPT_HANDOFF_REALIZATION_SOURCE_COMPATIBILITY_20260905.md; unified state: STAGE1_READINESS_AFTER_REALIZATION_SOURCE_MISMATCH_20260905.{json,md}. Development remains6roots/18trajectories, Stage1=0/48, formal=0/360. A proposed full nine-cell recovery would require explicit first-cell replacement plus eight unattempted cells and cumulative10scenes including the already-consumed scene. This proposal is not approval; Stage0/F4 adoption and training-related prohibitions remain unchanged.

# Nine Realizations Completed (2026-09-06)

- The user explicitly approved the bounded replacement/resume scope, and all9 requested real development variants are now verified: F1-A3r_inv_path, F1-B3r_inv_motion, F4-A3r_inv_path. They augment3 existing roots, not3 new independent roots. Accepted development totals6roots/27raw trajectories (F1:5roots/21; F4:1root/6). Stage1 remains0/48 and formal0/360; three complete6-cell pilot evidence matrices provide18 candidate reuse cells, not automatic stage acceptance. F2/F3 still have no complete accepted development roots.
- Preserve the initial source-mismatch terminal and subsequent UTF-8 receipt failure. The latter occurred after one complete real trajectory; raw/video were intact and the original verifier predicates were recomputed from saved trace with six historical regressions and negative controls. Its append-only trace-derived receipt is /nfs_share/lijunhui/Robotwin2/datasets/cmf_realization_utf8_receipt_recovery_v1/F1-red.derived_v1_1.json. No third execution of that cell occurred. The remaining8 cells completed in cmf_realization_unattempted8_v1_3; cumulative10scenes/123queries, within the approved budget. Do not rerun completed cells or request redundant acceptance/recovery of this completed scope.
- Last Guard/child1642824/1643202 on physicalGPU3 exited0; source/UUID/lease/pre-post/cleanup and independent host release check passed (15MiB/0%/P8/no task compute). Default-sandbox ps may not see host-launched processes; absence there is not proof of exit. Use host-context process checks and Guard evidence for cleanup decisions.
- All future durable JSON writers/readers must use explicit UTF-8 and exclusive atomic publication, independent of runtime locale. Reviewed helpers are realization_utf8_io_v1.py and realization_utf8_publication_v1.py in the Vault audit directory; V1.3 Guard also sets Python UTF-8 mode. Do not assume an older CPU-tested text writer remains safe after simulator/library locale changes.
- Current storage arrays contain two equal aliases of the shared38-DOF Aloha articulation. The model-visible state getter deduplicates to38qpos+38qvel=76 values; use the lossless decoding and raw-initial-state verification in REALIZATION_CURRENT_STORAGE_LAYOUT_AUDIT_V1_20260906.json, not a naive152-value concatenation. Original data stays unchanged.
- Latest single handoff is Vault-on-Fvl09/数据构造/实现审计/GPT_HANDOFF_NINE_REALIZATIONS_VERIFIED_20260906.md; current machine readiness is STAGE1_READINESS_AFTER_NINE_REALIZATIONS_20260906.json. REALIZATION_NINE_FINAL_AUDIT_V1_20260906.json and REALIZATION_NINE_COMPLETED_PUBLICATION_V1_20260906.json bind all raw/video/root/index evidence, including F4 cross-realization final-state equivalence. Next work is the existing conditional F2 endpoint-constraint and F3 gripper/world-model repair scopes after their CPU/source/Guard prerequisites; no Stage0 reopening, Stage1/formal/training/H-reveal/compression/pi0.5 expansion.

# Endpoint/Model Diagnosis Completed (2026-09-06)

- Latest user review t_6a9ce5b770bc81919311cc0f1407c53e was fully read and its narrow F2/F3 diagnosis executed. Current handoff is Vault-on-Fvl09/数据构造/实现审计/GPT_HANDOFF_F2_ENDPOINT_F3_GEOMETRY_DECISIONS_20260906.md; readiness is STAGE1_READINESS_AFTER_ENDPOINT_MODEL_DIAGNOSIS_20260906.json. F1/F4 remain6accepted development roots/27raw;18pilot candidate cells rechecked, Stage1=0/48 and formal=0/360, unauthorized. Do not rerun nine completed variants.
- F2 consumed2planner-only scenes (first configuration failure at0queries, second15IK/0trajectoryqueries),0physical/raw. C passed all three constraint profiles; U/D and all three registered yaw alternatives found no full-valid solution. Finite solver failure is not mathematical task infeasibility. Do not issue a third scene or another seed/hub/yaw sweep; the unused4route queries are conditional on a valid endpoint pair, not a new retry authorization. One inward-layout CPU proposal exists, with new root/current lineage and no GPU approval; original inside5/5 is retained, not automatically valid for the changed scene.
- F3 consumed2non-action scenes and completed40actual model checks in zero-new-scene replay after isolated cache/locked-joint repairs. Both actual model clear-state/FK checks pass, but original r3063/r1401 grasp endpoints have exact hand-table/pad intersections. Original micro execution condition is false; do not spend the unused6trajectoryqueries/2physical attempts on those endpoints. New top-down CPU geometry proposals are not IK/closed-grasp/postlift qualified and require new recipe review before GPU/physical execution. F4-B has a genuinely new layout/seed CPU proposal only, no GPU authorization.
- Actual environment meshes must use the configured solver base link's physical transform. Preserve the original legacy reported-goal tool/world/bias/Aloha correction separately; do not apply its empirical goal correction to physical world geometry. Check the real single/batch solver cache and named locked joints. CuRobo0.7.8 JointState.copy_ can return a clone for None velocity/acceleration; local f3_model_replay_v1/lock_compat.py explicitly synchronizes named locked positions. Installed library and active F1/F4 sources were not modified.

# New Conditional Scopes and CPU Readiness (2026-09-06)

- User review t_6a9d282120f881918863d7bf69035323 grants new conditional scopes, not reuse of consumed old budgets. Exact decision is Vault-on-Fvl09/数据构造/实现审计/EXTERNAL_NEW_F2_F3_F4B_DECISION_20260906.yaml; current handoff GPT_HANDOFF_NEW_SCOPES_CPU_READY_GPU_BUSY_20260906.md. F1/F4 accepted6roots/27raw and18pilot candidate cells remain unchanged; Stage1/formal/training gates remain closed.
- F3 new top-down recipes have passed sampled actual-joint closure, both inner surfaces contacting the same bottle cross-section, no forbidden support/palm intersections, and roundtrip against the original Robot planner entry. This is necessary CPU evidence only. StageA ready manifest F3_NEW_TOPDOWN_QUALIFICATION_READY_IDLE_WAVE1_20260906.json permits6IK/2non-action scenes; original prelaunch busy-card Guard launched no child and consumed0scene/query/physical. Use endpoint_idle_wave_scheduler_v1.py and f3_topdown_qualification_runtime_v1_1; do not invoke a Guard on a card already known busy. Conditional physical micro retains the new decision's2attempts/6trajectory/2physical-scene caps after actual qualification; it has not run.
- If no child or worker was ever launched, a busy card owned by another user is not a task baseline to restore. Preserve the no-launch receipt, verify task lease/cache release, and explicitly mark baseline restoration not applicable. Never claim another user's card is idle, never kill their process, and never misclassify a no-launch scheduler block as consumed physical execution. New V1.1 Guard and busy/nonzero-idle scheduler regressions encode this distinction.
- F2 new inward layout passes CPU table footprint/support, moving-pair/initial and held-arm geometry, and exclusive relation checks. New binding and carry/release state contract exist, but real IK/route integration for selective can-table support and released-can/open-gripper transition is pending (execution_ready=false). Conditional scope is3full-constraint IK+4trajectory/1planner-only scene/0physical; do not run the old attached-to-neutral route or disable the whole table.
- F4-B conditional14scene/10action-scene/136query/6raw scope has not launched. Current direct entry requires new candidate-bound planner terminals in addition to internal root planning, giving a lower bound17scenes/262queries before possible extra template/isolation costs. F4_B_ACTUAL_ENTRY_BUDGET_GAP_REVIEW_20260906.md records the gap; do not fabricate B terminals from A, drop prerequisite Gates, or exceed caps. A reviewed budget change or validated versioned integration that avoids duplicate planning is required before signing its job.

# New F3 Qualification Consumed; Micro Recovery Pending (2026-09-06)

- This supersedes the preceding F3 waiting/unexecuted wording. GPU7 became independently idle and the new StageA completed2scenes/6IK: r3063 current/pregrasp/grasp full-valid counts23/4/6; r1401 counts23/0/7. Only r3063 qualified. Do not rerun the StageA ready manifest; it is consumed. Preserve r1401's pregrasp failure and do not alter its0.12m approach or execute it physically under this result.
- The authorized r3063 micro consumed1scene/1trajectory-query/1physical-attempt slot, but reached no arm-control execution: pregrasp planning passed, then len(scene.trace) failed before _execute_control because the new direct runner omitted capture_current/anchor/initialize_trace. Save then failed for missing markers; no physical trace exists to reconstruct. This is infrastructure failure, not a physical grasp failure. GPU7 Guard2672555/child2672571 cleanup and independent host release passed.
- Isolated f3_topdown_micro_runtime_v1_1 fixes bootstrap and preserves primary/save errors separately; four direct run() CPU lifecycle regressions pass. No replacement GPU manifest/authority has been created. Latest decision forbids automatic reissue/per-recipe repeated attempts: require an explicit one-shot r3063 replacement decision, even though aggregate caps could fit one extra1scene/3trajectory/1attempt (cumulative4scenes/6IK/<=4trajectory/2attempts). Do not silently reallocate the unqualified r1401 physical slot. Current handoff is Vault-on-Fvl09/数据构造/实现审计/GPT_HANDOFF_F3_NEW_IK_AND_MICRO_BOOTSTRAP_FAILURE_20260906.md; readiness STAGE1_READINESS_AFTER_NEW_F3_QUALIFICATION_MICRO_FAILURE_20260906.json. Development remains6roots/27raw, Stage1/formal0; F2 checker integration and F4-B budget prerequisites remain pending.

# r3063 Replacement Consumed: Supported Grasp, Lift Model Blocked (2026-09-06)

- The user explicitly approved the single bootstrap replacement, which has now completed. Do not request that same approval again or reissue its manifest. Actual pregrasp/grasp and original full-window gates passed, close0.50/hold250 completed, and1404trace rows were saved. All250hold frames have both selected-finger physical contact and bottle-pad support, with no forbidden collision under the original classifier; max relative drift0.06772mm/0.00032076rad. This is supported-hold evidence, not unsupported lift success.
- Lift planning from actual postclose state failed INVALID_START_STATE_WORLD_COLLISION before lift execution. CPU literal-model audit finds all7world sphere overlaps are attached_bottle/pad__0, not robot/world intersections; worst4.858mm includes4mm buffer, and removing buffer alone leaves2overlaps. Preserve the actual native support/contact evidence. Do not call this a completed micro or physical grasp-stability qualification.
- Guard2698362/child2698383 on physicalGPU7 completed with verified task cleanup and independent host baseline release. New scope totals4scenes/6IK/4trajectory/2attempt slots, all consumed; only the replacement executed arm controls. No new root/training raw was accepted; F1/F4 remain6roots/27raw, pilot candidates18, Stage1/formal0.
- support_pair_collision_v1 contains a proposal-only pair-specific checker and pre-construction MotionGen factory;8CPU tests pass, but no GPU factory/kernel/plan/physical verification has run. It preserves full robot/table/pad checks and removes only the supported bottle/pad pair from the attached-object view, with mandatory native lift-escape screening and unchanged physical gates. A new separately authorized zero-scene model-conformance scope is proposed; do not silently reuse exhausted physical or planner budgets.
- Latest handoff is Vault-on-Fvl09/数据构造/实现审计/GPT_HANDOFF_R3063_REPLACEMENT_SUPPORTED_GRASP_20260906.md; readiness STAGE1_READINESS_R3063_SUPPORTED_GRASP_LIFT_MODEL_BLOCKED_20260906.json. F2 carry/release integration and F4-B budget review remain pending. PrimitiveCollisionCost caches collision callbacks at construction, so future world-checker fixes must bind before solver creation and verify the actual cached callbacks, not only replace an instance method afterward.

# Saved-State Support Model and Lift Plan Verified (2026-09-06)

- User approved the saved-state model validation plus at most one lift plan and asked to keep making ordinary scoped repairs/checks without repetitive questions. This does not reopen Stage0, authorize formal/training, or convert the explicitly nonphysical scope into a new physical attempt. Decision: F3_SUPPORT_MODEL_REPLAY_APPROVAL_V1_20260906.json. That model/plan scope is now completed and consumed; do not request approval for it again or rerun it.
- Real GPU2 job Guard2952209/child2952249 completed0scenes/0physical/0IK,4fixed start checks and1trajectory plan. Original full single/batch models reproduced world-collision rejection; pair-aware single/batch passed. Both actual caches14/13meshes,125attached spheres and94cached callback bindings per model verified. The lift plan succeeded and all109discrete native-geometry samples passed, planned actor rise24.99949mm. Physical lift and unsupported stability remain unverified; no new raw/root accepted.
- GPU2 UUID GPU-4306d28e-0eeb-2e26-bda4-b1b44058f63e returned14MiB/0%/P8/no compute, both task PIDs exited. Checker method-entry counts are not CUDA kernel-launch counts; kernel-launch total was not profiled and must remain unknown rather than0. Successful results override static prototype-status strings only for the actual frozen state and source used.
- f3_support_aware_physical_proposal_v1/micro.py has CPU-wired fresh hold/overlap certification and mandatory pre-execution native-escape gate, using support_pair_collision_v1/live_witness.py and escape.py. Saved real hold/plan regression and import checks pass; new physical integration has not run and has no execution manifest/authorization. F3_SUPPORT_AWARE_PHYSICAL_VALIDATION_PROPOSAL_V1_20260906.json is proposal-only. Preserve r1401's failed pregrasp and the two-candidate rule.
- Current handoff: Vault-on-Fvl09/数据构造/实现审计/GPT_HANDOFF_SUPPORT_MODEL_AND_LIFT_PLAN_PASS_20260906.md; readiness STAGE1_READINESS_SUPPORT_MODEL_LIFT_PLAN_PASS_20260906.json. SUPPORT_MODEL_PROJECTION_METADATA_OVERLAY_V1_20260906.json distinguishes historical source-capture metadata from projected actor poses/self-hashes; all fields used by the GPU model are unchanged and original artifacts remain immutable. F2/F4-B prerequisites remain pending; accepted development6roots/27raw, pilot candidate cells18, Stage1/formal0 remain unchanged.

# Continuous Pilot48 User Goal (2026-09-07)

- Latest explicit stopping instruction: “都上传完毕以后先暂停一下”. Finish the requested F1–F4 native-HD all-camera video delivery and scoped private Vault push, verify task GPU release, then pause. Do not automatically resume F2/F3 repair rollouts or new data collection after video delivery. The broader Pilot48 Goal remains incomplete; do not label the requested pause as goal completion or blockage.

- Latest user priority: complete and push the requested clear, all-camera F1–F4 display videos before further repair rollouts. F1/F4 use saved accepted states for render-only reconstruction, never re-execute their successful task trajectories. F2/F3 diagnostic reconstructions are labeled as such and do not satisfy original-camera HD delivery. Do not report video generation as running until an actual render process has started.

- Subsequent explicit video requirement: videos must be clear and include all available camera angles. Do not treat enlarged 320×240 frames as HD. Preserve existing low-resolution evidence; create separately labeled native-resolution multi-view display videos, including head, both wrists, observer and available side/static views where supported. Display-only cameras must not change model-visible current cameras or physical/data acceptance. Saved-state reconstruction must be labeled as reconstruction, not original RGB or a new successful rollout. This applies to remaking the presented demos as well as later videos; the requested F2/F3 diagnostic delivery is one video per family first.

- The user additionally requests a videos-only organized Vault directory and ongoing publication of later MP4 demos: `Vault-on-Fvl09/数据构造/演示视频/`. Keep only MP4 files beneath it, grouped by family, pilot root A/B and realization type; keep scripts/receipts/logs in the audit directory instead. Copy rather than move original videos, verify copied bytes against accepted video hashes, never overwrite differing files, and publish new accepted pilot demos with subsequent scoped Goal updates. The idempotent helper is `数据构造/实现审计/goal_pilot48_v1/sync_demo_videos.py`. This does not permit blind formal/test video access/publication or promote failed diagnostics into accepted demos.

- The user adopted https://chatgpt.com/s/t_6a9d9c54d9188191bd7bfbbf0579635c as the active persistent Goal. Full visible conversation is saved in Vault-on-Fvl09/数据构造/实现审计/goal_pilot48_v1/USER_GOAL_SOURCE.md; GOAL.md and CONTRACT.json specify authority and completion. This prospectively supersedes older per-stage/per-attempt approval stops within this Goal, not old receipts or consumed jobs. New subjobs are ISSUED_UNDER_USER_GOAL, not fabricated independent GPT approvals.
- Goal endpoint: all four families collect successfully,8pilot roots/48real input trajectories accepted, independent data/structure checks, a frozen-version continuous collection segment of at least6trajectories across2families, and formal360 production drafts. Existing18cells may be explicitly reused after audit. Do not stop because micro/CPU/one family is done. Formal360 execution, training, H-reveal/compression/pi0.5 and scientific Stage1 completion remain unapproved/unproven; pilot input acceptance is a separate state.
- Parent budget is solver problems6000, fresh scenes240, task robot-action scenes120, full collection attempts96, GPU lease86400seconds, max1GPU concurrently. Main /root owns GPU launch and total-ledger writes; agents may do assigned disjoint CPU work. Every subjob needs a finite worst-case reservation, new ID/output, actual reconciliation including failures, source locks and all existing fresh-idle GPU0–7/UUID/lease/pre-post/cleanup rules. Unknown use pauses GPU; never reset budgets across sessions. Same failure class allows at most3evidence-based preregistered revisions, not unchanged retry loops.
- F3 development confirmation is now prospectively two successful fresh scenes of the SAME frozen recipe. Cross-asset diversity remains separately reported, not a gate blocking single-template shared-V/root development. Original single-run physical gates and family semantics remain unchanged. Goal permits micro→sharedV→prefix replay→full root→pilot realizations and corresponding F2/F4-B work without returning for individual approvals.
- Checkpoints: goal_pilot48_v1/STATE.json, budget_ledger.jsonl, attempts.jsonl, pilot_cells.json. Existing18cells passed independent raw/trace/video, N/N+1,250Hz,current-row0 and legal-resolution audits and are explicitly accepted as Goal pilot inputs; no new raw or development root was created by reuse. Initial pilot input count18/48; full scientific Stage1 is not marked passed.
- Goal runtime meter counts actual batch target N, excludes nested IK inside one MotionGen problem from double counting, and survives trace resets. Legacy dummy MotionGen.warmup is explicitly skipped/logged under Goal-only initialization policy; real solvers initialize lazily. This does not change model parameters or physics thresholds and does not hide actual task solves. Model construction/setup actions are separate telemetry. All actual task actions require initialized trace. Do not run old scripts without this Goal budget/lifecycle layer.
- F4-B full preserved qualification chain needs actual target-problem reservation2266 (normal2245),26scenes/18action scenes/6collections, not the oldAPI-only646 or17/262 lower bound. Planner-assisted batches have10targets. New B CPU binding/stage/adapter modules are in goal_pilot48_v1/f4_b_runtime_v1; new F2-specific carry/release planner code in f2_inward_runtime_v1 is CPU-tested, not GPU-validated yet. No A qualification is inherited as B success.

# Pilot48 Runtime Audit Clarifications (2026-09-07)

- Later actual Goal terminals supersede the preceding static CPU-only labels only within their measured scope. F4-B source qualification p48_f4_b_stage_a_001 passed its own new scene/seed,135 actual solver problems and original source gates; this is not program/isolation/template/root success. F2 p48_f2_inward_001 verified real model/cache/locked-state wiring but failed its U/D endpoints, so release transition and route remain unexecuted. Consult current STATE.json and append-only log for later jobs, never rerun these consumed manifests.
- Goal runtime/meter.py currently has no collection-attempt hook. Before any new full collection, use a versioned integration with collection_meter_review_v1 or an equally audited hook; zero collection counters from an uninstrumented collector are invalid. Count strict-prefix branch requests before scene factory/creation, preserve failed attempts, and reconcile against branch lifecycle and raw outputs independently. Prerequisite scenes are not full collection attempts.
- Active and historical F1 DenseTraceMixin classes have different Python identities. Patching only active controlled_multi_future may miss historical cmf_parent_f1_9873 dense actions even when shared Base_Task records scene creation. Any future F1 job needs its actual namespace/instance action coverage verified, with scene-level deduplication; existing accepted F1 cells need no new rollout for this audit.
- Check critical command exit codes explicitly before dependent actions, especially source publication and GPU dispatch. A returned tool result is not proof that commit/push/preflight succeeded. Preserve already-frozen source bytes when a harmless formatting check fails; never silently launch after a failed critical step or describe a late publication as preceding launch.

# Explicit Pilot48 Git Publication Approval (2026-09-07)

- In direct reply to the request to allow ongoing commit/push for this Goal to the private Vault-on-Fvl09/main, the user explicitly replied “允许允许！！！！”. This authorizes ongoing scoped publication of this Goal's code, manifests and research logs to https://github.com/T1doo/Vault-on-Fvl09.git main. It does not authorize public RoboTwin pushes, credentials, unrelated files, force-push, history rewriting, or scientific scope expansion. The previously rejected push of45d7ff4 succeeded after this current confirmation; do not repeatedly request the same scoped Git approval.

# F2 Inside Geometry Design Exception (2026-09-07)

- In direct response to the exact native-envelope/floor-only-inside proposal, the user approved “允许允许，你把改动记录清楚就好”. Authority is recorded separately in Vault-on-Fvl09/数据构造/实现审计/goal_pilot48_v1/F2_INSIDE_NATIVE_FLOOR_DESIGN_APPROVAL_V1_20260907.md. Future F2-inside may use audited native geometry and a real floor-support boundary instead of the incompatible bottom5mm inset; original side/top5mm boundaries, physical support/release/stability/rest thresholds remain unchanged. Use new design/verifier/source versions and full positive/negative tests; old failures remain failures. Do not edit meshes/add support blocks, disable the entire box, change other families, rewrite canonical files, reset Goal budgets, or infer formal/training approval.

# F2 Contact and F3 Upright Design Approval (2026-09-07)

- In direct response to the two pending F2 inside-contact and F3 new-grasp decisions, the user replied “不用我确认，都可以改”. The scoped authority is recorded in Vault-on-Fvl09/数据构造/实现审计/goal_pilot48_v1/USER_F2_CONTACT_F3_UPRIGHT_APPROVAL_V1_20260907.md. Do not request these same approvals again. F2 may version the last-descent-only, fully native/contact-certified can–box__9 allowance while retaining original raw rows and the original failed audit; other contacts and numerical Gates remain unchanged. F3 may perform the proposed same-asset upright design's CPU selection and at most23solver/2fresh/2action/0collection/3060lease qualification-and-conditional-confirmation round within the existing Goal budget. Old stability revisions are not reset, A/B are not separate physical budgets, and failures stop the finite round. New source/spec versions and all GPU checks remain mandatory; do not edit a running job's bound source. This does not authorize Stage0 reruns, formal360 execution, training, H-reveal, compression or pi0.5, or relax unrelated scientific thresholds.
