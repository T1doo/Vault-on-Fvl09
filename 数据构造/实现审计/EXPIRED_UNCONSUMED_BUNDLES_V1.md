# Expired unconsumed bundles V1

V1.2下F1 run3、F2 run1、F3 run3、F4 run3在有效期内始终没有遇到fresh-idle GPU，现均为`EXPIRED_UNCONSUMED_DO_NOT_RUN`。

四个authorization均未消费，无Guard、ledger、cache、output或GPU child。新identity为F1/F3/F4 run4、F2 run2。
