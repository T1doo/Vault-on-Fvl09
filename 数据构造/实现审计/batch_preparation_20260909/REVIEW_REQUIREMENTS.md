**这批交接可以接受。现在适合让 Astra 接手，但任务应当切换为“批量生产准备与数据存储规划”，不是继续做高清视频，也不是重新修 F2/F3。**

我核对了最新提交 **`31a0287a976371f490baa1d0e7ca5ecd9cea3fcd`** 的交接说明、24 格来源清单、48 格引用索引、显示计划，并检查了当前场景生成接口、pilot 合同、正式数据规范和 F1 历史验收材料。读取时远端 `main` 与该提交一致。**服务器原始文件的存在性和哈希，本次依据 Luna 发布的文件级核对记录，没有把它说成我在服务器重新读取了一遍。**fileciteturn705file0L2-L2

下面先给你检查结论，再给一段可以直接复制给 Astra 的完整任务。

---

# 一、这次交接是否有问题？

## 总体没有需要推倒重来的问题，但还不等于已经具备正式批量入口

| 检查项 | 结论 |
|---|---|
| **F2/F3 当前 24 格** | 来源清单已列出 trace、初始观测、anchor、prefix、验收及场景配置；汇总记录这些文件和哈希检查通过。 |
| **统一 48 格** | 正确区分了 F1/F4 历史开发引用与 F2/F3 当前 scoped pilot，没有再冒称 48 条全部完成科学 Stage 1。 |
| **原始数据保护** | 本轮没有新物理执行，也没有改写旧 raw 或失败记录。 |
| **高清视频** | 只有计划，未执行。可以暂时搁置，不阻塞批量准备。 |
| **清晰的数据副本** | **还没有建成。**目前主要是指向原运行目录的来源索引，不是你要求的独立、可迁移副本。 |
| **正式批量能力** | **还需规划并检查实际入口。**成功的 pilot 模板不能直接等同于任意新场景的批量生成器。 |

前三项与交接和索引内容一致。`Stage1 eligible=0` 表示这些引用没有被自动升级为完整科学阶段资格，**不代表 F2/F3 的物理成功又被撤销。**fileciteturn706file0L2-L2 fileciteturn710file0L2-L2 fileciteturn709file0L2-L2

---

# 二、我发现三件应交给 Astra 处理的具体事情

## 1. F1 的缺项先查已有跨目录验收，不要直接安排重采

缺项具体是：

```text
F1-A / F1-red / r_inv_path
```

它的 trace 仍存在，但新索引引用了原恢复目录里的 `receipt.provisional.json`，并将该目录的 root receipt 记为缺失。fileciteturn715file0L2-L2

**我进一步找到了一个值得优先追查的现有来源：**

```text
数据构造/实现审计/REALIZATION_NINE_FINAL_AUDIT_V1_20260906.json
```

这份九条变体总审计中，`F1_A_path` 已有三分支汇总接受记录，且同时引用了恢复目录中的 F1-red 数据，以及另一个目录中的最终 root receipt：

```text
.../cmf_realization_unattempted8_v1_3/F1_A_path/root_receipt.json
```

因此，**“原恢复目录里没有 root receipt”，不等于“这条数据完全没有最终汇总验收”。**但还需要 Astra 按实际文件身份核对它是否确实覆盖这一条，不能只凭同名就补上。fileciteturn714file0L2-L2

正确处理是一次定向来源核对：能接上已有证据就追加引用；接不上就保持该条历史缺项。**不为它重采整批 F1，也不让它阻止其他三族的批量规划。**

## 2. 当前程序仍明显带有“小批 pilot 专用”的结构

v43 的 `scene_spec()` 只接受：

```text
F2-A-v2、F2-B-v2、F3-A-v2、F3-B-v2
```

主要变化仍是 A/B 两套固定配置。pilot 合同每根也只包含标准轨迹和一种变体，共六条。fileciteturn716file0L2-L2 fileciteturn719file0L2-L2

而当前正式规范是：

```text
每族 10 个 root
每 root 3 个意图
每个意图 3 条真实 realization
每族 90 条，四族共 360 条
```

另有每族四个有序备用场景。fileciteturn713file0L2-L2 fileciteturn720file0L2-L2

**因此，Astra 必须检查“固定 A/B、每根六条”怎样扩展到“参数化新场景、每根九条”，不能只给现有程序套一个循环。**这是下一阶段真正需要做的生产准备，不是重新否定已经成功的 pilot。

## 3. AGENTS 的当前进度仍有旧内容，需精简更新

当前 AGENTS 还保留“P3 首波尚未启动、GPU 消耗为零”等早期描述，容易让新窗口又回到旧修复入口。fileciteturn712file0L2-L2

Astra 应只更新“当前任务、当前事实入口、当前允许范围”，不要再堆入全部历史：

> **P4 数据采集已经收口；现在做批量生产准备与数据副本规划。旧 P2/P3/P4 的单次任务不重新执行。**

---

# 三、给 Astra 的任务应当是什么？

我建议把目标明确为：

> **在不重做成熟动作、不接模型、不启动高清渲染的前提下，核查四族批量入口，制定各族的场景变化与采集方案，设计按 F1→F2→F3→F4 逐族完成的执行流程，并落实可独立迁移的数据副本规范。**

**这次先交可执行的生产准备方案和必要 CPU 验证，不直接启动正式批量采集。**是否已经能稳定放量，最终要由获批后的首批真实新场景回答，不能仅凭这次代码检查宣布“批量已验证”。

下面整段可以直接放进 Astra 的新 Codex 窗口。

---

## 给 Astra 的完整任务

```text
请将以下任务作为当前“批量生产准备与数据存储规划”Goal。

本次用户要解决的是：
在已有成功模板基础上，怎样可靠、按计划、条理清晰地批量造数据；
怎样逐族完成；
怎样在lijunhui目录下另外保留一份可查询、可独立迁移的数据副本。

本次不是重新研发F2/F3抓取，不是模型训练，
不是高清回放任务，也不是重新审阅全部历史。

==================================================
一、当前基线、目标和边界
==================================================

已审阅发布基线：
31a0287a976371f490baa1d0e7ca5ecd9cea3fcd

工作区：
/nfs_share/lijunhui

Vault：
/nfs_share/lijunhui/Vault-on-Fvl09

RoboTwin实际源码：
/nfs_share/lijunhui/Robotwin2/project/RoboTwin

启动先检查最新提交、Git status、当前日志和活动任务，
保护已有修改，不reset、不clean、不覆盖未知文件。

本次交付目标：

1. 查清F1–F4各自是否具备进入首批批量验证的条件。
2. 提前制定四族完整批量方案，而不是造完一族再临时想下一族。
3. 规划F1→F2→F3→F4的逐族采集、验收、归档和副本同步流程。
4. 明确现有pilot入口到正式数据规格的最小实现差距。
5. 给出实际可落地的数据副本目录、命名、内容、复制校验和迁移方法。
6. 形成首族首批可直接执行的任务清单、命令模板和一次性预算申请。

本Goal可进行：
读取文件、实际源码检查、已有数据的定向CPU审计、
统计文件大小/空间、生成配置与任务表、
编写必要的小型CPU检查工具及测试、
非破坏性的迁移小样验证、文档和代码草案发布。

本Goal不进行：
GPU初始化、planner资格实验、SAPIEN物理执行、
新数据采集、模型训练或推理、
高清重渲染、H-reveal、compression、正式批量启动、
全量数据搬移或删除。

不把“模型效果尚未验证”当作批量准备的前置阻塞。
同时不把CPU检查通过写成“真实批量生产已验证”。

==================================================
二、读取顺序：先当前证据，再实际调用链
==================================================

先读工作区AGENTS及Vault同步副本，
辨认旧“P3尚未开始”等失效进度，
用当前交接更新简短的当前工作段，不引入新的历史长清单。

当前证据入口：

数据构造/实现审计/f2_f3_redesign_execution_v2/
  P4_HANDOFF_AND_DISPLAY_PREPARATION.md
  P4_CURRENT_DATA_SOURCE_INDEX.json
  P4_UNIFIED_48_REFERENCE_INDEX.json
  P4_SCOPED_24_FINAL_RECONCILIATION.json
  P4_CURRENT_RESEARCH_ELIGIBILITY.json

P4_DISPLAY_ONLY_PLAN.json只用于确认其为可延期的展示任务，
不得把其中24个display scenes、48个MP4或28800秒显示预算
变成本Goal前置条件或自动执行范围。

科学与数据规范：
Idea/项目核心Idea.md
数据构造/数据构造方案.md

重点读取：
family语义；
root/current/anchor；
candidate与prefix；
三种realization；
正式规模、split、difficulty、reserve；
raw字段、独立验收和来源要求。

规范中的旧执行状态和早期授权，不作为现在重跑旧任务的理由。
科学定义与当前机器事实分开处理。

实际实现：
F2/F3以runtime_source_snapshot_v43及其对应active代码为参考；
F1/F4根据有效来源清单找到真正成功的source profile和adapter。
不要假设F2/F3当前代码直接支持F1/F4，
也不要把v43当成所有历史轨迹当时使用的源码。

只追查影响批量生产、复用或迁移的直接依赖。
不要从头重新阅读所有旧失败、所有wrapper和全部高清视频。

==================================================
三、先做一份四族“生产准备度”检查
==================================================

每族分别记录：

A. 已由真实数据证明的能力；
B. 当前代码已经实现、但尚未在新场景验证的能力；
C. 批量开始前必须修改的实现缺口；
D. 可以后续扩展、不阻塞首批的事项。

不要只输出“READY=true”。
至少区分：
TEMPLATE_VERIFIED
CPU_BATCH_ENTRY_READY
FIRST_WAVE_PHYSICS_PENDING
BATCH_REPEATABILITY_VERIFIED

本次没有新物理执行，不能标最后一项通过。

重点检查真正生产入口是否可以：

- 从明确scene_spec生成新场景，而非只换seed/名称；
- 使用通用root ID，不只接受A/B四个固定字符串；
- 接受正式program×realization矩阵；
- 为每个新root生成自己的current/anchor/prefix；
- 正确保存原始观测和26-D未来动作；
- 从磁盘独立验收，而非信任runner pass；
- 一格失败后保存进度、恢复未完成格；
- 普通场景不可行按规则退出，共享代码错误停止相关批次；
- 保持实际计数、计时、资源清理与断点恢复；
- 生成数据副本并完成内容校验。

当前F2/F3 scene_spec只允许四个固定root，
pilot合同只生成六格。
明确列出需要参数化的函数、调用者和验收依赖，
不要通过字符串改名或修改一个循环次数冒充批量支持。

只提出最小必要改动。
不重写已工作的Guard、物性工厂、规划器或独立finalizer。

==================================================
四、F1历史缺项：一次定向核对，不引发整族返工
==================================================

当前统一索引的缺项是：
F1-A / F1-red / r_inv_path。

它有trace，但本目录只有receipt.provisional.json，
root_receipt被记为缺失。

优先核对：
数据构造/实现审计/
REALIZATION_NINE_FINAL_AUDIT_V1_20260906.json

其中F1_A_path有三分支汇总接受记录，
同时引用恢复目录的F1-red，以及：
.../cmf_realization_unattempted8_v1_3/F1_A_path/root_receipt.json

根据trace/raw/video身份、branch来源、parent root和最终接受记录，
判断已有跨目录汇总是否覆盖这一条。

如果能够严格关联：
追加来源映射和说明，不伪造本来不存在的旧root文件，
不改旧raw和历史验收。

如果不能关联：
保留该条为历史开发参考并标明缺口，
明确它是否影响“复用旧数据”，还是影响“复用生产模板”。

不得由一个历史回执缺项推出必须重采整个F1。
也不得为寻找该文件无限翻全部历史。
其余三族的规划可继续。

==================================================
五、四族批量变化规则必须提前写具体
==================================================

为每族交付一张参数表，包含：

参数名、单位、固定/变化；
首批取值及依据；
批量候选范围、离散集合或分布；
参数之间的联动与几何约束；
已验证范围与待验证范围；
实际由哪个scene/target/verifier函数消费；
失败如何记录及处理。

不能只有“位置随机”“物体随机”。
也不能把建议范围写成实测安全范围。

区分：
root间场景变化；
同root内不同intent；
同intent下path/motion实现变化。

root内物体、设施、观测和candidate universe保持一致。
不得为某一分支单独移动设施。
不同root不能只是ID或seed不同，实际current完全相同。

F1：
保留三近邻同类对象选一、送入共同容器的语义。
具体规划对象位置、间距、排列、可见属性与容器位置。
避免固定“红色永远在左边”或候选位置直接泄漏标签。
明确首批沿用哪些资产，哪些外观/尺寸/实例变化推迟，
以及启用它们所需的资格检查。
共同前缀不能提前选中隐藏目标。

F2：
保留同一主体的inside-box、on-scale、beside-stand组合。
分别规划主体初始位置、盒/秤/支架布局、合法目标区和运输高度。
优先沿已成功的direct geometry route与真实罐底定义扩展，
保留A/B成功的实际source和lift等参数，不静默退回默认失败路线。
reference与support分离，beside仍是桌面支撑。
尺寸变化必须联动开爪、退出和支撑几何；
不要首批就同时随机化所有设施和物体尺寸。
不宣称已经验证relation与facility解耦。

F3：
保留竖瓶抓持及已通过的V/H程序。
V/H使用既定table-frame方向；
程序仍VVHH/VHVH/VHHV，共享首个完整V、返回共同终态。
规划瓶子/pad布局、可见外观与工作中心等有限变化。
工作高度根据实际夹持总成和负V净空计算。
运动时间或path变体规则对各标签一致，
不为某个程序单独设置容易泄漏标签的速度。
初期优先使用已验证资产和物性，
不能把换瓶型当成无需验证的普通随机化。

F4：
保留共同X和ABC/ACB/BAC，
保持对象—目标槽映射与最终世界状态合同。
规划源物体、对应目标槽和共同区域的有限成组变化，
同时保持所有顺序可行、避碰和先决关系。
不要为了布局多样性改变任务语义或增加自然顺序依赖，
让某些程序实际上做不了。

每族区分首批核心变化与后续扩展变化。
不要一开始把位置、朝向、尺寸、资产、障碍和光照全部一起随机化。
但也不能只复制已有A/B两套场景就称为正式多样性完成。

==================================================
六、批量数量、正式规格与划分
==================================================

以当前canonical的40roots/360条作为规划基线：
每族10个正式root；
每root 3 intents × 3真实realizations = 9条；
每族90条；
每族4个有序reserve，共16个，不预先计入成功分母。

每root都要有：
r_pc
r_inv_path
r_inv_motion

现有pilot A=标准+path六条、B=标准+motion六条，
不能将二者拼成一个正式九条root。

检查四族执行器和finalizer是否支持同一root九条，
列出从pilot合同到正式合同的最小扩展。

默认正式生产使用新的planned roots。
旧pilot不自动充入360，不进入untouched validation/test。
需要promotion时单列影响、补齐条件和授权，不默默计数。

按既定root/super-root粒度制定split。
默认沿规范每族5/2/3 train/validation/test和对应难度分配，
若成功模板的实际可行范围无法支持，明确报告计划冲突，
不能造完后随意修改split或难度来凑数。

planned slot、seed、generator版本、候选生成和备用顺序
在相应正式资格之前固定。
未采集的current/candidate/prefix哈希保持待生成，
不能提前填虚构hash。

近似复制、同current拆候选、相关版本跨split都要防止。
查看过的开发场景不得改名成未见测试场景。
保留失败分母，不能一直重抽到成功而隐藏筛选。

不做模型实验，不因H-reveal或模型准确率未测而阻断本规划。
若旧规范把数据流程与机制实验混写，
列出一次明确的“数据生产门/科学实验门”映射，
保留科学定义，不把模型训练偷偷加成造数据前提。
真正需要改变原规则的地方，在最终计划中单列待用户决定。

==================================================
七、严格按F逐族执行的生产计划
==================================================

先规划全部四族，再实际按：
F1 → F2 → F3 → F4。

同一family内部，可按实际就绪情况和I/O能力使用独立空闲GPU并行。
不并行混跑不同family。
CPU准备其他family可以并行，但不推进其物理采集。

每族流程：

1. 使用预注册的新场景做首波完整root采集。
2. 首波必须按最终九条规格、完整观测和独立验收执行。
3. 首波成功数据可作为该族正式目标的一部分，
   前提是启动前就采用正式合同、来源和划分规则。
4. 检查真实完整root通过率、自动恢复、实际成本及数据副本。
5. 满足预先制定的放量条件后，完成该族其余planned roots。
6. 整族验收、去重、索引、资源收尾和副本校验通过，
   才进入下一族。

首波取哪些root、多少个、如何放行或停批，
由你根据实际调用链与风险提出具体值和依据，
不能写“视情况决定”然后留给执行者临时猜。

不要重新要求所有随机场景零失败。
区分：
普通规划/物理不可行 → 有限重试或按reserve规则整组替换；
共享代码/数据记录错误 → 停受影响批次并修复；
资源/来源完整性异常 → 停新派发并对账。

未完成的九条root不能降低R或删掉困难intent凑数。
已完整且独立验收通过的同合同cell可按规则恢复复用，
不用每次从头重采整组。

给出一族完成后进入下一族的机器可检查条件，
获批后不要求每个cell、每个root重新询问用户。
当前Goal只制定方案，不启动首波。

==================================================
八、独立数据副本：必须是真正可迁移的数据，不只是索引
==================================================

建议新副本根目录：
/nfs_share/lijunhui/CVPR_FutureIntent_Data/

先检查是否已存在、空间/配额/文件数限制、底层挂载和权限；
不能覆盖已有目录。
它位于lijunhui下，不放进Vault Git或RoboTwin Git。

原始运行目录保留不动。
副本是另外一份物理文件，不用指向原目录的软链接、
硬链接或快捷方式冒充独立副本。
同一底层存储的副本不宣称是异地或硬件故障备份。

至少区分：

reference/    既有历史和scoped pilot参考，带资格状态
releases/     正式批量通过、可独立使用的数据版本
_staging/     复制中或未完成包，不暴露为正式可用
manifests/    索引、校验、原始来源到副本的映射

正式目录按：
dataset版本 → F → root → intent → realization

例如：
releases/formal_v1/F2/F2_000001/
  group_manifest.json
  common/
  intent01_inside/r_pc/
  intent01_inside/r_inv_path/
  intent01_inside/r_inv_motion/
  intent02_on/...
  intent03_beside/...

每条实际current捕获仍需可独立追溯；
可以在副本内部做明确的内容去重，
不能只存一份图就删掉其他capture的实际证据。

原始文件副本保持字节不变，旧receipt中的绝对路径不原地改写。
另建portable manifest：
记录original_path、relative_copy_path、
file_hash、source ID、角色、依赖和当前资格。
新读取器依赖包内相对路径，不依赖旧服务器目录。

包内必须包括：
实际输入数据；
对应的场景/candidate/prefix/anchor；
监督与audit的正确分离；
独立验收与必要的reconciliation；
读取说明和最小读取工具。

展示视频可选，不阻塞数据包完整性。
不能拿视频替代原始RGB。
模拟器复现需要的额外资产和环境说明单列，
不复制整个conda环境、凭据或无关服务器文件。

必须回答两种迁移需求：
A. 拷走后不依赖原目录即可读取数据；
B. 需要重新仿真时，哪些资产/代码/环境还需要随包或单独提供。
不能只解决B而把A也绑定到原服务器。

==================================================
九、复制、验证、恢复与空间估算
==================================================

从当前索引和磁盘统计各family实际文件大小，
区分唯一文件、重复引用、压缩文件和解压峰值。
估计360条、primary/reserve失败暂存、双份数据、
复制临时文件和安全余量所需空间。

采集时间估算用实测记录；
保守核销不能当真实耗时。
GPU、CPU、渲染、写盘、验收、复制耗时分开说明。
没有跨场景通过率时如实标为首波待测，
不从两个成功root推导可靠成功率。

复制流程：
源文件已封存且不再写入
→ 临时副本
→ 内容hash/必要文件/读取校验
→ 副本原子发布
→ 索引登记与复制终端

正常每个完整root通过后同步，
每个family完成后清点整族。
副本失败不重跑机器人，不改变原始物理成功状态；
但该族尚不满足“归档完成”。

保持预算/采集与复制故障分离。
不得用rsync删除模式、源目录移动或大范围清理省事。

可编写小型inventory、copy/export、verify工具并用CPU测试。
只做一个大小明确、空间允许的已通过cell迁移小样，
或先用fixture验证异常恢复，再做一个真实读取验证。
不在本Goal全量复制所有历史数据或自动打包整个工作区。

迁移小样的读回测试必须显式禁用原目录回退：
只给新包路径，也能加载RGB/state/future/候选与监督；
同时验收来源可追溯。
不真的删除或重命名原始目录来制造测试条件。

最终写清：
已备份/只复制/仅引用/待复制/不可迁移的边界，
不能只写一个backup_pass=true。

==================================================
十、预算和具体执行交接
==================================================

本Goal不继承过去剩余GPU预算来执行新作业。
先做CPU准备，提出一次完整生产预算申请。

按family、首波、余下生产、reserve和恢复分别估算：
fresh、action、collection、solver、GPU lease；
另列存储、复制和CPU/I/O开销。
总量不按GPU或会话重复分配。

计数以实际调用链为依据：
setup若有动作要计；
失败求解和途中失败要计；
前缀生成、资格与数据采集是否共用须说清；
首波属于目标数量，不重复算成额外数据。

方案获批后在范围内自治推进，
不再每个helper单独审批。
当前不要把预算提案写成已授权或已消耗。

显示计划继续搁置：
不执行28800秒高清任务，
不把高清视频完成放在批量准备之前。

==================================================
十一、最终交付：精简但可执行
==================================================

至少交付以下内容，可以集中在少量文件中：

1. BATCH_READINESS_REVIEW.md
   四族已有证据、实际入口、必要缺口及优先级；
   F1-red历史来源核对；
   哪些可以进入首波、哪些仍缺CPU实现。

2. F1_F4_BATCH_PRODUCTION_PLAN.md
   四族具体参数表、资产和布局变化范围、
   正式九条规格、数量/split/difficulty/reserve、
   F1→F2→F3→F4执行顺序及首波放量条件。

3. 可机读的计划配置/slot清单
   明确generator version与pending状态；
   能发现重复root、相同current伪装、缺intent或缺realization；
   不填尚未产生的实际观测hash。

4. DATA_COPY_AND_MIGRATION_PLAN.md
   副本目录、文件内容、命名、相对路径映射、
   空间估算、复制与恢复规则；
   必要CPU工具和迁移小样的真实结果。

5. EXECUTION_HANDOFF.md
   下一个执行窗口的读取顺序、最小代码修改、
   实际命令/配置、首族预算、验收、恢复和停点；
   不依赖读完整个聊天历史才知道下一步做什么。

真正需要用户决定的范围变更集中列出，
不要用几十个待审批项把任务再次切碎。

测试要调用被审查的真实参数生成、索引与复制接口；
CPU通过不冒充实际新场景、IK或物理通过。
没有实验支持的参数明确写“拟用/首波待验证”。

普通读取、定向检查、小型工具与文档发布自主完成。
阶段由唯一发布者commit/push私有Vault/main，
核远端SHA再报告。
不提交原始大数据、凭据或无关修改。

本Goal的结束条件：
四族计划具体、必要缺口明确、
存储副本方案可迁移且有CPU验证、
首族首批清单和完整预算可审阅。

不是“正式360已开始”，也不是“所有模型实验已完成”。
不要再只交一句“方向合理，可以批量”。
```

---

## 最后强调一下本次任务的重点

**这次让 Astra 做的是“把已成功的模板变成有规则、有预算、有目录、有恢复能力的生产方案”。**

其中最重要的两项实际扩展是：

> **把固定 A/B、每根六条的 pilot 入口，扩成真正生成新场景、每根九条的正式入口；把分散在运行目录里的数据，整理成不依赖原路径的独立副本。**

计划先覆盖四族，真正执行再按你要求的 **F1 → F2 → F3 → F4**，一族采完、验完、存好，再进入下一族。**模型和高清展示都不应再挡在这条主线上。**

---

If you want, I can:

- 制定具体F2/F3批量生产入口扩展方案
- 规划四族按F1→F4逐族执行的生产计划
- 制定F1历史缺项定向核对和处理流程