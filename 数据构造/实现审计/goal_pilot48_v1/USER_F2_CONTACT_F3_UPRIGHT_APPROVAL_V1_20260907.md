# 用户确认：F2窄接触修订与F3新竖直抓持有限轮次

日期：2026-09-07。父Goal：`cmf_pilot48_20260907`，原合同和累计预算不重置。

## 直接用户依据

主线程向用户报告“F2 inside的接触规则和F3新抓持方案仍待确认”后，用户直接回复：**“不用我确认，都可以改”**。

据此，前述两项已具体说明的待决方案现获批准；在这些范围内的常规代码修复、配置、版本化有限验证无需再次逐项确认。这里不是独立GPT审批，不把自动Goal continuation当批准，也不解释为任意数值阈值、无限重试或正式训练扩张。

## F2：最后受控下降的box__9接触规则

设计ID：`F2_SUPPORTED_DESCENT_BOX9_RULE_V1`。

影响材料：`f2_controlled_inside_runtime_v2/TRANSPORT_IMPACT.md`，SHA `7f48f389ac99b1e906d119a9df8e929947f715f6536835283809d5ac84d62b6a`。

- 仅最后受控下降阶段，允许通过原生几何、准确shape身份、无穿透、原V8物理接触证据和持续抓持核验的can–box__9接触。
- 保留原始完整rows和旧未适配transport审计结果；若另建审核投影，每个被允许的pair须记录行号/身份/证据哈希，不能把原失败改写为原规则成功。
- carry/preinsert、侧壁、其他floor片、其他设施、未知/不完整接触仍按原门拒绝；不加whole-box白名单，不删除原始轨迹片段。
- 原数值阈值、50帧支撑、慢开、250settle、原最终50窗/EEF速度、原关系排他性及其他物理规则保持。此前已批准的native-floor几何边界范围保持。
- 使用新设计/实现/验证版本，原未执行V2和历史失败保持；在父Goal剩余预算内签有限新作业，不因本批准增加总预算或重跑已成功prefix。

## F3：同资产竖直上部侧夹的新有限资格

设计ID：`F3_UPRIGHT_SAME_ASSET_QUALIFICATION_V1`。

提案：`f3_next_design_impact_review_v1/PROPOSAL.md`，SHA `c947808c12e2698d5890b4c8c9aedd84a2c17367b7d500c3d0e2d578f9103b25`。

- 允许同一13号瓶改变初始朝向/actor高度和抓取朝向，先CPU审查颈部凸缘候选A；仅CPU必要时审查备选B，在GPU前唯一冻结一个设计/pose/seed。
- 原资产ID/scale/native碰撞体、质量/惯量/摩擦、夹指形状/close0.50、F3程序与table-frame V/H、250Hz及全部物理成功阈值保持。
- 新资格轮次最多23 solver、2 fresh scenes、2 action attempts、0 collections、3060 lease秒，计入父Goal余额；第一次6 solver，只有全部通过才第二次最多17 solver。第一scene站立/支撑失败即停；第二微门通过后才条件接shared V。A/B不各占一份预算，物理失败后不换备选或补第三scene。
- 旧三次稳定配方修订和全部失败保留，不能称旧上限被重置。新current/anchor/prefix/返回释放几何及观察任务树影响均需独立核验。
- 此有限作业本身不生成完整F3 root或pilot输入接受；通过后，后续工作仍按父Goal的条件、源锁和新有限清单推进，不越过未通过的Gate。

## 不变边界

GPU仍是当时独立fresh-idle的物理0–7之一、单GPU作业、Guard/UUID/lease/pre-post/owned cleanup。当前on/beside运行作业不得中途改绑定源；新修订进入独立版本。

Stage0不重开；正式360执行、训练、H-reveal、compression和π0.5不因本回复获得批准。已接受24条pilot与旧证据不重写，科学Stage1/整个Goal完成不提前宣称。
