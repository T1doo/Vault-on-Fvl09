# F3 trace-capable scene interface recovery

The two earlier upright scene attempts remain consumed infrastructure failures:
one rejected initialization actions, and the next omitted DenseTraceMixin.
Both performed zero task solver/action/collection calls. Their scenes and lease
charges remain in the parent ledger; the earlier two-scene envelope is exhausted.

This is a separately versioned, single-scene collector-interface recovery under
the persistent Pilot48 Goal's autonomous bounded-repair authority, not a reset
of that envelope or of the old grasp-stability revisions. Proposed maximum:
6 solver problems (3 IK + 3 MotionGen), 1 new scene, 1 task-action scene,
0 collections, 900-second child and 1080-second lease. This would bring the
upright lineage to at most three attempted scenes, not three successful scenes.
No confirmation, shared-V, root collection or additional repeat is issued here.

Only the scene superclass selection changes to the official trace-capable F3
registry entry. The upright actor factory, nominal B pose, seed, asset, mass,
physics, thresholds, 250 Hz trace, existing live setup-depth repair and HD camera
wrapper remain unchanged. Original files and terminal receipts stay immutable.

CPU tests execute methods from the actual source class hierarchy without
constructing SAPIEN/CUDA objects. They reject the former plain scene, retain
trace methods through the real upright factory, call the real empty-contact
reader and the real video-finalization hook, and include all prior 39 tests.
They do not prove standing, reachability, grasp stability or live HD recording.
