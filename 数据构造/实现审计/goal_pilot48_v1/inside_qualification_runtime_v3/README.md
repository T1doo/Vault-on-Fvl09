# Inside first carry waypoint revision1 qualification

This new namespace privately reuses the already audited one-fresh-scene
canonical replay/local+live meter/trace+cleanup shell. It dispatches only
`f2_inside_carry_waypoint_revision_v1.runtime`, which changes target0 to the
uniquely derived prefix-height horizontal carry and adds its fresh-model
geometric prerequisite. The remaining four goals and approved contact Gates
are unchanged. Old consumed jobs and source versions are untouched.

Pure issuer: `goal_pilot48_v1.runtime.issue_inside_carry.build_manifest`.
It binds the consumed inside001 Goal/local/Guard/query evidence and immutable
CPU review, verifies every inherited source/input hash, requires a supplied
finite reservation, and emits only a new p48_f2_inside_carry_revision1_* job.
Caps5solver/1scene/1action/0collection, child1800/lease1980 and10 high-level
state checks remain. It never reserves, writes jobs/ledgers or launches GPU.

Tests: `python -m unittest
goal_pilot48_v1.inside_qualification_runtime_v3.test_all -v`.
CPU fixtures do not establish physical inside or root success.
