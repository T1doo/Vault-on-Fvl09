# F4-B 三motion独立CPU入口

run(manifest,meter=)固定0solver/3scene/3action/3collection，runtime_v2 requires_live_meter=true。没有issuer、reserve、GPU、pilot_cells写入。原B root不存在/未accepted/无finalizer/Guard通过时严格拒绝；当前只有CPU fixtures。

source_root_{goal_terminal,guard_terminal,manifest}_{path,file_sha256}指真实B root。root_inputs先核对460/11/7/3和实际root finalizer，再从root manifest递归追溯template/isolation/program/Stage-A。父目录必须p48_f4_b_root_*，不接受F4-A；重新运行磁盘finalizer审查3raw/视频/current/anchor/verifier及root/branch完整一致。

catalog()生成仅B三候选的F4_B_motion cells，分别绑定B own source suffix/control/branch/raw/trace/video及shared current/anchor/prefix；job需冻结catalog_sha256和精确root_artifact_files表。无需对任何A输出复制/重采。

原V1.3 pipeline SHA c02e55f4f9436d7f5920b0b4eef9a8f684069b2d0c628e496d3a0d3acc016a34、retiming SHA b5b2601701bb689ba31454df04530c5854de1a379885d3ed7dc854fe97e52adb，以真正ModuleType.__dict__作函数globals的私有AST导入，不导入旧硬编码catalog。make_adapter绑定B，raw/frozen_suffix局部writer显式替换UTF8 atomic，不只是外层write_new。meter.instrument_collector_factory能覆盖实际函数globals中的factory。

只3个*_carry_mid的原1.10/250Hz单调C1 retime，不调用planner；实际variation verifier检查真实采样数与execution interval scale。原same-current/anchor/prefix、family verifier、raw/MP4/cleanup门保留。cohort finalize先执行原三branch finalizer修正divergence，再写最终branch与root；与父3r_pc合并六个完整finalstate调用原等价检查，全部通过才publication index和3accepted cells，独立root增量0，绝不直接更新pilot_cells。

有限timeout建议child3600s/lease3780s：历史同规模F4-A三条真实变体分别559.0199/564.5432/566.1617s，合计1689.7247s；约2.13倍预算。它们当时还有path规划，本次motion无新solver但1.10部分时长，3600s仍保留余量；A仅作耗时参考，绝非B资格继承。真正issuer生成时需绑定这些timing receipt hashes。

CPU测试证明私有factory hook有效、3请求计3、原motion分支只改3段controls且planner未调用、源controls不变、raw/manifest writer无局部import回落、无B root就不能执行。尚未跑GPU，也不把控制样本变换冒称真实成功trajectory。
