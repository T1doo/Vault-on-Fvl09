# Approved supported-descent contact rule V1

This independent runtime supersedes no historical result or source byte.
The explicit user approval file SHA is
`5eeb6a2998656cec08657cbcbcb2379b544a53a7e0f139509559f2f61ef6fd5e`.
Its exact prior impact document is also verified at runtime and issuance.

The allowed interval is the first new trace row produced by final controlled
descent through the last of the original 50 closed-gripper support-confirmation
frames, ending before the first slow-open command. This precise interval was
confirmed by the main thread as the approved controlled-support stage, not a
new authorization or permission to omit the support window. The boundary row
itself remains the preinsert state and is excluded. Absolute and relative
trace row numbers are saved. After the first opening request, this adapter
refuses further supported-held projection calls.

For every projected pair, the actual row must have original continuous
selected-contact/actor identity across the whole retained held window, an
original-V8 physical selected-finger contact on that row, native can/box
geometry without impermissible penetration, exact shape registry identities,
and an original-V8 physical can/box contact on box shape9. The independently
audited native-floor verifier supplies the row proof. Unknown identities,
sidewalls, other floor pieces, facilities and incomplete/nonphysical contacts
are never removed. Carry/preinsert use the original complete-contact audit.

The original rows are never mutated or truncated. Their physical decision
fields are hash-bound (inactive planner-goal NaNs are not physical evidence).
Both the original failed audit and the separately labeled projection audit
are retained. Every exception saves absolute/relative row, original pair
index/hash, both shape identities, and native row-proof hash/content.
`validate_projection` reconstructs the result from original rows and rejects
tampering even if someone recomputes the outer selfhash.

All five targets, current/anchor lineage, carry/released collision models,
numeric thresholds, original support/slow-open/settle/final windows and
relations remain from frozen v2. Private globals and a subclass change only
the approved transport auditor; no frozen module/global is modified. This
implementation alone proves neither GPU planning nor physical inside success.
