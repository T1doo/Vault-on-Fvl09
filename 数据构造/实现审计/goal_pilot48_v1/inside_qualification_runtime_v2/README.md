# Approved one-scene inside qualification v2

The v1 fresh scene/current/anchor, exact canonical replay, trace persistence,
cleanup and independent local/live V3 meter implementation is reused through
private function globals. Only the versioned specification and inside suffix
dispatch change to the new explicit approval-bound supported-contact rule.
The v1 preview and public fail-closed issuer remain unchanged historical code.

New pure issuer: `goal_pilot48_v1.runtime.issue_inside_supported.build_manifest`.
It requires an exact externally supplied main-thread reservation, validates
the original parent prefix Goal/local/Guard chain and all source/input hashes,
adds both new module trees and approval hash, and retains the same finite
5 MotionGen / 1 fresh scene / 1 action / 0 collection envelope. Ten high-level
state checks remain separately bounded. Child timeout1800/lease1980 seconds.
This function itself never reserves, writes a manifest, launches GPU or
updates any Goal ledger. Main-thread preflight/publication/Guard remain needed.

No prefix qualification is repeated; the new scene physically replays the
real saved prefix and thus counts one action scene even if its first action
fails before a new row appears. There is no restored held-state shortcut,
training raw, whole-root acceptance or automatic follow-up after a failed Gate.

CPU test entry: `python -m unittest
goal_pilot48_v1.inside_qualification_runtime_v2.test_all -v`.
The contact fixtures use real native geometry with explicitly synthetic
contact signals; they are not newly generated trajectories or physical success.
