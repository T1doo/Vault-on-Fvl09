# Luna执行交接：全新F2/F3任务，持续到完成

版本：`LUNA_F2_F3_HANDOFF_V2_1`；完成日期2026-09-08，文件名沿用设计日期。用户已确认目标为F2/F3，F3/F4为笔误。当前交接准备完成，不代表运行层或数据已经完成。

## 1. 先明确目标与授权来源

你接手的是全新任务`f2_f3_redesign_20260907`：依V2.1完成F2/F3最小真实闭环、同冻结方案fresh确认、三分支采集，以及F2-A/B与F3-A/B四个pilot roots共24条新输入；与已有F1/F4各12条通过只读引用组成48格，完成结构/时间/事件/负例/严格模型出口检查。

用户已要求按GPT此次审查意见修改并交Luna执行。执行依据是本次用户指令及其采纳的审查，不是旧Goal。先完成七项必要实现，再在同一任务内自主推进，不能在CPU提案、单个helper、一次micro或单族完成后就当总任务完成。预算和明确停止条件仍须遵守；“直到完成”不授权超额或降低成功标准。

不读取旧Goal以恢复它；旧`cmf_pilot48_20260907`合同/STATE/ledger、修订计数/余额/审批/暂停仅历史。已有F1/F4数据不重采、不重采纳、不重复登记；旧失败不覆盖。不要加回连续跨族6条或formal草案，formal360/训练/H-reveal/压缩/π0.5不执行。

## 2. 文件读取顺序

以`/nfs_share/lijunhui`为工作区，下列Vault文件均在`Vault-on-Fvl09/`内。

| 顺序 | 文件 | 读完应明确什么 |
| --- | --- | --- |
| 1 | `/nfs_share/lijunhui/AGENTS.md`；Vault根同字节副本 | 当前权限、GPU多卡/清理、阶段自动push和独立任务边界 |
| 2 | [F2F3重设计与构造日志.md](F2F3重设计与构造日志.md)，先读当前入口和最新尾部 | 实际进度、是否已有在途job；旧条目已撤回方案不恢复 |
| 3 | [主计划](F2F3重设计审查与后续执行计划_20260907.md)，版本应为V2_1 | 工作包、24格、语义与总验收 |
| 4 | [V2.1执行附录](F2F3重设计V2.1执行附录_20260907.md)，全文 | R1–R7必须实现的具体合同 |
| 5 | [GPT审查原文](实现审计/f2_f3_redesign_review_20260907_v2_1/GPT_REVIEW_SOURCE.md)，全文 | 解释与裁决；已采纳七项不再次整体外审 |
| 6 | [数据构造方案.md](数据构造方案.md)：D3、D5、D7、D8、D10、D12、D15、D17相应章节；[核心Idea](../Idea/项目核心Idea.md)定义/边界部分 | same-current、prefix、realization、事件/输入/统计约束，区别pilot输入和科学Gate |
| 7 | [旧要求排查表](实现审计/f2_f3_redesign_review_20260907_v1/LEGACY_REQUIREMENTS_AUDIT.md)与主计划§3引用的关键失败证据 | 哪些旧假设不应复用；不全读几百条旧日志 |
| 8 | `环境配置/Robotwin2环境配置/RoboTwin2环境操作手册.md`，按任务选段 | 实际env/source入口；数字选卡示例不替代当前UUID Guard |

规则冲突以用户最新指令、当前AGENTS、V2.1为准；canonical保留科学定义，历史文件不能覆盖新任务。不要以原分享正文“串行/旧余额”或早期V2“待再次审查”阻断已交接实施。

## 3. 当前有哪些实物，还没有哪些

已存在：F1/F4接受数据与来源、旧F2/F3失败trace/分析、源代码/基础环境、V2.1三份执行文档和原文、当前简化AGENTS。

尚未由本轮创建：新TASK/CONTRACT/STATE、预算事件账本、运行模块、verification_profile、新资产物性readback、多卡实际集成、F2/F3新闭环、新24条数据。路径中出现“建议/拟”不等于文件已经存在或通过；先核实际文件和Git status，不从名称推断已完成。

建议独立控制面：`Vault-on-Fvl09/数据构造/实现审计/f2_f3_redesign_execution_v1/`；建议实现：`Robotwin2/project/RoboTwin/controlled_multi_future/redesign_f2_f3_v1/`；输出：`Robotwin2/datasets/f2_f3_redesign_v1/<job_id>/`；短TMP及私有cache均在Robotwin2下。若同名已存在，先读它的来源/进度，禁止覆盖。

## 4. 里程碑A：建立可信资产和最小真实入口

1. 查新日志、两个实际Git根的status、是否有新任务live进程/已完成结果；只读核F1/F4来源，建立历史数据引用清单，不导入旧Goal运行字段。
2. 新建明确任务目标和独立合同/状态/账本，写本次共同cap和阶段分配、定义purpose、计量规则、failure class与有限修订。多作业状态采用`running_by_job_id`一类映射；只有CPU协调者写总账和Git。
3. 实现最小GPU Guard/UUID锁、事件预留/dispatch/私有终端/幂等reconcile。验证锁两进程可见、重启恢复、超额事实落账、未知保留预留、单job终态不清其他job。协调者不初始化CUDA后fork；child独立Python/env，UUID可见后逻辑cuda:0。
4. 新工厂统一effective_scale/语义点/mass/COM/完整惯量张量及实际材料，避免旧Actor包装再覆盖。依R1逐阶段readback，冻结一张verification_profile及scene/asset/控制参数版本。
5. 实际run顺序必须初始化current/anchor/trace/markers/meter后再task action，finally完整保存原错误及owned cleanup。CPU异常测试不代替首个真实入口；不为未来全部collector组件推迟第一次有界物性/闭环验证。

A验收：新控制面与实际入口具备最小可运行条件；真实factory属性证据与目标几何可审；一个或两个独立ready小job可按fresh闲卡立即调度。没有物理结果的字段标未验证。每个GPU动作即便仅构造/渲染也走Guard和新预算。

## 5. 里程碑B：两个最小真实闭环及fresh确认

F2顺序：抓起直立罐→宽口盒上方→受限下降→真实底板支撑→净开度单调增加→分离→空手退出→稳定终态。目标由实际T_EEF_can推导；规划预声明floor接触许可，不要求未来接触已发生；真实支撑另验。容器覆盖闭手、开手、退出完整扫掠，不回旧179.8°目标/box__9。

F3顺序：自然站稳→直壁对称侧夹→微抬离台→全确认窗→足够工作高度→完整V→完整H→放回支撑→慢开分离→退出/rest。五阶段模型切换依R3，支撑例外不进入自由V/H；检查负V净空、正V及左右H全臂扫掠、最不利姿态包络。

每族第一次完整通过后，以同一冻结方案第二fresh完整执行确认；可复用后续预先声明、证据等价的完整资格运行，不为“确认”名字再浪费一次。参数改变后不能将两不同配方凑成两次成功。

W2默认qualification trace/诊断视频/verifier，collection0，不调用生产collection；要兼作生产分支必须启动前声明完整root/collection用途并计数，不能看成功后晋升。视频实时一清楚外部视角即可，保持原数据相机规范。

任一族闭环/确认通过即可进本族C，不等另一族；独立族可在不同idle卡并行。真实失败按最早阶段诊断、新版本有限修复；已声明首选/备选切换属于整体范围，不再逐次问批准。

## 6. 里程碑C：三分支、四个pilot roots、最终48格

每root先planned scene→三候选任务/物理可行→冻结candidate/source/current/anchor/目标规则→公共prefix资格与冻结→三个r_pc重放。prefix后允许实际状态在线规划，完整计数与保存；不带回后缀零规划要求。

| root | 三个intent与realization | 验收数 |
| --- | --- | ---: |
| F2-A | inside/on/beside，各r_pc+r_inv_path | 6 |
| F2-B | inside/on/beside，各r_pc+r_inv_motion | 6 |
| F3-A | VVHH/VHVH/VHHV，各r_pc+r_inv_path | 6 |
| F3-B | VVHH/VHVH/VHHV，各r_pc+r_inv_motion | 6 |

A/B场景事先指定，不同root不能同current重命名。root内各branch可独立fresh scene，只要source/current/anchor等价和程序一致；一root一卡，不拼失败轨迹片段，不复用旧布局on/beside当新资格。

三条r_pc是开发三分支通过，六条才是该pilot root完整；9条规则属于正式数据，不将本轮扩成9条。path/motion是真实新rollout，程序/事件顺序/arm/250Hz不变。

branch raw/trace/video/receipt→family verifier→same-current/prefix/final-state/root完整性→新pilot index顺序发布。保存26-D/250Hz、N/N+1、target/request/effective/realized分流；F3事件按隐藏realized motion/contact判，不按命令名通过。

新增24条验收后，以只读引用连接旧F1/F4各12条，建立新48格索引。旧24条只核来源/兼容性，不重新接受或增加计数。完成结构、时间、事件、反例、兼容集与严格类型化出口检查；拒绝允许顶层字段内嵌path/ID/phase/verifier等。监督与audit分离，不开发训练系统。

## 7. 新共同预算及运行纪律

共同cap：`solver_problems=2980; fresh_scenes=125; action_scenes=74; collection_attempts=48; gpu_lease_seconds=61200`。每卡共用总额，GPU时间相加。阶段份额可有记录地调整并保留后续必要额度，不逐次求批；总额不增加。预算是管理上限，不保证能完成。

每job先审实际call graph并预留有限solver/scene/action/collection/timeout/lease；W1自动回位动作也计action。普通初始化/路径/序列化bug不扣物理设计修订次数，但实际消耗照计。已声明两模板及每模板同类至多3次有证据物理修订有限执行，不换failure名刷新。

actual>reservation必须记真实数和BUDGET_OVERRUN，停止新派发，不截断为cap、不拒绝对账；unknown保留预留先查。不要修改旧账本或从旧Goal复制余额。

GPU0–7独立fresh-idle即用，wave前及job前检查；一root一卡、一卡一项目job，不等八卡。项目UUID锁不约束外部用户。post检查分owned_cleanup_pass/device_idle_observed；明确自己的进程/上下文退出后，别人占卡不妨碍任务清理完成或另一idle卡推进。归属不明才全局停派发，绝不kill他人。

## 8. 持续推进、暂停与恢复

每完成可验收阶段立即更新新日志、写实际结果/下一ready工作包并由唯一发布者commit/push私有Vault/main，核远端commit；发布失败修发布，不重跑实验。实验进行时不能修改其绑定源码，下一修订用新版本。

每次接续先读新STATE、账本/终端、新日志和Git status；核任务owned进程。已完成branch不重采；有终端未对账先幂等结算；已预留未dispatch核证后处理；活进程继续监测，不新发同job。以可重放事件为准重建STATE/index，不因会话切换从头来。

无idle卡：安全等待并报告，不能共享忙卡，不是物理失败。模板达到本次上限或下一步需改变未准科学/成功标准：暂停该分支，其他独立族可继续。全局停止新派发：预算耗尽/超额、实际消耗未知、源完整性失败、所有权不明、自己的残留无法清理、用户明确暂停；保留在途证据并按所有权安全收尾。

产品支持持续Goal时，用本新任务目标和完成谓词管理持续执行；不恢复旧Goal，不将其伪标完成以绕开产品状态。只有本节及预算/权限需要外部决定时才报告阻碍，不因完成一个helper提前交回。

## 9. 何时可宣布整个任务完成

所有以下项都有真实证据才能标本新任务完成：

1. F2完整inside闭环与同方案fresh确认通过。
2. F3完整离台保持、V/H、放回/退出闭环与同方案fresh确认通过。
3. F2-A/B、F3-A/B各六格完整，新增24条真实输入接受；三分支/current/anchor/prefix/事件/realization/终态及失败历史均验收。
4. 新48格引用索引正确，旧F1/F4来源保留且未重复计数；结构/时间/事件/反例/严格出口审计通过。
5. 新账本实际消费与终态一致，无unknown/活动预留/owned残留；各次post与owned清理可追溯，最新成果和总结已私有push核验。

最终报告分别列上述五项及新预算使用、失败/修复、产物路径和远端commit。24/48输入齐备不意味着科学机制、Temporal Identifiability或泛化已经证明；不追加formal草案/连续6条旧Goal条件。

## 可直接给Luna的启动语

> 请按本交接说明、V2.1主计划及七项执行附录，建立全新的F2/F3任务并连续推进至第9节全部完成。先读当前AGENTS与新日志，先落实必需运行合同再执行；允许独立空闲GPU0–7并行，每阶段自动私有commit/push。普通范围内修复、阶段调配、已声明备选和下一工作包自主推进，不逐helper求批。旧Goal仅历史；F1/F4不重采。预算/安全/科学边界按本文件执行，真实阻碍如实报告，不能用降门限或伪造验收完成目标。
